# --- Bootstrap .gitignore (Atlas) — Rhea extends this in US-E1-01 for web/ + api/ ---

# Claude Code local-only settings + per-agent memory (never commit personal/local state)
.claude/settings.local.json
.claude/agent-memory/
**/.claude/agent-memory/
**/.claude/settings.local.json
# Installed skills + transient agent worktrees are local tooling, not project code
.claude/skills/
.claude/worktrees/

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Secrets — only .env.example is ever committed
.env
.env.*
!.env.example

# Logs / temp
*.log

# --- Node / Next.js (web/) ---
node_modules/
.next/
out/
build/
dist/
.turbo/
*.tsbuildinfo
.npm/
.pnp.*
.yarn/*

# --- E2E (Playwright, e2e/) ---
test-results/
playwright-report/
playwright/.cache/
blob-report/

# --- Python / FastAPI (api/) ---
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
*.so

# Retrieval-smoke run artifacts (generated by US-QA-D10 test; regenerated each run)
docs/qa/eval/results/retrieval-smoke-*.json
# RAGAS harness run artifacts (generated by US-E7-00 runner; regenerated each run)
docs/qa/eval/results/ragas-*.json
# Backend regression run artifacts (generated by US-QA-D13 pack; regenerated each run).
# Only the committed README.md stays — the dated + latest reports are throwaway outputs.
docs/qa/regression/regression-*.md
# Shopping-journey agent transcript run artifacts (US-QA-D20 E2E; regenerated each run).
# Only the committed README.md stays — the dated + latest transcripts are throwaway output.
docs/qa/agent/shopping-journey-*.md
