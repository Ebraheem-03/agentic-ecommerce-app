export default function HomePage(): JSX.Element {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: "2rem" }}>
      <h1>Agentic AI E-Commerce</h1>
      <p>
        Scaffold is live. The full application arrives in later weeks — this page
        is a placeholder so the container and CI foundation can be built first.
      </p>
    </main>
  );
}
