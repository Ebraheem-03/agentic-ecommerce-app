import { PlaceholderPage } from "@/components/shell/PlaceholderPage";

export default function SellerPage(): JSX.Element {
  return (
    <PlaceholderPage
      testId="seller-dashboard-page"
      eyebrow="Sell on Hearth"
      title="The maker dashboard"
      body="Onboarding, listing, fulfilment, and the merchandising nudges land in a later story. The route is live so the seller journey is connected."
    />
  );
}
