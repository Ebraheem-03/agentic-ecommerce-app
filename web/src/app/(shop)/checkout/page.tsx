import { PlaceholderPage } from "@/components/shell/PlaceholderPage";

export default function CheckoutPage(): JSX.Element {
  return (
    <PlaceholderPage
      testId="checkout-page"
      eyebrow="Checkout"
      title="Agentic checkout, with an approval gate"
      body="The address and payment steps plus the human-in-the-loop approval gate land in a later story."
    />
  );
}
