# Hearth web — environment

# Base URL of the FastAPI backend the app talks to (server-side proxy + SSR).
# No trailing slash. Defaults to http://localhost:8000 when unset.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
