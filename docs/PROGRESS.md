# PROGRESS — live execution log

> Detailed execution timeline. `STATUS.md` is the compact current-state board; this is the
> append-only event log. Every delegated agent run logs: timestamp · agent · story · action ·
> status · files touched · tests run · next step. Newest entries at the bottom of each day.

## 2026-06-09 — Day 1: Repo & tooling foundation

| Time (UTC) | Agent | Story | Action | Status | Files | Tests | Next |
|---|---|---|---|---|---|---|---|
| 12:30 | Atlas | — | Read STATUS + week-1; confirmed repo state (scaffold untracked, only `main`). Asked human `[REVIEW]` remote policy → **push + real PRs**. | done | — | — | Build branch tree |
| 12:33 | Atlas | — | Committed project baseline to `main` (`6b1ea6d`); added bootstrap `.gitignore` + `PROGRESS.md`. Created `dev` + `integration/foundations`; pushed `main`,`dev`. | done | `.gitignore`, `docs/PROGRESS.md` | — | US-E1-02 |
| 12:36 | Atlas | US-E1-02 | `CONTRIBUTING.md` (branch flow + Conventional Commits) on `feature/foundation-conventions`; merged → `integration/foundations` (`6d91782`); pushed. | done | `CONTRIBUTING.md` | — | Delegate Rhea |
| 12:48 | Rhea | US-E1-01 | Monorepo `web/`+`api/`+`infra/`, README, extended `.gitignore`; minimal Next.js + FastAPI (`/health`). | done | `web/*`,`api/*`,`infra/*`,`README.md` | api `/health` smoke | US-E1-03 |
| 12:48 | Rhea | US-E1-03 | `docker-compose.yml` (web+api+db pgvector) + multi-stage Dockerfiles + `.env.example`. Live `up`: built, `/health` ok, `vector` ext on. | done | `docker-compose.yml`,`*/Dockerfile`,`.env.example`,`infra/db/init/01-extensions.sql` | `docker compose config` + live up | US-E1-04 |
| 12:48 | Rhea | US-E1-04 | GitHub Actions CI skeleton: lint·type-check·test·docker-build placeholders + aggregate `ci` gate (green no-op). ADRs 0009–0011. | done | `.github/workflows/ci.yml`, ADR 0009-0011 | YAML valid | Hand back to Atlas |
| 12:49 | Atlas | US-E1-01/03/04 | Reviewed compose + CI for fit; FF-merged `feature/foundation-scaffold` (3 commits) → `integration/foundations`; pushed. | done | — | — | Delegate Juno |
| 12:53 | Juno | US-QA-D01 | Playwright `e2e/` workspace, `npm run e2e` cmd, env contract, server-free `@smoke` + `e2e-smoke` CI job into `ci` gate. ADR 0012. | done | `e2e/*`, `.github/workflows/ci.yml` | `e2e:smoke` 2 passed (no server); full 2 passed/2 skipped | Hand back to Atlas |
| 12:54 | Atlas | US-QA-D01 | Reviewed CI slot; FF-merged `feature/foundation-e2e` → `integration/foundations`; pushed. | done | — | — | Consolidate docs + open PR |
| 12:50 | Atlas | — | Consolidated STATUS/PROGRESS/DAILY-LOG; opening PR `integration/foundations` → `dev`. | done | `docs/STATUS.md`,`docs/PROGRESS.md`,`docs/DAILY-LOG.md` | — | Day-7 integration / Day-2 brand |
| 13:00 | Atlas | US-E1-03 | GitGuardian flagged placeholder DB creds (false positive, no real secret). Fix (A): derive `DATABASE_URL` from `POSTGRES_*` vars + drop the literal default password (`CHANGE_ME` placeholder) — repo ships no real creds. (A2, human call) rewrote `integration/foundations` history so no commit ever contains the creds; force-pushed; deleted merged feature branches; de-pinned volatile SHAs from this log. | done | `docker-compose.yml`, `.env.example`, `docs/PROGRESS.md` | `docker compose config` resolves; `git log -S` clean | Confirm PR #1 GitGuardian green |
| 13:10 | Atlas | — | PR #1 GitGuardian green (9/9). Prepped Day 2: created `integration/design` (off foundations, ADR-0013), `docs/brand/` + `docs/qa/` READMEs, STATUS handoff block. | done | `docs/brand/README.md`, `docs/qa/README.md`, `docs/decisions/0013-*.md`, `docs/STATUS.md` | — | Start Day 2 (Nova) |

## 2026-06-10 — Day 2: Brand & design language

| Time (UTC) | Agent | Story | Action | Status | Files | Tests | Next |
|---|---|---|---|---|---|---|---|
| 12:05 | Atlas | — | Read STATUS + week-1 Day-2 block + brand/qa READMEs. Ran the 4 [REVIEW] stories sequentially through human gates. | done | — | — | Delegate Nova US-E2-01 |
| 12:06 | Nova | US-E2-01 | Brand brief: name options (Hearth/Maven/Tend), positioning, tone, personas, design hand-off. | done | `docs/brand/brand-brief.md` | — | Human name pick |
| 12:08 | Atlas | US-E2-01 | Human locked **Hearth**. Committed `feature/brand-brief` → FF `integration/design` (`d00c447`). | done | `docs/brand/brand-brief.md` | — | US-E2-02 |
| 12:10 | Nova | US-E2-02 | Round 1: 3 logo directions as code-native SVG (Stitch unreachable from subagent sandbox). | done | `docs/brand/logo-directions.md`, 6 SVG | XML well-formed | Render for human |
| 12:12 | Atlas | US-E2-02 | No rasterizer present → rendered SVGs via Playwright/Chromium (installed chromium). Round 1 **rejected** by human. | done | — | playwright render | Round 2 |
| 12:16 | Nova | US-E2-02 | Round 2: 6 distinct concepts (typographic/geometric/ember/monogram/guidance/wildcard); deleted round-1 assets. | done | `logo-directions.md`, 12 SVG | XML well-formed | Render board |
| 12:20 | Atlas | US-E2-02 | Rendered round-2 board (light/dark, 120→16px) in headed Chromium. Human picked **"Keystone"** (Concept 2). Canonical assets + ADR-0014. Committed `feature/brand-logo` → FF (`df20f69`). | done | `hearth-logo-{mark,lockup}.svg`, ADR-0014 | playwright render | US-E2-03 + US-QA-D02 |
| 12:22 | Juno | US-QA-D02 | QA matrix v0: 4 personas, 17 journeys (`J-*`, 1:1→D03), 9 WCAG AA, 5 RAGAS gates. | done | `docs/qa/qa-matrix.md` | — | Human review |
| 12:24 | Atlas | — | Removed stray nested `e2e/.claude/agent-memory` (subagent local state); hardened `.gitignore` with `**/.claude/...`. | done | `.gitignore` | — | — |
| 12:26 | Atlas | US-QA-D02 | Human approved matrix as-is (faithfulness ≥0.90). Committed `feature/qa-matrix-d02` → FF (`86d122d`). | done | `docs/qa/qa-matrix.md` | — | US-E2-03 |
| 12:28 | Nova | US-E2-03 | Palette + type (run in background): Clay `#B5512F`/Ember `#E08A3C`, Fraunces+Outfit, warm-neutral ramp, semantic, light+dark, WCAG AA computed; self-contained specimen HTML. | done | `palette-and-type.md`, `preview/palette-and-type.html` | contrast verified | Render for human |
| 12:34 | Atlas | US-E2-03 | Rendered specimen in headed Chromium. Human approved + chose Fraunces wordmark; recut lockup SVG Outfit→Fraunces. Committed `feature/brand-palette` → FF (`397f6ec`). | done | `hearth-logo-lockup.svg`, palette files | playwright render | End-of-day checkpoint |
| 12:36 | Atlas | — | Day-2 close: updated STATUS/PROGRESS/DAILY-LOG; chore commit (.gitignore + tracking); pushed `integration/design`. | done | `docs/STATUS.md`, `docs/PROGRESS.md`, `docs/DAILY-LOG.md`, `.gitignore` | — | Day 3 (tokens→code, screens) |

## 2026-06-11 — Day 3: Design system & key screens

| Time (UTC) | Agent | Story | Action | Status | Files | Tests | Next |
|---|---|---|---|---|---|---|---|
| 15:50 | Atlas | — | Read STATUS + week-1 Day-3 block + brand hand-off (`palette-and-type.md` §7) + qa-matrix. Confirmed `web/` is bare Next 14 (no Tailwind). Stack call: **Tailwind v4 (CSS-first)** for tokens. Launched Iris (US-E2-04) + Juno (US-QA-D03) in parallel, isolated worktrees. | done | — | — | Integrate both |
| 16:00 | Juno | US-QA-D03 | E2E spec skeleton: 17 `J-*` spec stubs (1:1 w/ matrix, all `test.fixme`), `data-testid` registry (`test-ids.md`) + typed `_selectors.ts`; agent voice as shared `agent-*` namespace. ADR-0015(→0016). | done | `docs/qa/test-ids.md`, `e2e/tests/J-*.spec.ts` (17), `e2e/tests/_selectors.ts`, `e2e/README.md` | `e2e:smoke` 2 passed; full 2 passed/36 skipped; `tsc` clean | Atlas integrate |
| 16:05 | Atlas | US-QA-D03 | FF-merged `feature/qa-e2e-skeleton-d03`. ADR collision (both agents took 0015) → renumbered QA to **0016**; gitignored `.claude/skills/` + `.claude/worktrees/` (had been swept in as embedded repos). | done | ADR 0016, `.gitignore` | — | Render tokens |
| 16:10 | Iris | US-E2-04 | Tokens→code: Tailwind v4 (`@theme inline`, no config), all spec vars on `:root`+`[data-theme=dark]`, Fraunces/Outfit via `next/font`, `Button` (ember+ink primary / clay+white brand), `HearthMark` duotone, `/tokens` specimen + theme toggle. ADR-0015. | done | `web/src/app/globals.css`, `web/src/app/tokens/page.tsx`, `web/src/components/{Button,HearthMark,ThemeToggle}.tsx`, `layout.tsx`, ADR-0015 | build✓ type-check✓ lint✓ | Render for human |
| 16:14 | Atlas | US-E2-04 | Rendered `/tokens` in headed Chromium (dev server w/ ipv4-first font-fetch workaround). Human **approved w/ tweaks**: dark-mode tint badges had invisible text (light `--text` on always-light wash). | review | — | playwright screenshots L/D | Fix + merge |
| 16:18 | Atlas | US-E2-04 | Fix: tint-badge on-color → fixed `text-n-900` (dark ink in both themes). Re-rendered dark ✓. Committed on branch; **merged** `feature/design-system-tokens` → `integration/design` (no-ff `915ac62`). | done | `web/src/app/tokens/page.tsx` | type-check✓ lint✓ | Nova US-E2-05 |
| 16:30 | Atlas | US-E2-05 | Tooling call: hi-fi screens **code-native, token-truthful HTML** (not Stitch), per ADR-0014 precedent → screens consume live tokens + carry `data-testid` registry. Launched Nova (isolated worktree). | done | ADR-0017 | — | Render board |
| 16:45 | Nova | US-E2-05 | 7 hi-fi screens + index board under `docs/brand/preview/screens/`: `tokens.css` (mirror of globals), `screens.css` shell, home/search/product/cart/checkout/order-status/seller-dashboard. Agent voice as signature (grounded recs, honest refusals, seller merch nudge). All testids from registry, none new. | done | `docs/brand/preview/screens/*` (10 files) | rendered home/search/seller @1280 in e2e Chromium, 0 errors | Render for human |
| 16:55 | Atlas | US-E2-05 | Rendered all 7 + board in headed Chromium. Human **approved w/ tweaks**: (1) qty was a `<select>` dropdown → wants conventional stepper; (2) photo placeholders too flat; + general polish; agent tone & home-panel left to Nova. | review | — | playwright screenshots ×7 | Apply tweaks |
| 17:05 | Atlas | US-E2-05 | Applied tweaks directly in Nova's worktree (SendMessage unavailable; cold respawn couldn't target the branch): −/value/+ `.qty` stepper on product+cart (testids preserved), `.ph` studio-sheen+vignette+frame placeholders. Agent voice reviewed — already confident/honest, kept. | done | `screens.css`, `product.html`, `cart.html` | re-rendered product/cart/home, steppers + framed photos verify | Merge |
| 17:12 | Atlas | US-E2-05 | Committed tweak pass; **merged** `feature/design-system-screens` → `integration/design` (no-ff). ADR-0017 (code-native token-truthful screens). | done | — | — | Day-3 close |
| 17:18 | Atlas | — | Day-3 close: STATUS/PROGRESS/DAILY-LOG; pushing `integration/design`. All 3 stories integrated; ERD (Day 4) is next, new branch `integration/data`. | done | `docs/STATUS.md`, `docs/PROGRESS.md`, `docs/DAILY-LOG.md` | — | Day 4 (Sable: ERD + schema) |

## 2026-06-12 — Day 4: Data model (ERD + schema/migrations)

| Time (UTC) | Agent | Story | Action | Status | Files | Tests | Next |
|---|---|---|---|---|---|---|---|
| 15:30 | Sable | US-E3-01 | ERD **[REVIEW]**-approved by human. Applied rulings to `docs/data/erd.md`: header→APPROVED; added `reviews` entity (+ derived-rollup note on `products.rating_avg/count`); added minimal `payments`; kept sessions/conversations/messages/agent_actions; resolved §6 decisions + §7 open questions inline; `EMBED_DIM`-driven vector dim (default 768, Echo owns final). | done | `docs/data/erd.md` | — | Build migrations |
| 15:45 | Sable | US-E3-02 | Initialized Alembic in `api/` (env.py reads `DATABASE_URL` from env — no creds committed; `target_metadata=Base.metadata`). Added deps (sqlalchemy 2 / alembic / psycopg3 / pgvector / pydantic-settings) to requirements + pyproject. Authored 3 migrations: 0001 pgvector ext + 14 enums; 0002 core schema (20 tables, FKs, CHECKs, partial-unique one-open-cart, UNIQUEs on email-live/slug/sku/order_number/inventory.variant_id, UUIDv7 fn + defaults); 0003 embeddings `vector(EMBED_DIM)` + HNSW cosine index. | done | `api/alembic.ini`, `api/alembic/{env.py,script.py.mako,README.md}`, `api/alembic/versions/000{1,2,3}_*.py`, `api/app/core/config.py`, `api/app/db/base.py`, `api/{requirements.txt,pyproject.toml,Dockerfile,.dockerignore}`, `docker-compose.yml`, `.env.example` | — | Prove on fresh DB |
| 16:00 | Sable | US-E3-02 | Proved on a throwaway `pgvector/pgvector:pg16` (port 5433): **`alembic upgrade head` ✓** (3 revs); verified 21 tables + 14 enums, `uuid_generate_v7()` emits version-nibble 7, `embedding` = `vector(768)`, partial-unique cart index + HNSW present; functional checks: dup-open-cart rejected, review rating CHECK rejects 6. Then **`downgrade base` ✓** (only `alembic_version` left; 0 enums, fn dropped) → **`upgrade head` ✓ again**. Round-trip de-risks US-QA-D04. ruff + mypy(strict, app) clean. | done | — | `alembic upgrade head`/`downgrade base`/`upgrade head`; ruff✓ mypy✓; psql constraint checks | Commit + ADR-0018 |
| 16:10 | Sable | US-E3-02 | ADR-0018 (UUIDv7 in-DB SQL fn — no native uuidv7 in PG16; configurable pgvector dim/HNSW index). Committed `feature/db-schema-migrations` → FF `integration/data`. STATUS/PROGRESS/DAILY-LOG updated. | done | `docs/decisions/0018-uuidv7-and-pgvector-dim.md`, `docs/STATUS.md`, `docs/PROGRESS.md`, `docs/DAILY-LOG.md` | — | US-QA-D04 (Juno): migrate fresh DB + rollback + seed-reset contract |
| 10:33 | Juno | US-QA-D04 | Built the migration **contract** turning Sable's one-off proof into a repeatable, CI-runnable gate. pytest harness (`tests/db/`): per-test throwaway DB (CREATE/DROP DATABASE on the env-driven server, random name, full cleanup), assertions **by name** not just count. Scenario 1 `test_clean_create`: empty→`upgrade head`→**21 app tables + 14 enums** (named sets), pgvector ext, `uuid_generate_v7()`, HNSW index on `embeddings.embedding` (verified access method = `hnsw`), partial-unique one-open-cart, reviews rating CHECK, inventory.variant_id UNIQUE. Scenario 2 `test_rollback_to_base_then_roundtrip`: `downgrade base`→0 tables/0 enums/fn gone, only empty `alembic_version`; `upgrade head` again→full inventory at `0003_embeddings`. Scenario 3 reset script `scripts/db_reset.sh`: guarded (refuses non-`dev|test|local` DB names w/o `--force`), idempotent, drop+recreate+migrate, seed-hook for US-E3-03. ADR-0019 (throwaway-DB harness + config-reload-per-test note). Plan doc written. **Defect-grade finding:** `0001` downgrade intentionally leaves the `vector` extension after `downgrade base` — documented exception, asserted in the test (not a bug, but pinned). | done | `api/tests/db/{__init__.py,conftest.py,test_migrations.py}`, `api/scripts/db_reset.sh`, `docs/qa/db-migration-test-plan.md`, `docs/decisions/0019-migration-contract-throwaway-db.md`, `docs/STATUS.md`, `docs/PROGRESS.md` | **pytest 3 passed** (2 migration + 1 health) against `pgvector/pgvector:pg16`; skips gracefully w/o DB url; reset script: guard-refusal ✓, dev reset ✓ (dropped leftover marker, 21 tables), idempotent 2nd run ✓; ruff ✓ mypy(strict, app+tests) ✓ | Commit `integration/data`; CI wiring documented (Postgres-service `test-api` job) but left for Day-7 W1 integration so the shared gate stays green |

## 2026-06-13 — Day 5: Data-access models + seed

| Time (UTC) | Agent | Story | Action | Status | Files | Tests | Next |
|---|---|---|---|---|---|---|---|
| 09:40 | Sable | US-E3-04 | SQLAlchemy 2.0 typed ORM (`Mapped`/`mapped_column`) for all **21 tables** on the shared `Base`, split by domain under `api/app/db/models/` (`user/catalog/cart/order/returns/policy/agent/embedding` + `_shared` helpers). Native PG enums referenced by name (`create_type=False`, labels mirrored from 0001 so writes validate); server defaults mirrored (`uuid_generate_v7()`/`now()`/bools/jsonb); partial-unique cart + email-live indexes; `embeddings.embedding` = pgvector `Vector(settings.embed_dim)` (never hardcoded) + HNSW cosine index. Thin DA layer: `app/db/session.py` (engine + `sessionmaker` + `session_scope`). | done | `api/app/db/models/*` (10 files), `api/app/db/session.py` | — | Prove parity |
| 09:55 | Sable | US-E3-04 | Parity tests vs a **real migrated** throwaway DB (reuse Day-4 `migration_db` fixture): `test_no_metadata_diff` (Alembic `compare_metadata` finds no drift; name-only inline-UNIQUE vs `uq_*` renames filtered as non-drift), `test_table_and_column_parity` (per-table column sets), `test_core_roundtrip` (user→store→product→variant→inventory + an `embeddings` row at `settings.embed_dim` — exercises uuidv7/now() defaults, native enums, vector insert/readback). **pytest 6 passed** (4 new + 2 migration) on `pgvector/pgvector:pg16` @5433; ruff ✓ mypy(strict, app+tests) ✓. FF-merged `feature/data-access-models` → `integration/data`. | done | `api/tests/db/test_models_parity.py`, `api/tests/db/test_models_roundtrip.py` | pytest 6 passed; ruff✓ mypy✓ | US-E3-03 seed |
| 11:20 | Sable | US-E3-03 | Seed `app.db.seed` (runnable `python -m app.db.seed`) for a believable **Hearth** maker marketplace via Story-1 ORM: 12 users (5 buyers/5 sellers/support/admin, all roles), 5 active stores, 10 products, 15 variants (real SKUs/price_minor/options jsonb), 15 inventory rows (low-stock + restock_eta spread for interesting agent answers), product images, 12 reviews (1–5 CHECK respected) with **recomputed `rating_avg/rating_count` rollups**, and **7 policies** (platform returns/shipping/payments/platform + per-store care/returns) as genuine quotable RAG prose. Embeddings for products+policies via a single Echo-replaceable `embed_text` stub (`model='seed-stub'`, vector length = `settings.embed_dim`, **never hardcoded**). **Idempotent** — upserts on natural keys (email-live/slug/sku/(source_type,source_id,chunk_index)). Wired into `db_reset.sh` SEED hook (`SEED=0` opt-out). | done | `api/app/db/seed/{__init__,__main__,run,data,embeddings}.py`, `api/scripts/db_reset.sh` | seed×2 counts unchanged (users=12 stores=5 products=10 variants=15 inventory=15 reviews=12 policies=7 embeddings=17, all dim 768); `db_reset.sh`×2 (reset→migrate→seed) idempotent; pytest 8 passed; ruff✓ mypy(strict)✓ | FF-merge → `integration/data`; Echo swaps real embeddings |
| 14:43 | Juno | US-QA-D05 | Golden eval dataset v0: **25 buyer/support Q&A** records (`docs/qa/eval/golden-v0.jsonl`, JSONL — RAGAS/HF-native) each with `question`, grounded `expected_answer`, `source_docs` (machine-checkable seed refs: product `slug` / variant `sku` / policy `kind@store|platform`), and `tags` (persona/category/difficulty/`expects_refusal`). Strictly grounded in Sable's seed — no invented SKUs/prices/policy. Spread: buyer 18 / support 7; catalog 12 / inventory 4 / reviews 1 / returns 3 / shipping 1 / payments 1 / care 2 / policy 1; direct 11 / multi-fact 12 / combine 2; **4 refusal items** (no 36" belt, no lavender candle, out-of-catalog steel bottle, no-return opened bath&body). Embedding-agnostic (no EMBED_DIM/vector refs). README documents schema + RAGAS metric mapping + exactly what Echo adds Week-2 to execute. Validator `api/tests/qa/test_golden_eval.py`: static well-formedness (count==25, unique ids, controlled-vocab tags, ≥3 refusals, combine⇒≥2 docs) + **DB-backed seed-resolve** reusing Sable's `migration_db` throwaway-DB fixture (migrate→seed→assert every source_doc resolves to a real row = anti-drift). Validator caught one self-inconsistency (EVAL-023 mistagged combine) — fixed. | done | `docs/qa/eval/golden-v0.jsonl`, `docs/qa/eval/README.md`, `api/tests/qa/{__init__,conftest,test_golden_eval}.py` | pytest 8 passed on pgvector/pgvector:pg16 @55432 (incl. seed-resolve); 7 passed +1 skipped w/o DB (CI-safe); ruff✓ mypy(strict)✓ | FF-merge → `integration/data`; Echo wires RAGAS runner Week-2 |

## 2026-06-14 — Day 6: API contract inventory

| Time (UTC) | Agent | Story | Action | Status | Files | Tests | Next |
|---|---|---|---|---|---|---|---|
| 16:30 | Orion | US-E4-00 | **API contract DRAFT** ([REVIEW], no business logic). Route inventory of **36 operations** (auth 4 · catalog 5 · search 1 · cart 4 · orders 6 · returns 3 · agent 3 · seller 6 · admin 3 · health 1) in `docs/api/contract-v0.md`, each mapped to method/path/auth-role/req+resp model/status/`J-*` journey. **Pydantic v2 DTOs** in `api/app/schemas/` (enums mirror the 14 native PG enums by value; DTOs separate from ORM). **One success envelope** `{data, meta}` (cursor pagination) + **one canonical error** `{error:{code,message,details}}` with a **closed 16-code set** mapped to HTTP. Test-mode payment intent→confirm (deterministic decline). Agent reply shape = message + `citations[]` + `recommendations[]` + `action.outcome∈applied\|refused\|hitl_deferred` (the contract Echo implements Week-2). **Scalar** API reference wired at `/docs` (Swagger/ReDoc disabled); handlers are `501 not_implemented` stubs so the reference renders for review. Deps added: `scalar-fastapi`, `email-validator`. Schema gaps NOTED not changed (return-window source, guardrail storage, seller↔order join, nudge persistence). 6 [REVIEW] decisions surfaced (auth=session-token, envelope=wrapped, pagination=cursor, search=keyword-v0, agent=single-response, payment=intent/confirm). | needs-review | `docs/api/contract-v0.md`, `api/app/schemas/*` (10), `api/app/api/routers/*` (9)+`_contract.py`, `api/app/main.py`, `api/requirements.txt`, `api/pyproject.toml` | app boots; `/openapi.json` 200 w/ 36 ops + 92 component schemas; `/docs` Scalar 200 (no swagger-ui); ruff✓ mypy(strict, 47 files)✓ | **Human ratifies 6 [REVIEW] decisions** → Atlas merges `feature/api-contracts-inventory` → `integration/backend`; Week-2 implements handlers |
| 18:10 | Orion | US-E4-00 | **Apply [REVIEW] rulings + LOCK contract.** Human ratified all 6 decisions (2026-06-14). 5 kept as drafted (auth=session-token, envelope=wrapped `{data,meta}`, pagination=opaque cursor, search=keyword-v0 semantic-ready, payment=intent→confirm). **Decision 5 CHANGED: agent chat → SSE streaming** (`text/event-stream`), replacing the single-JSON `AgentReply`. Re-spec'd `app/schemas/agent.py` with typed per-frame payloads `TokenEvent`/`CitationsEvent`/`DoneEvent`/`StreamError` (+`SseEventCatalog` ref) and rewrote `app/api/routers/agent.py` so both message-producing POSTs advertise a single `200 text/event-stream` (`response_class=StreamingResponse`) and yield an illustrative `token→citations→done` sequence; transcript GET stays JSON. Added reference route `GET /agent/_sse-events` so the event shapes render in Scalar. Documented event protocol + **deterministic payment-decline trigger** (`PaymentConfirmRequest.outcome="failed"` → `402 payment_declined`) in `docs/api/contract-v0.md`; converted §5 [REVIEW] → **DECISIONS — RATIFIED 2026-06-14** table. Wrote **ADR-0021**. | done (locked) | `docs/api/contract-v0.md`, `api/app/schemas/agent.py`, `api/app/api/routers/agent.py`, `docs/decisions/0021-api-contract-v0-ratified.md` | app boots; `/docs` Scalar 200 shows both agent routes as `text/event-stream` + 5 SSE event schemas; both POSTs single `text/event-stream` 200; live stream emits 4 ordered frames; ruff✓ mypy(strict,47)✓ | Atlas FF-merges `feature/api-contracts-inventory` → `integration/backend`; Week-2 Echo implements agent SSE loop, Juno wires J-BUY-04 decline fixture (`outcome="failed"`) |
| 18:30 | Juno | US-QA-D06 | **E2E fixture plan + executable spine.** Built the "one source → 3 layers" canonical fixture system. `api/tests/fixtures/handles.py` derives stable handles FROM the seed (never duplicates): 6 personas (buyer×2/seller×2/support/admin by email), 3 products + 4 variants spanning stock edges (`mug_in_stock` q24, `mug_low_stock` q3, `wallet_oos` q0), 5 policies keyed `<kind>@<scope>`, return-reason set, and the payment test-mode switch (`outcome="failed"`→`402 payment_declined`). `manifest.py` emits `fixture-manifest.json` (committed) for the non-Python layers; spine test asserts on-disk JSON == live handles (anti-drift). `api/tests/conftest.py` adds `seeded_db` (reuses Sable's `migration_db`: migrate→seed throwaway DB), `handles` (every handle resolved to a real seeded UUID), `api_client`/`persona_client` (`ContractClient` over Starlette TestClient/ASGI; speaks contract-v0 `{data,meta}` + Bearer; `login(persona)` via real `/auth/login`). Browser layer imports the manifest via `e2e/tests/_fixtures.ts` (typed). `docs/qa/fixture-plan.md` = catalog + **per-journey matrix (all 17 J-* mapped → fixtures → layers)**, J-BUY-04 explicitly uses the decline trigger; factory-vs-seed set specified (8 factories for Week-2). | done | `docs/qa/fixture-plan.md`, `api/tests/fixtures/{__init__,handles,manifest,test_fixture_spine}.py`, `api/tests/fixtures/fixture-manifest.json`, `api/tests/conftest.py`, `e2e/tests/_fixtures.ts`, `e2e/tsconfig.json` | throwaway DB on :55444; `pytest tests/fixtures/` → **8 passed, 6 xfailed** (per-persona login xfail pending Week-2 auth handler); full suite 24 passed/6 xfailed; ruff✓ mypy(strict)✓ | Week-2: auth-backed seeding writes the pinned test passwords (flips login xfails green); cart/order/return handlers unlock the 8 factories; FF-merge → `integration/backend` (held, not pushed, for Day-7) |

## 2026-06-15 — Day 7: W1 integration review (capstone)

| Time (UTC) | Agent | Story | Action | Status | Files | Tests | Next |
|---|---|---|---|---|---|---|---|
| 09:00 | Atlas | US-QA-D07 | **Topology assessment.** Mapped the four W1 lines: all fork from scaffold `6b1ea6d`; `dev` still at scaffold. Found they are a **strict linear chain** (`foundations ⊂ design ⊂ data ⊂ backend`) — `integration/backend` already contains every other line, so **zero cross-branch conflicts** (the STATUS "expect overlaps" warning didn't apply). Found CI was green-by-**placeholder** (echo stubs) — the real Postgres-service `test-api` job documented Day-4 still unwired. `integration/data`+`backend` unpushed. **[REVIEW] → human chose: 1 consolidated PR** (`backend → dev`), roll up PR #1 + the (never-opened) design PR. | done | — | — | Dry run + wire CI |
| 09:30 | Atlas/Juno | US-QA-D07 | **Local integration dry run — green by code** on `pgvector/pgvector:pg16` (alt host port 55444; 5432 taken by stray `local-postgres`). docker `db` healthy; migrations+seed exercised via `seeded_db`; **ruff✓ mypy(strict, 47 files)✓ pytest 24 passed + 6 xfailed**; app boots `/health` 200, Scalar `/docs` 200, `POST /agent/conversations` = `text/event-stream`; placeholder E2E smoke 2 passed. | done | — | ruff✓ mypy✓ pytest 24p/6xf; curl health/docs/agent; e2e:smoke 2 passed | Wire real CI |
| 10:00 | Rhea | US-QA-D07 | **Wired the real CI gate** (the deferred Day-4 job). Replaced `ci.yml` placeholders: lint/type-check (web)=`npm ci`+`next lint`+`tsc --noEmit`; lint/type-check (api)=`ruff check .`+`mypy --strict`; **test-api**=`pytest` against a `pgvector/pgvector:pg16` service. docker-build stays a documented no-op (compose proven in the dry run). web lint+type-check verified locally clean before wiring. | done | `.github/workflows/ci.yml` | yaml valid; web lint+tsc clean locally; ruff/mypy/pytest reproduced | Push + PR |
| 10:30 | Atlas | US-QA-D07 | Pushed `integration/backend` (first push) + opened consolidated PR. **GitGuardian failed: 8 secrets / 2 incidents** — both *test/ephemeral* creds, no real secrets: (1) CI service `postgres:postgres` literal, (2) the pinned fixture test password in `handles.py`+`fixture-manifest.json`. Fetched exact findings via the checks API. **[REVIEW] → human chose: rewrite history + env-derive the password.** | done | — | GitGuardian: 8 secrets uncovered | Remediate |
| 11:00 | Rhea/Juno | US-QA-D07 | **GitGuardian remediation.** CI service → `POSTGRES_HOST_AUTH_METHOD=trust` (no password literal; validated locally: 15 DB tests pass against an identical passwordless pgvector container). Fixture password → **env-derived** (`TEST_PASSWORD = os.environ.get("HEARTH_TEST_PASSWORD", "CHANGE_ME_test_pw")`); manifest regenerated; spine test green. Squashed the CI commits clean; scrubbed the historical literal with `git filter-repo` **on the range `7745495..backend`** (graft point = parent of the literal-introducing commit) so the scaffold base shared with `dev` (`6b1ea6d`) stays byte-identical. (A first whole-branch filter-repo severed the common base — caught + redone from a `backup/pre-scrub` safety ref.) Force-pushed (authorized). | done | `.github/workflows/ci.yml`, `api/tests/fixtures/handles.py`, `api/tests/fixtures/fixture-manifest.json`, `docs/qa/fixture-plan.md` | local ruff✓ mypy✓ pytest 24p/6xf; trust container 15 passed; merge-base(dev,backend)=6b1ea6d preserved | Open PR + merge |
| 11:30 | Atlas | US-QA-D07 | Opened consolidated **PR #3** (`integration/backend → dev`); #2 was auto-closed by GitHub during the force-push and un-reopenable. **Full gate green by code:** GitGuardian *No secrets detected ✅* + ci, web lint/type-check, api lint/type-check, **test (api) (real pgvector)**, e2e-smoke, docker-build all pass. **Merged → `dev`** (`385e541`, merge commit preserving per-day history). PR #1 (foundations) auto-marked **merged** (its commits are on `dev`); design line included in the same merge. No open PRs remain. | done | — | PR #3 all checks green; merged | Update docs, close Day 7 |

## 2026-06-16 — Day 8 (W2D1): Backend skeleton gap-close — runtime error envelope + CORS + API smoke

| Time (UTC) | Agent | Story | Action | Status | Files | Tests | Next |
|---|---|---|---|---|---|---|---|
| 09:00 | Atlas | — | Read STATUS + week-2 Day-8 block. **Reconciliation finding:** the planned Day-8 stories (US-E4-01 skeleton/health/error-envelope · US-E4-02 Scalar docs · US-E4-03 Pydantic base schemas) were **front-loaded into Day 6** (the contract-v0 build) and already on `dev` — verified by code (app boots, `/health` 200, `/docs` Scalar, 10 schema modules + envelope present). **Two genuine gaps remained:** (1) the canonical error envelope was *documented* in OpenAPI but never *emitted* at runtime — `stub()` raised a bare `HTTPException`→`{"detail":...}`; (2) **no CORS** anywhere (frontend can't call the API; US-QA-D08 tests it). Scoped Day-8 = close those two + the real US-QA-D08 smoke. All `[AFK]`, no `[REVIEW]`. | done | — | — | Housekeeping + delegate Orion |
| 09:10 | Atlas | — | Housekeeping: pruned 3 stale `worktree-agent-*` branches; fast-forwarded `integration/backend` (was 2 behind `dev`) → `ea1dd66`; cut `feature/api-cors-envelope` off it. | done | — | — | Orion |
| 09:30 | Orion | US-E4-01 | **Runtime error envelope + CORS.** New `app/core/errors.py`: `APIError(status,code,message,details)` + `install_error_handlers(app)` registering handlers for `APIError`, Starlette `HTTPException` (explicit status→closed-`ErrorCode` map + status-class fallback), `RequestValidationError` (422 `validation_error`, per-field errors in `details`), and a catch-all `Exception`→500 `internal_error` (no internals leaked). `_contract.stub()` now raises `APIError(501, not_implemented)` so every stub returns the canonical `{"error":{code,message,details}}`. CORS: env-driven `cors_origins` on `Settings` (`HEARTH_CORS_ORIGINS`, comma-split via `NoDecode`+before-validator; no global env_prefix so bare `DATABASE_URL`/`EMBED_DIM` keep working), `CORSMiddleware` in `main.py` (allow_credentials, methods, headers `Authorization`/`Content-Type`/`Idempotency-Key`). `.env.example` updated. ADR-0022. | done | `api/app/core/errors.py`, `api/app/core/config.py`, `api/app/main.py`, `api/app/api/_contract.py`, `api/tests/test_error_envelope.py`, `.env.example`, `docs/decisions/0022-runtime-error-envelope-and-cors.md` | ruff✓ mypy(strict, 48 files)✓ pytest 19 passed/15 skipped (DB tests, no PG); commit `5e7ed35` | Atlas review |
| 10:00 | Atlas | US-E4-01 | **Review — accepted.** Re-ran gate independently (ruff✓ mypy(strict,48)✓) + live boot smoke (no DB): `/health` 200 · `POST /auth/login` stub → **501** canonical `not_implemented` · `GET /nope` → **404** `not_found` · invalid body → **422** `validation_error` · CORS preflight from allowed origin → 200 + `access-control-allow-origin`. errors-module placement (app-core, not `_contract`) + the HTTP→code map are sound. | done | — | ruff✓ mypy✓; 5/5 live envelope+CORS checks pass | Delegate Juno |
| 10:30 | Juno | US-QA-D08 | **Named API smoke suite.** New `api/tests/test_api_smoke.py` (15 tests, DB-free, `TestClient` only): app boot + full router inventory mounted (parametrized 1 path/group — auth/catalog/search/cart/orders/returns/agent/seller/admin; catalog mounts at `/products`+`/stores`, no `/catalog` prefix), `/docs`→200 HTML w/ Scalar marker, `/openapi.json`→200 valid (`openapi` 3.x + non-empty `paths` + every group present), CORS positive (allowed `Origin` echoes ACAO) **and negative** (`evil.example` preflight not echoed → Starlette 400), + cross-group envelope spot-check (`GET /products`→501) proving the envelope is app-wide. Left Orion's `test_error_envelope.py` as the per-code detail layer (non-overlapping scopes, documented in both docstrings). | done | `api/tests/test_api_smoke.py` | ruff✓ mypy(strict,48)✓ **pytest 34 passed/15 skipped**; `pytest --co` collects all 15; commit `6727bb5` | Atlas integrate |
| 11:00 | Atlas | US-E4-01/QA-D08 | **Integration check + merge to integration line.** Independent full gate green by code (ruff✓ mypy(strict,48)✓ **34 passed/15 skipped** — skips are DB tests, no PG). FF-merged `feature/api-cors-envelope` (`5e7ed35`+`6727bb5`) → `integration/backend` (`6727bb5`); pushed `feature/*` + `integration/backend` to origin. `dev` PR **held for the Day-14 W2 gate** (US-QA-D14), mirroring W1's Day-7 cadence. | done | `docs/STATUS.md`, `docs/PROGRESS.md`, `docs/DAILY-LOG.md` | gate green by code; pushed | Surface auth-pull-forward decision; Day-9 auth (US-E4-04 `[REVIEW]`) |

## 2026-06-17 — Day 9 (W2D2): Authentication & authorization

| Time (UTC) | Agent | Story | Action | Status | Files | Tests | Next |
|---|---|---|---|---|---|---|---|
| 09:00 | Atlas | US-E4-04 | Read STATUS + week-2 Day-9 block. Loaded auth surface (contract §1.3 + §5-decision-1, auth schemas, `users`/`sessions` ORM, `ErrorCode` enum, seed personas, the 6 `/auth/login` xfails). **Mechanism already ratified** (opaque session-token, ADR-0021); only 3 impl calls open → put them to the human as the US-E4-04 `[REVIEW]`. | done | — | — | Record decision |
| 09:20 | Atlas | US-E4-04 | **`[REVIEW]` ratified by human.** (1) Password hashing = **argon2id** (argon2-cffi). (2) Session = **sliding 7-day** opaque token (SHA-256 of `token_urlsafe(32)` stored in `sessions.token_hash`; each authed request bumps `expires_at`). (3) Email-verify = **real `email_verified` column + login gated `403` while unverified** — so **auth-backed seeding must mark personas verified** to flip the 6 login xfails. Authored **ADR-0023**; flipped contract `[REVIEW] §5`→RATIFIED + annotated decision-1 row. *(Plan row says "JWT" loosely; contract overrides — opaque tokens, not JWT.)* | done | `docs/decisions/0023-auth-hashing-sessions-email-verify.md`, `docs/api/contract-v0.md` | — | Delegate Orion (US-E4-04 + US-E4-05) |
| 09:40 | Orion | US-E4-04 / US-E4-05 | **Auth handlers + scope dep.** Migration `0004` (`users.email_verified bool NOT NULL DEFAULT false`, reversible) + ORM col. `app/core/security.py` (argon2id hash w/ constant-time `None`-hash failure; SHA-256 opaque-token helpers, `token_urlsafe(32)`). `app/api/deps.py` (US-E4-05): `require_auth` Bearer→SHA-256→live-session→**sliding 7d bump**→user, `require_user`, `require_role(*roles)`→403. Real `/auth/{register,login,logout,me,verify}`: register→unverified+session (409 `email_taken` live dup); login→401 bad-creds/unknown, **403 unverified**; logout deletes row; verify flips flag — all via `APIError`. Auth-backed seeding writes per-persona argon2(`HEARTH_TEST_PASSWORD`) + `email_verified=true` (idempotent). **Deviation:** `EmailStr` rejects RFC-6761 `.test` TLD → narrow startup shim allows only `.test` (`app/core/email_compat.py`, **ADR-0024**) — else every persona login 422s. | done | `api/alembic/versions/0004_*.py`, `api/app/db/models/user.py`, `api/app/core/security.py`, `api/app/core/email_compat.py`, `api/app/api/deps.py`, `api/app/api/routers/auth.py`, `api/app/db/seed/run.py`, `api/pyproject.toml`, `api/tests/api/test_auth_handlers.py`, `docs/decisions/0024-*.md` | ruff✓ mypy(strict,71)✓ pytest 57 passed + 2 failed (both **stale Juno-owned** assertions the real handlers invalidate); **6 persona-login xfails now PASS**; migration up→down→up clean; commits `b382a0f`,`61e75e5` | Atlas review → Juno |
| 11:00 | Atlas | US-E4-04/05 | **Review — accepted.** The 2 failures are legitimate consequences (login no longer a 501 stub; head moved `0003→0004`), not regressions — correctly left for Juno (test ownership). Accepted the `.test`-TLD shim (ADR-0024): `.test` is the globally-reserved test TLD; renaming personas would ripple through handles/manifest/golden-eval/fixtures, so the narrow shim is the right tradeoff. argon2-for-passwords / SHA-256-for-high-entropy-tokens split is correct. | done | — | — | Delegate Juno (US-QA-D09 + 2 cleanups) |
| 11:30 | Juno | US-QA-D09 | **Auth E2E + stale-assertion cleanup.** New `api/tests/api/test_auth_e2e.py` (14 DB-backed tests): full register→verify→login→/auth/me→logout→denied lifecycle + negatives (bad-pw 401, unknown 401, unverified 403, missing/garbage/wrong-scheme/expired token 401, dup 409) + sliding-window bump. **Scope (US-E4-05):** test-only `/admin-only` route reusing prod `require_role("admin")` + error handlers + throwaway DB → wrong-role 403 / admin 200 / no-token 401. Fixed cleanup #1 (`test_error_envelope.py` 501-assert `/auth/login`→`GET /products`) + #2 (`test_migrations.py` head→`0004_email_verified`); #3 n/a (imperative xfail, no decorator). No handler bugs found. | done | `api/tests/api/test_auth_e2e.py`, `api/tests/test_error_envelope.py`, `api/tests/db/test_migrations.py`, `docs/STATUS.md` | ruff✓ mypy(strict,72)✓ **pytest 73 passed, 0 failed**; 6 persona logins green; commits `5400f92`,`b51f746` | Atlas integrate |
| 12:00 | Atlas | US-E4-04/05/QA-D09 | **Integration check + merge.** Independent full gate green by code on DB host `55444`: ruff✓ mypy(strict,72)✓ **pytest 73 passed, 0 failed** (lone warning = upstream `python-multipart` deprecation). 6 persona-login xfails confirmed flipped. FF-merged `feature/auth-handlers` (`b382a0f`→`b51f746`) → `integration/backend` (`b51f746`); pushed `feature/*` + `integration/backend`. `dev` PR **held for Day-14 W2 gate**. Decision-of-record: ADR-0023 (impl calls) + ADR-0024 (`.test` shim); contract `[REVIEW] §5`→RATIFIED. | done | `docs/STATUS.md`, `docs/PROGRESS.md`, `docs/DAILY-LOG.md`, `docs/api/contract-v0.md`, `docs/decisions/0023-*.md` | gate green by code; pushed | Day 10 — catalog & hybrid search (all `[AFK]`) |

## 2026-06-18 — Day 10

| Time | Agent | Story | Action | Status | Files | Tests | Next |
|---|---|---|---|---|---|---|---|
| 14:30 | Orion | US-E4-06 | **Catalog endpoints (list/detail) + inventory.** Replaced the `/products` list, `/products/{id_or_slug}` detail, and `/stores/{id_or_slug}` detail stubs with real handlers (public reads) built to contract-v0 (`{data, meta}` envelope, cursor pagination). New `app/api/pagination.py`: opaque base64url **keyset** cursor `(created_at,id)` over `created_at DESC,id DESC`, `limit+1` has-more probe, garbage cursor→`422 validation_error`; reusable for all list routes. New `app/services/catalog.py`: ORM→DTO projection, `selectinload` (store/images/variants/inventory) to kill N+1, filters category/store_id, active+not-deleted only. **Inventory:** `VariantOut.in_stock = (qty_on_hand-qty_reserved)>0` (no inv row→OOS) + `restock_eta_days` passthrough — seeded low-stock (q3, in_stock=true+eta) & OOS (q0, in_stock=false) surface correctly. id-or-slug lookup guards the UUID cast (only OR id when value parses as UUID, else slug-only) → no PG `DataError`; missing→`404 not_found`. Reviews list/create stay stubs (separate story). Repointed 2 stub-era 501 spot-checks (`test_error_envelope`, `test_api_smoke`) from `/products`→`/products/{id}/reviews` (still a stub) — flagged for Juno. **ADR-0025.** | done | `api/app/api/routers/catalog.py`, `api/app/api/pagination.py`, `api/app/services/{__init__,catalog}.py`, `api/tests/api/test_catalog.py`, `api/tests/test_error_envelope.py`, `api/tests/test_api_smoke.py`, `docs/decisions/0025-*.md` | DB host `55444`; ruff✓ mypy(strict, 54 app files)✓ **pytest 84 passed, 0 failed** (73 prior + 11 new catalog) | Atlas review |
| 17:40 | Sable | US-E4-08 | **Pluggable embedding provider seam + refresh-on-change.** Formalized the ad-hoc seed `embed_text` stub into a real seam in new `app/services/embeddings.py`: `Embedder` Protocol (`model`, `embed_text`, `embed_batch`), deterministic `StubEmbedder` default (`model='seed-stub'`, len==`settings.embed_dim`, content-derived), and `get_embedder()` resolving `_PROVIDERS[settings.embed_provider]` (new `EMBED_PROVIDER` config, default `"stub"`). **Echo swap = one line**: register a provider in `_PROVIDERS` + set `EMBED_PROVIDER` — no call-site changes; unknown provider fails loudly. **Refresh path:** `refresh_product_embedding(session, product_id)` rebuilds the doc from the persisted product (`build_product_document`: title/description/category/sorted-attributes/active-variant options) and UPSERTs the `(product, id, 0)` embedding row — reusable by the seed AND future seller write handlers. Seed now routes products through `refresh_product_embedding` and policies through `upsert_embedding` (shared `Embedder` instance); old `seed/embeddings.py` is a back-compat shim re-exporting `SEED_STUB_MODEL`/`embed_text`. `EMBED_DIM` untouched (768); migration 0003 untouched. **ADR-0026.** | done | `api/app/services/embeddings.py`, `api/app/core/config.py`, `api/app/db/seed/{run,embeddings}.py`, `api/tests/db/test_embeddings.py`, `docs/decisions/0026-*.md` | DB host `55444`; ruff✓ mypy(strict, **55 app files**)✓ **pytest 86 passed, 0 failed** (84 prior + 2 new embedding); golden-eval seed-resolve still green | Atlas integrates `feature/catalog-embeddings` |
| 21:30 | Juno | US-QA-D10 | **Search E2E (J-BUY-01) + RAGAS retrieval smoke (no-LLM).** Two layers added on the `seeded_db`/`api_client` spine, both non-overlapping with Orion's endpoint-unit `test_search.py` (layer split documented in each module docstring). **(1) Search E2E** `tests/api/test_search_e2e.py` (4 tests): the discovery journey end-to-end — a search hit resolves to a real fetchable PDP (id AND slug → same `ProductDetail`, variants exposed); query refinement (second AND-term narrows to a strict subset); category facet hits are individually fetchable and stay in-category; `meta.mode` stable (keyword) across the journey incl. no-match. **(2) RAGAS retrieval smoke** `tests/qa/test_retrieval_smoke.py` (4 tests): deterministic **no-LLM** retrieval-half — context **precision@5 / recall@5** over the **17/25 product-grounded** golden items (variant sku→product slug resolved live against the seed, anti-drift; **8 policy-only items excluded** as out-of-catalog = Echo's agent-RAG surface). Captures a machine-readable artifact `docs/qa/eval/results/retrieval-smoke-<date>.json` (+ stable `-latest.json`) with scores/scope/per-item/failing-sample log, prints a summary table, logs every failing sample. **Finding (honest):** mean precision@5 / recall@5 = **0.000** — `websearch_to_tsquery` ANDs every token, so raw natural-language *questions* match nothing; a separate liveness anchor proves keyword search DOES recall the right product from a distinctive *term* (tide/wallet/candle), so the zero is the lexical gap semantic retrieval closes (Decision-4), NOT a broken endpoint. **SMOKE not a gate:** asserts only that it RAN + scores well-formed in `[0,1]` + failures logged + artifact written — NO recall floor (that would hard-fail on the expected gap). README updated: this is the retrieval-half baseline; Echo's Week-4 semantic+judge-LLM RAGAS picks up from `-latest.json`. | done | `api/tests/api/test_search_e2e.py`, `api/tests/qa/test_retrieval_smoke.py`, `docs/qa/eval/results/retrieval-smoke-2026-06-14.json`, `docs/qa/eval/results/retrieval-smoke-latest.json`, `docs/qa/eval/results/README.md`, `docs/qa/eval/README.md` | DB host `55444`; ruff✓ mypy(strict, **82 files**)✓ **pytest 103 passed, 0 failed** (95 prior + 8 new); no heavy `ragas`/LLM dep added | Atlas integrates `feature/qa-search-ragas` |
| 19:10 | Orion | US-E4-07 | **Hybrid search endpoint — keyword LIVE + semantic/rerank seam.** Replaced the `GET /search` 501 stub with a real handler built to contract-v0 Decision-4 (keyword live, semantic-ready, no contract change later). **Keyword = Postgres FTS**, not ILIKE: migration **0005** adds a STORED generated `tsvector` `products.search_tsv` (weight title `A` > category `B` > description `C`) + **GIN** index `ix_products_search_tsv`; queried with `websearch_to_tsquery('english', q)` + `@@`, ordered by `ts_rank` (the per-result `score`). New `app/services/search.py`: `Retriever` Protocol (`mode`, `retrieve`→`ScoredProduct[]`) with live `KeywordRetriever` + inert `SemanticRetriever`/`HybridRetriever` seams (raise `NotImplementedError` if selected; semantic seam references `app.services.embeddings.get_embedder()` for Echo's wiring), `get_retriever()`/`_ACTIVE_MODE` swap point, identity `rerank()` stub. `mode` surfaces in `meta` (new `SearchMeta(PageMeta)` + `SearchEnvelope`, preserves `{data, meta}`). **Echo swap = flip `EMBED_PROVIDER` + repoint `_ACTIVE_MODE`** → same endpoint/response, only `meta.mode`+`score` change. No ranked cursor v0 (relevance has no keyset; `next_cursor=null`, single ranked page, `cursor` accepted but unused); no-match→empty `data` (200 not 404); empty/over-long `q` + limit>100 → 422 canonical. Generated col marked `Computed(persisted=True)` on the ORM (never written) + index declared in `__table_args__` for model↔schema parity. Migration-contract test head `0004→0005_product_search` + new FTS-col/GIN asserts. `EMBED_DIM`/0003 untouched. **ADR-0027.** | done | `api/alembic/versions/0005_product_search_fts.py`, `api/app/db/models/catalog.py`, `api/app/services/search.py`, `api/app/api/routers/search.py`, `api/app/schemas/search.py`, `api/tests/api/test_search.py`, `api/tests/db/test_migrations.py`, `docs/decisions/0027-*.md` | DB host `55444`; ruff✓ mypy(strict, **56 app files**)✓ **pytest 95 passed, 0 failed** (86 prior + 9 new search); migration 0005 up→down→up clean; parity green | Atlas integrates `feature/catalog-search` |
| 22:30 | Atlas | US-E4-06/07/08, QA-D10 | **Day-10 integration — all 4 stories merged to `integration/backend`, sequential gate-then-FF.** Ran each feature branch through an independent gate on DB host `55444` before FF-merge (dependency order 06→08→07→QA-D10, shared working tree ⇒ sequential not parallel). **US-E4-06** catalog: ruff✓ mypy(strict,54)✓ pytest **84**✓ → FF `fda3d9e`. **US-E4-08** embeddings seam: ruff✓ mypy(strict,55)✓ pytest **86**✓ → FF `0202e4f`. **US-E4-07** search: ruff✓ mypy(strict,56)✓ pytest **95**✓ (migration 0005 up→down→up clean) → FF `aeac4eb`. **US-QA-D10** search E2E + retrieval smoke: ruff✓ mypy(strict,56 app)✓ pytest **103**✓ → FF. **Hygiene fix (`eecc97a`):** the smoke test rewrote tracked `results/*.json` with a wall-clock `generated_at` on every run (tree churn / CI noise) — untracked + gitignored the two run artifacts (scores were byte-identical bar the timestamp), kept `results/README.md` tracked; test still writes them on run (acceptance "scores captured" satisfied). Pushed `integration/backend` (`b51f746`→`eecc97a`) + all 4 `feature/*` to origin. **`dev` PR held for Day-14 W2 gate.** **Honest finding surfaced to human:** keyword retrieval recall on full NL questions = 0.000 by construction (FTS ANDs tokens) — this is the gap semantic retrieval (Echo, Week-4) closes, not a bug; endpoint proven live on distinctive terms. | done | `docs/STATUS.md`, `docs/PROGRESS.md`, `docs/DAILY-LOG.md`, `.gitignore` | full gate green by code; integration/backend @ `eecc97a` pushed | Day 11 — cart & orders (US-E4-09 `[AFK]` + US-E4-10 `[REVIEW]` payment) |
| 23:45 | Orion | US-E4-09 | **Cart endpoints (add/update/remove, idempotent) — LIVE.** Replaced all four `/cart` 501 stubs with real handlers (auth-required via `require_user`, session via `get_session`), thin over new `app/services/cart.py` (mirrors `services/catalog.py`). **GET /cart** lazily creates the single open cart → 200; repeated GET returns the same cart (honors `uq_carts_one_open_per_user`; lost create-race → `409 cart_already_open`). **POST /cart/items** → **201**, idempotent UPSERT on (cart, variant): existing line increments qty, new line appended; unknown/inactive variant → `404 not_found`. **PATCH /cart/items/{id}** sets absolute qty → 200; `qty=0`/negative → `422 validation_error` (Pydantic `ge=1`). **DELETE /cart/items/{id}** removes a line → 200. **out_of_stock (409):** available = `qty_on_hand - qty_reserved` (mirrors catalog math); add/update raise when the **resulting** line qty exceeds available — checks cumulative qty, not per-request delta. **NO inventory mutation** (reservation is checkout/US-E4-10). **Cross-user isolation:** PATCH/DELETE of a line not in the caller's own open cart → `404 not_found` (never leaks another user's line). Projection eager-loads items→variant→product/inventory via `selectinload` (no N+1); `CartItem` got a read-only `viewonly=True, lazy="raise"` `variant` relationship (ORM metadata only — **no DDL, no migration**) so projection raises rather than silently lazy-loads. No new ErrorCode members needed (`out_of_stock`/`cart_already_open`/`not_found`/`validation_error` all pre-existed). Did NOT touch orders/payment (US-E4-10). | done | `api/app/api/routers/cart.py`, `api/app/services/cart.py`, `api/app/db/models/cart.py`, `api/tests/api/test_cart_handlers.py` | DB host `55444`; ruff✓ mypy(strict, **57 app files**)✓ **pytest 118 passed, 0 failed** (103 prior + 15 new cart handler tests) | Atlas integrates `feature/cart-endpoints` |
| 2026-06-14 01:20 | Orion | US-E4-10 | **Order create + test-mode payment + lifecycle — LIVE (`[REVIEW]` human-approved).** Replaced 5 `/orders` 501 stubs with real handlers (auth-required `require_user`), thin over new `app/services/orders.py` (+ `app/services/idempotency.py`, `app/services/errors.py`; mirrors `services/cart.py`). **POST /orders** checkout: open cart→single multi-store `Order(status=placed)` 201 `OrderDetail`, per-line `OrderItem` snapshots (`title_/options_/store_name_snapshot`,`unit_price_minor`,`qty`) frozen at checkout, `order_number=HEA-YYYYMMDD-XXXXXX` (unique col guard), cart→`converted`; empty→`409 empty_cart`; any line short→`409 out_of_stock` **ATOMIC** (validate whole cart before mutating, nothing reserved). **GET /orders** newest-first (`placed_at DESC,id DESC`). **GET /orders/{id}** detail+payments; foreign/missing→`404 not_found` (no leak). **POST /orders/{id}/payment-intent** mock `Payment(pending)`, mock `client_secret`/`provider_ref`, amount=`order.total_minor`, 201. **POST /orders/{id}/payment-confirm** deterministic by body `outcome`: `captured`→200 `PaymentOut` + CAPTURE inventory (`qty_on_hand-=qty` AND `qty_reserved-=qty`); `failed`→**402 `payment_declined`** + RELEASE reservation (`qty_reserved-=qty`), order stays `placed`. **Reservation model:** reserve-at-checkout/capture-on-pay; `available=on_hand-reserved`; `SELECT…FOR UPDATE` row locks on inventory; counters floored at 0 (defends CHECK≥0); **retry-after-decline** re-reserves on a fresh `payment-intent` when latest payment is `failed` (no double-reserve on fresh checkout). **Decline `commit()`s-then-raises** so release+`failed`+idempotency row persist (request session rolls back on raise — deps.py). **Idempotency:** new **`idempotency_keys` table (migration 0006**, up→down→up clean; ORM `IdempotencyKey`; migration/parity table counts 21→22) keyed `(user_id,key,endpoint)`→stored `(status_code,response_json)`; replay re-emits original (incl. re-raising 402); `Idempotency-Key` header or `CheckoutRequest.idempotency_key` body fallback. **Shipping/tax v0 = flat $0** (total==subtotal; columns stay for a future rule). `/orders/{id}/returns` left a 501 stub (later epic). No NEW ErrorCode members (`empty_cart`/`payment_declined` already in enum). **ADR-0028.** | done | `api/app/api/routers/orders.py`, `api/app/services/{orders,idempotency,errors}.py`, `api/app/db/models/{idempotency,__init__}.py`, `api/alembic/versions/0006_idempotency_keys.py`, `api/tests/api/test_orders_handlers.py`, `api/tests/db/{test_migrations,test_models_parity}.py`, `docs/decisions/0028-*.md` | DB host `55444`; ruff✓ mypy(strict, app 61 + tests 28)✓ **pytest 131 passed, 0 failed** (118 prior + 13 new); migration 0006 up→down→up clean; parity green | Atlas integrates `feature/orders-checkout` |
| 2026-06-14 03:10 | Juno | US-QA-D11 | **Checkout API E2E (J-BUY-04) — journey layer over the live cart+orders spine.** New `api/tests/api/test_checkout_e2e.py` (6 DB-backed tests on `seeded_db`/`persona_client`/`handles`; inventory read live variant→`inventory` row, variants resolved from seed = anti-drift, no hardcoded counts). Layer split documented in the module docstring: handler-boundary atoms in Orion's `test_orders_handlers.py`/`test_cart_handlers.py` are NOT re-asserted; these assert the *cumulative* ADR-0028 invariants across the WHOLE chain. **(1) Happy path:** add→`GET /cart` reflects line→`POST /orders` 201 `OrderDetail` (snapshots frozen, total==subtotal, payments==[])→**reserve-at-checkout asserted (`qty_reserved`+=qty, `qty_on_hand` unchanged)**→source cart converted (fresh `GET /cart` is a NEW empty cart, different id)→payment-intent 201 pending (amount==total)→confirm `captured` 200→**capture asserted (`qty_on_hand`-=qty AND `qty_reserved` restored to baseline)**→`GET /orders`+`GET /orders/{id}` show the captured payment. **(2) Decline:** confirm `failed`→402 `payment_declined`, then PERSISTED side effects (commit-then-raise, ADR-0028 §5): reservation released (`qty_reserved` back to baseline, `qty_on_hand` untouched), payment row `failed`, order still `placed`. **(3) Idempotency:** replay `POST /orders` w/ same `Idempotency-Key`→same order id, order count +1 only, `qty_reserved` +=qty once (not doubled). **(4) Out-of-stock:** line at availability then drained out-of-band→409 `out_of_stock`, **atomic** (order count unchanged, no checkout reservation). **(5)** cross-user `GET /orders/{id}`→404; **(6)** unauth checkout→401. All assertions pinned to closed `ErrorCode`+status. **No contract/impl discrepancy found** — handlers match ADR-0028 + contract-v0. | done | `api/tests/api/test_checkout_e2e.py`, `docs/PROGRESS.md` | DB host `55444`; ruff✓ mypy(strict, 1 file)✓ **pytest 137 passed, 0 failed** (131 prior + 6 new; module alone 6✓); no regressions | Atlas integrates `feature/qa-checkout-e2e` |
| 2026-06-19 | Atlas | US-E4-09/10, QA-D11 | **Day-11 integration — all 3 stories merged to `integration/backend`, sequential dependency-order FF.** `[REVIEW]` on US-E4-10 resolved up-front with the human: approved (1) **reserve-at-checkout / capture-on-pay** inventory and (2) building the **two-step payment exactly as ratified** (contract-v0 / ADR-0021). Cart kicked off `[AFK]` in parallel with the review. Shared working tree ⇒ sequential FF in dependency order (orders reuses `app/services/cart.py`): **US-E4-09** cart `1fa81ee` → **US-E4-10** orders `5c7edf2` → **US-QA-D11** checkout E2E `deb6b05`. Final independent gate on DB host `55444`: ruff✓ · mypy --strict✓ (90 files) · **pytest 137 passed, 0 failed**; migration 0006 up→down→up clean. Pushed `integration/backend` (`eecc97a`→`deb6b05`) + all 3 `feature/*`. **`dev` PR held for Day-14 W2 gate.** | done | `docs/STATUS.md`, `docs/PROGRESS.md`, `docs/DAILY-LOG.md` | full gate green by code; `integration/backend` @ `deb6b05` pushed | Day 12 — agent tools + SSE loop (Echo) |

## 2026-06-14 — Day 12 (W3): Agent tool layer (Echo)

| Time (UTC) | Agent | Story | Action | Status | Files | Tests | Next |
|---|---|---|---|---|---|---|---|
| Day 12 | Echo | US-E5-01 | **Typed tool definitions + provider-agnostic registry.** New `api/app/agent/` package (room for the later SSE loop). **8 tools** in `app/agent/tools.py`, each a Pydantic v2 input model (`extra="forbid"`) + typed output, exposed via a `ToolSpec`/`REGISTRY` whose `provider_schema()` returns a serializable `{name, description, parameters=model_json_schema()}` dict — NO Groq/Gemini specifics. Tools: `search`/`productDetails`/`inventory`/`orderStatus` (reads), `addToCart`/`draftOrder` (mutating), `refund` (mutating+sensitive), `applyCoupon` (pure). Outputs reuse `ProductDetail`/`CartOut`/`OrderDetail`/`PaymentOut`/`PaymentIntentOut`/`ProductSummary` + small purpose models (`SearchOutput`/`InventoryOutput`/`DraftOrderOutput`/`RefundOutput`/`CouponDiscountOut`). New `app/services/coupons.py` — v0 deterministic stub (static `WELCOME10`→10%, `HEARTH5`→$5; unknown→`not_found`; **no coupons table**, gap in ADR-0029). `mutating`/`sensitive` flags drive the executor. **ADR-0029.** | done | `api/app/agent/{__init__,tools}.py`, `api/app/services/coupons.py`, `api/tests/agent/{__init__,test_tool_schemas}.py`, `docs/decisions/0029-agent-tool-layer.md` | DB host `55444`; ruff✓ mypy(strict)✓ **pytest 27 new schema tests pass** (schema generates, valid args parse, invalid raise, provider spec JSON-serializable) | US-E5-02 executor |
| Day 12 | Echo | US-E5-02 | **Tool execution layer — validate, scope, idempotency, retry, audit.** Single dispatcher `execute_tool(name, raw_args, *, session, user, conversation_id=None, idempotency_key=None, retry, clock, sleep)` in `app/agent/executor.py`. **(1) Validate** raw args against the tool's input model → `APIError(422, validation_error)` with per-field `details.errors` (never bare `HTTPException`). **(2) Scope** (ADR-0029): reads = any authed user; `addToCart`/`draftOrder` = owner (enforced inside services, 404 on foreign = no leak); `refund` = sensitive → order owner OR support/admin (foreign buyer → `not_found`, no captured payment → `409 conflict`). ONE identity path — caller passes the `User` from `require_user`/`require_role`. **(3) Execute** against the real service fns (not HTTP); `draftOrder` = checkout the open cart (places order) + create payment-intent WITHOUT confirming. **(4) Idempotency** for mutating tools via `services/idempotency` `endpoint=tool:<name>`; replay re-emits stored success or re-raises a stored error status; no key = no-op. **(5) Retry+timeout** (`RetryPolicy` + injectable clock/sleep, deterministic for QA): only `TransientToolError` (pre-effect) is retried; domain `APIError` is NEVER retried; mutating retries run inside the idempotency window (no unsafe double-apply); exhaustion/timeout → `APIError(429, rate_limited)` (chosen over 500 — documented). **(6) Audit** every terminal path as an `AgentAction` row (`action_type`, `payload`, `outcome` ∈ applied\|refused\|hitl_deferred; `conversation_id` may be None). Shared-session freshness: `session.expire_all()` per call so a later tool sees earlier tools' flushed effects (no stale identity-mapped collection) — no service code changed. **No new ErrorCode members.** **`[REVIEW]` guardrail fork flagged:** refund allowed for owner OR support/admin with NO spend cap / NO HITL threshold (sensible v0 default; `hitl_deferred` outcome + `_check_scope` gate are the seams for the future guardrail epic). | done | `api/app/agent/{__init__,executor}.py`, `api/tests/agent/test_tool_executor.py`, `docs/PROGRESS.md` | DB host `55444`; ruff✓ mypy(strict, app + tests)✓ **pytest 176 passed, 0 failed** (137 baseline + 39 new agent: 27 schema + 12 executor); no regressions | Atlas integrates `feature/tools-layer` (do NOT merge yet) |
| 2026-06-14 | Juno | US-QA-D12 | **Tool-call E2E matrix — exhaustive per-tool success/validation/unauthorized + journey-level cross-cutting.** New `api/tests/agent/test_tool_e2e.py` (36 DB-backed tests on `seeded_db`/`handles`, calling `execute_tool` directly with a real `Session`+seeded `User` = one identity path). Layer split documented in the module docstring: Echo's `test_tool_schemas.py` (schema atoms) + `test_tool_executor.py` (cross-cutting + 1 happy/tool) are NOT re-asserted; this is the exhaustive behavioural matrix. **Per-tool (all 8): success + validation-failure + unauthorized/domain-rejection**, all pinned to closed `ErrorCode`+status (never prose) and asserting the REAL effect. **search** (typed hits; extra-arg→422; cross-user read works). **productDetails** (slug→detail+variants; missing arg→422; ghost id→404). **inventory** (per-variant signal+any_in_stock; extra field→422; ghost variant→404). **orderStatus** (placed order detail; empty id→422; cross-user→`not_found` no-leak). **addToCart** (line present+qty; qty=0→422; OOS variant→`out_of_stock` 409; ghost variant→404). **applyCoupon** (WELCOME10 case-insensitive→500 minor discount; negative subtotal→422; unknown code→`not_found`). **draftOrder** (order `placed`+pending intent+**stock reserved at checkout**; missing ship_address→422; empty cart→`empty_cart` 409). **refund** (owner→captured Payment→`refunded`; support privileged any-order; extra arg→422; no captured payment→`conflict` 409; foreign buyer→`not_found` no-leak + payment untouched; seller non-owner→`not_found`). **Cross-cutting:** idempotency same-key applies-once (cart qty 2 not 4; reserved unchanged — addToCart reserves nothing) / different key independent (qty 5) / refund same-key refunds-once (payment refunded, replay no double-mark); retry/timeout driven THROUGH `execute_tool` w/ injected `clock`/`sleep` (transient retried→success; exhaustion→`rate_limited` 429+refused audit; domain APIError run once; retried mutating call applies once — no double effect); audit `applied` on success / `refused` on rejection per terminal call. | done | `api/tests/agent/test_tool_e2e.py`, `docs/PROGRESS.md` | DB host `55444`; ruff✓ mypy(strict, 1 file)✓ **pytest 212 passed, 0 failed** (176 baseline + 36 new; module alone 36✓); no regressions | Atlas integrates `feature/qa-tools-e2e` (do NOT merge yet) |
| 2026-06-20 | Atlas | US-E5-01/02, QA-D12 | **Day-12 integration — agent tool layer + per-tool E2E matrix merged to `integration/backend`, sequential dependency-order FF.** All 3 `[AFK]`, no `[REVIEW]` to gate up-front (US-E5-01/02 inseparable → Echo built both on one branch, two commits, like Day-9 auth). Shared working tree ⇒ sequential FF: Echo `feature/tools-layer` (`7fcabb1` = `05cb623` defs + `b82d92f` executor) → Juno `feature/qa-tools-e2e` (`a25c13d` = `4273454` matrix). Final independent gate on DB host `55444`: ruff✓ · mypy --strict✓ (98 files) · **pytest 212 passed, 0 failed** (137→176 Echo +39, →212 Juno +36); no migration. Pushed `integration/backend` (`25bc447`→`a25c13d`) + both `feature/*`. **`dev` PR held for Day-14 W2 gate.** **Carried to human (non-blocking):** Echo's refund-guardrail `[REVIEW]` (no spend cap / no HITL threshold; `_check_scope` no-op ⇒ refund mis-scope surfaces `not_found` not `403`) — ADR-0029, decide alongside Day-13/14 guardrail work. | done | `docs/STATUS.md`, `docs/PROGRESS.md`, `docs/DAILY-LOG.md` | full gate green by code; `integration/backend` @ `a25c13d` pushed | Day 13 — RAGAS harness (US-E7-00) + backend regression pack (US-QA-D13) |

## 2026-06-14 — Day 13 (W2): RAGAS harness (Echo)

| Time (UTC) | Agent | Story | Action | Status | Files | Tests | Next |
|---|---|---|---|---|---|---|---|
| Day 13 | Echo | US-E7-00 | **RAGAS-style eval harness — all 4 metrics, deterministic CI-safe default, real judge/answerer as one-line swaps.** New offline library `api/app/eval/`: `dataset.py` (typed Pydantic v2 `GoldenRecord`/`SourceDoc`/`EvalTags` loader over `golden-v0.jsonl` + centralised JSONL parsing), `metrics.py` (deterministic **context precision/recall** set-overlap — the SAME formula the Day-10 retrieval smoke uses, factored to one shared helper + lexical Jaccard/containment for the judge stub), `answerer.py` (`Answerer` Protocol + default `StubAnswerer`=`expected_answer` stand-in + `get_answerer()`/`EVAL_ANSWERER`), `judge.py` (`Judge` Protocol + default **`DeterministicJudge`** lexical stub for **response relevancy + faithfulness** + `_JUDGES`/`get_judge()`/`EVAL_JUDGE`; real Claude judge registers under `"claude"`, default model `claude-opus-4-8`, referenced-not-called), `harness.py` (`score_record`→4 metrics + `summarize`→means), `runner.py` (DB-aware: resolves `source_docs`→gold keys live + retrieves via live keyword `GET /search`, writes `results/ragas-<date>.json`+`ragas-latest.json`). Added `EVAL_JUDGE`/`EVAL_ANSWERER` settings to `app/core/config.py` (mirrors `embed_provider`). **Default path runs with NO LLM keys + NO network**; both registries fail loud on unknown (like `get_embedder`). **Refusal awareness:** `looks_like_refusal` heuristic logged per `expects_refusal` item in the artifact (signal, not a gate). **No `ragas`/LLM hard-dep** (pure stdlib; real ragas slots behind the same `Judge` Protocol — documented). **CI-safe sample run** `api/tests/qa/test_ragas_harness.py` (1 DB-backed run-over-golden + 12 pure unit pins on metric math/registry resolution/refusal detection): asserts the harness RAN + 4 means well-formed `[0,1]` + artifact written + refusal log — NOT quality thresholds on stub scores (smoke-style honesty). **Sample run (deterministic judge+stub answerer, 25 records):** context precision **0.000** / recall **0.000** (expected raw-question keyword gap — what semantic retrieval closes, Week-4), response relevancy **0.613**, faithfulness **1.000**; scope 17 product-grounded / 8 policy-only; all 4 `expects_refusal` items detected refusing. `.gitignore` += `docs/qa/eval/results/ragas-*.json` (matches smoke). **ADR-0030.** **`[REVIEW]` carried:** confirm Claude as the eval JUDGE + the exact model (`claude-opus-4-8`) before wiring; product runtime LLM stays Groq/Gemini. | done | `api/app/eval/{__init__,dataset,metrics,answerer,judge,harness,runner}.py`, `api/app/core/config.py`, `api/tests/qa/test_ragas_harness.py`, `.gitignore`, `docs/decisions/0030-ragas-harness.md`, `docs/qa/eval/README.md` | DB host `55444`; ruff✓ mypy(strict, **106 files**)✓ **pytest 225 passed, 0 failed** (212 baseline + 13 new); no migration | Atlas integrates `feature/evals-ragas-harness` (do NOT merge yet) |

| Day 13 | Juno | US-QA-D13 | **Backend regression pack — one ordered clean-seed traversal of the whole API surface + a readable Markdown report.** New `api/tests/regression/` package (`test_backend_regression.py`) adds the **integration-pass** layer no per-feature suite provides: a single ordered pass over auth (login→`/auth/me`) → catalog (list+detail) → search (keyword hit→PDP) → cart (add→`GET /cart`) → order (checkout→reserve→intent→capture→status) → agent tools (`execute_tool`: search+productDetails read, orderStatus, addToCart+draftOrder mutating) → error envelope (garbage-token 401 + cross-user 404), all against ONE freshly-seeded throwaway DB. Pins the **cross-feature invariants** that only hold across stitched surfaces (and are asserted nowhere else): REST search/catalog + agent search/productDetails resolve to the SAME seeded product; order `total_minor` == cart subtotal; reserve+capture move inventory by exactly the ordered qty; agent `orderStatus` mirrors REST `GET /orders/{id}`; failure carries the canonical `{"error":{code,message,details}}` shape with a closed `ErrorCode`. Reuses `seeded_db`/`persona_client`/`handles` (no hand-seed); error asserts pin closed `ErrorCode`+status, never prose; inventory deltas read live (anti-drift). **Report emitter:** a `report` fixture flushes `docs/qa/regression/regression-<date>.md` + stable `regression-latest.md` — header carries seed identity (live product count) + timestamp + surfaces-green/total, body is a per-surface table (exercised / key invariant / pass-fail). Run artifacts gitignored (`docs/qa/regression/regression-*.md`, matching the Day-10 smoke convention); committed `README.md` documents the pack + the one-command runner `pytest tests/regression` (or `-k regression`). NO contract/impl discrepancy found — the whole surface is green as built. | done | `api/tests/regression/{__init__,test_backend_regression}.py`, `docs/qa/regression/README.md`, `.gitignore` | DB host `55444`; ruff✓ mypy(strict, app+tests)✓ **pytest 228 passed** (225 baseline + 3 new, 0 regressions); regression report shows **8/8 surfaces green**; no migration | Atlas integrates `feature/evals-regression-pack` (do NOT merge yet) |

| Day 13 | Atlas | US-E7-00 + US-QA-D13 | **Integration — both Day-13 `[AFK]` stories onto `integration/backend`.** Sequential dependency-order FF on DB host `55444` (shared working tree ⇒ sequential): Echo's `feature/evals-ragas-harness` (`530c829`) → independent gate (ruff✓ mypy --strict 106 files✓ pytest 225) → Juno's `feature/evals-regression-pack` (`e0f599d`). Final independent full gate on `integration/backend`: ruff✓ · mypy --strict✓ (**108 files**) · **pytest 228 passed, 0 failed** (~8 min, per-test DB create/drop). No migration. Updated `docs/STATUS.md` board (Day-13 Done + Day-14 START-HERE) + this log. **`dev` PR opened** (`integration/backend`→`dev`, the single Week-2 gate). | done | `docs/STATUS.md`, `docs/PROGRESS.md` | full gate **pytest 228 passed**; no migration | Day-14 `[REVIEW]`: human ratifies the `dev` PR once GitHub CI is green |

| Day 14 | Atlas | US-QA-D14 | **W2 `dev` PR + CI remediation.** Opened PR `integration/backend`→`dev` (the single Week-2 gate, Days 8–13). First real GitHub CI run surfaced two genuine drifts (CI only fires on PRs to `dev`, so the Week-2 backend had never been CI-scanned): (1) **missing dep** — `argon2-cffi` declared in `pyproject.toml` but absent from `requirements.txt` (what CI installs) → `type-check (api)` 4× import-not-found + `test (api)` conftest `ModuleNotFoundError`; added to `requirements.txt`. (2) **secret** — GitGuardian flagged one literal DB cred (`postgres:postgres@…:55444`) in the Day-13 regression `README.md`; replaced with `USER:PASSWORD` placeholder + **history-rewritten** (cherry-pick rebuild: `e0f599d`→`64cc1f5`, `5a466e0`→`2edf915`) so no PR commit ever carried it, force-pushed (authorized clean-history policy), `feature/evals-regression-pack` re-pointed. No code changed by the rewrite (single doc line). | done | `api/requirements.txt`, `docs/qa/regression/README.md`, `docs/STATUS.md`, `docs/PROGRESS.md` | local gate unchanged (228 passed); CI re-run pending on the fresh PR | human ratifies the `dev` merge once GitHub CI (incl. GitGuardian) is green |

| Day 14 | Atlas | US-QA-D14 | **GitGuardian green — actual finding was a test-password false-positive.** Initial identification was wrong: the GG block was NOT the postgres URI but a **Generic Password** in `api/tests/api/test_auth_e2e.py:179` (`"definitely-not-it"`, a negative-path wrong-login) + siblings (`GOOD_PASSWORD`/`NEW_PASSWORD`/`"wrong-password"`). Got the real finding from the GG PR comment. Scrubbed all four test-password literals to `CHANGE_ME_*` placeholders via `filter-repo` scoped to `integration/backend` only (files absent on dev); 27 auth+regression tests still pass. (The earlier postgres-URI scrub across dev+integration was valid hygiene but not the blocker — it had passed the Day-7 scan.) Force-pushed; **PR #5 now all-green incl. GitGuardian ✅** (test (api) finishing). Backups: `backup/pre-scrub*` tags. | done | `api/tests/api/test_auth_e2e.py`, `api/tests/api/test_auth_handlers.py` (via history rewrite), `docs/STATUS.md`, `docs/PROGRESS.md` | 27 auth+regression pass; GG ✅; lint/type-check/e2e green | human ratifies the `dev` PR #5 merge |

| Day 14 | Atlas | US-QA-D14 | **Week 2 MERGED to `dev` `[REVIEW ✓ human-ratified]`.** Verified PR #5 fully green (ci · web+api lint/type-check · pgvector `test-api` · e2e-smoke · docker-build · **GitGuardian "No secrets detected ✅"**; state CLEAN/MERGEABLE), then merged `integration/backend` → `dev` as merge commit **`cc23501`** (per-day history preserved, W1 Day-7 pattern). CI runs on PRs-to-`dev` + pushes-to-`integration/**`, not on the merge push, so no separate post-merge gate — PR #5 validated the exact merged tree. **Week 2 complete.** Updated STATUS (Week-2 ✅, Week-3 START-HERE) + DAILY-LOG + this log. | done | `docs/STATUS.md`, `docs/PROGRESS.md`, `docs/DAILY-LOG.md` | PR #5 all checks green incl. GitGuardian ✅; `dev` @ `cc23501` | Week 3 kickoff: read `docs/plan/week-3.md`, branch `integration/<module>` off `dev` |

## 2026-06-23 — Day 15 (W3D1): Orchestrator + shopping agent (Echo)

| Time (UTC) | Agent | Story | Action | Status | Files | Tests | Next |
|---|---|---|---|---|---|---|---|
| Day 15 | Echo | US-E5-03 | **Orchestrator/router (LLM intent classify) + session state — LangGraph `StateGraph`.** New `app/agent/graph.py`: `classify` node (LLM structured output → route + confidence) → conditional edges to `shopping` / `support` / `merchandising`; **ambiguity fallback** (confidence < `CLARIFY_THRESHOLD`=0.55 → shopping agent + one `clarify` turn, no guess/no dead-end); support/merch are **registered-but-deferred** nodes returning a graceful "not available yet" (no 501/crash). Session state keyed on `thread_id`==`conversation.id` via a LangGraph checkpointer; **durable** turns persist to existing `conversations`/`messages` (new `app/agent/persistence.py`) + `agent_actions` (executor audit). LLM reasoning is INJECTABLE behind `IntentClassifier`/`ShoppingPlanner` Protocols (`app/agent/brains.py`) so CI runs the real graph with stub brains + NO provider key (fake chat models can't do `with_structured_output`). **Provider wiring:** new `app/agent/llm.py::get_chat_model()` resolves `settings.llm_provider`→`ChatGroq`\|`ChatGoogleGenerativeAI` (closed `_PROVIDERS`, fail-loud); config += `llm_provider`(groq)/`groq_api_key`/`gemini_api_key`/`groq_model`(llama-3.3-70b-versatile)/`gemini_model`(gemini-2.0-flash); `model_config` now loads gitignored `.env` (bare `DATABASE_URL`/`EMBED_DIM` still resolve). `.env.example` += `LLM_PROVIDER` + model-name vars. **ADR-0031.** | done | `api/app/agent/{graph,brains,llm,persistence,runner,langchain_tools}.py`, `api/app/core/config.py`, `.env.example`, `api/requirements.txt`, `api/tests/agent/{test_orchestrator,test_llm_factory}.py`, `docs/decisions/0031-langgraph-orchestrator.md` | DB host `55444`; ruff✓ mypy --strict✓ (117 files) · routing/clarify/deferred + LLM-factory fail-loud/swap green | US-E5-04 shopping agent + SSE wiring |
| Day 15 | Juno | US-QA-D15 | **Agent conversation E2E + tool-call-accuracy artifact.** New `api/tests/agent/test_agent_convo_e2e.py` drives Echo's REAL LangGraph runtime (`app.agent.runner`→`graph`) against a real `seeded_db` through one buyer conversation (single `thread_id`==`conversation.id`): **(1) search** turn classifies→shopping + calls `search` (grounded); **(2) comparison** turn calls `productDetails`×2 in the SAME convo (session continuity); **(3) add-to-cart** turn calls `addToCart` and the REAL `CartItem` lands (verified vs seeded DB, right variant+qty); **(4) checkout** turn `interrupt()`s — asserts NO `Order` at the stop, resume(approved)→real `draftOrder` places order (count==1), and a 2nd convo covers resume(reject)→no order. Scripted brains via the `app.agent.brains` injection seam (NO provider key, CI-safe); products/variants resolved from `handles` (anti-drift). **Tool-call accuracy** = expected (script) vs ACTUAL tools, where actual is read from the executor's own `AgentAction` AUDIT rows (authoritative, not stub-echoed); latest run **1.000 (5/5 matched)**. **Artifact** `docs/qa/agent/agent-convo-<date>.{json,md}` + `-latest` (gitignored, matching retrieval-smoke/regression precedent) + committed pack `README.md`. Layer split documented in the module docstring — does NOT re-assert Echo's node/route/factory/SSE/interrupt atoms; owns journey-level invariants (continuity, real cart effect, hard approval stop, one-catalog) only. | done | `api/tests/agent/test_agent_convo_e2e.py`, `docs/qa/agent/README.md`, `.gitignore`, `docs/PROGRESS.md` | DB host `55444`; ruff✓ mypy --strict✓ · **full suite 246 passed** (244 baseline + 2; no regression) · tool-call accuracy 1.000 | Atlas integrates `feature/qa-agent-e2e` (do NOT merge yet) |
| Day 15 | Echo | US-E5-04 | **Shopping agent (discovery→cart→agentic-checkout plan-execute) + HARD approval stop + SSE wiring.** `shopping` node = bounded plan-execute loop (`MAX_PLAN_STEPS`=6): discovery/cart tools (search/productDetails/inventory/applyCoupon/addToCart) → reply OR **propose checkout** → `approve_checkout` node calls LangGraph **`interrupt()`** (graph pauses BEFORE any order); only `Command(resume={"approved":True})` runs `checkout` → real **`draftOrder`** tool (Week-2 executor: reserve+intent, audited). Tools are a THIN LangChain adapter over `execute_tool` (`app/agent/langchain_tools.py`) — no reimplementation of validation/scope/idempotency/audit/error-envelope; acting `User`+`Session`+`conversation_id` closed over from request ctx (via `app/api/deps.py`), NEVER from the LLM. **SSE LIVE:** `app/agent/runner.py::stream_turn` drives the graph + emits contract-v0 frames (token→[approval]→citations→done); `app/api/routers/agent.py` 2 POST routes wired (auth-scoped `require_user`, own session txn since stream body runs post-deps) + transcript GET reads the durable record. **ADR-0031.** | done | `api/app/agent/{graph,runner,langchain_tools}.py`, `api/app/api/routers/agent.py`, `api/tests/agent/{test_orchestrator,test_agent_sse_e2e}.py` | DB host `55444`; full shopping turn→interrupt (no order placed), resume-approved→order placed (count==1), resume-rejected→no order, SSE E2E token→citations→done + unauth 401 + transcript GET green | Atlas integrates `feature/agent-orchestrator` (do NOT merge yet) |
