# STATUS — single source of truth

> Atlas updates this at the start and end of every session. Keep it short.

## ▶ Next session — START HERE
You are **Atlas**, the orchestrator (full brief: `.claude/agents/atlas.md`). **Week 1 + Week 2 COMPLETE** on `dev` @ `cc23501` (PR #5, all CI + GitGuardian ✅). **Week 3 is running on TWO parallel integration lines off `dev`** — they reconcile at the **Day-21 W3 gate (US-QA-D21)**:
   - **`integration/agents`** (Days 15–17, agent layer **feature-complete**) — orchestrator/router + shopping/support/**merch** agents, hybrid RAG, guardrails, LLM failover + semantic cache, agent eval suite. Tip `ec741db`. ADRs 0031–0034. *(Those Done-sections live on that branch's STATUS, NOT here — this branch was cut off `dev` before they merged.)*
   - **`integration/frontend`** (Day 18+, **THIS branch**) — the Next.js frontend. **Day 18 (W3D4)** shadcn/ui-on-tokens foundation + auth UI + browser smoke (`9f86966`). **Day 19 (W3D5)** catalogue search + PDP + **conversation-first** Ember chat (ADR-0036 revised) + UI E2E (`c848c45`). **Day 20 (W3D6) SHIPPED + integrated @ `903379e`** (pushed): cart + **agentic-checkout approval gate** (US-E6-06 `[REVIEW ✓ human-locked]`, ADR-0038 — inline HITL gate maps to LangGraph `interrupt()`), order status/returns + seller merch dashboard (US-E6-07), full shopping-journey E2E (US-QA-D20). All over deterministic **mock** endpoints (ADR-0037; backend not live — flip-to-live = config at the W3 gate). e2e **@web 80 / @smoke 4**. **Next is Day 21 (W3D7) — the W3 integration gate (US-QA-D21): merge BOTH lines to `dev`.**
1. Read **only** this file + the **W3D7** block of `docs/plan/week-3.md`. Don't re-read earlier days or the whole repo. **Day 21 reconciles BOTH lines** (`integration/frontend` @ `903379e` + `integration/agents` @ `ec741db`) into `dev` — expect doc merge conflicts (STATUS/PROGRESS/DAILY-LOG diverge across the two lines; resolve chronologically).
2. **Day 21 (W3D7) — W3 integration review & merge** (`docs/plan/week-3.md`): **US-QA-D21** `[REVIEW]` — FE+agent regression + RAGAS gate + merge to `dev`; acceptance = CI green + browser E2E + agent E2E + RAGAS gate all green, PR merged. This is the **flip-to-live** moment: the frontend's env-driven base URLs + the agentic-checkout Approve action wire to the real backend + the agent runtime's `interrupt()` resume (ADR-0038); confirm the assumed money/auth shapes against the live API. **Carried follow-ups from Day 20 (non-blocking, address at the gate):** (a) UI-only convenience fields (`OrderItemOut.slug`, cart-line `option_value`/`slug`, `OrderDetail.return_eligible`) are additive over the Pydantic schemas — confirm the live backend supplies them or resolve client-side; (b) **no seed order is both `delivered` AND out-of-window**, so the returns→`hitl_pending` UI path lacks a deterministic data spine — Juno's E2E drives it test-side (intercept the returns POST); **fix-forward = Iris adds a delivered+ineligible seed order to `web/src/lib/mock/orders.ts`**. Also confirm whether the **direct** (non-agent) `POST /orders/{id}/returns` out-of-window should defer to `hitl_pending` (chosen for UX) vs `409 return_window_closed`. **NB the sandbox blocks build-time Google-Fonts fetch** (`next/font/google` ETIMEDOUT) — local `next build` needs network or a temporary `next/font/local` swap; **CI has network so the gate build is fine there.**
   - **Error-envelope + CORS are now live** (not just documented): every non-2xx returns `{"error":{code,message,details}}` via `app/core/errors.py` (`APIError` + `install_error_handlers`); raise `APIError(status, ErrorCode.x, msg)` in new handlers, **don't** use bare `HTTPException`. CORS origins are env-driven (`HEARTH_CORS_ORIGINS`).
3. **CI is real now** — every PR to `dev` runs web+api lint/type-check + the **pgvector `test-api`** job + GitGuardian. Keep it green by code. The `test-api` service uses **trust auth** (no credential literal). docker-build is still a documented no-op (wire `docker compose build` when CI minutes are tuned).
4. **Locked contract-v0 (the Week-2 build target):** `docs/api/contract-v0.md` + ADR-0021. 36 ops. Ratified: opaque **session-token** auth · `{data, meta}` envelope · **cursor** pagination · **keyword search** (semantic-ready, pgvector deferred to Echo) · **agent chat = SSE** `text/event-stream` (token→citations→done) · **two-step payment** intent→confirm, deterministic decline via `PaymentConfirmRequest.outcome="failed"` → `402 payment_declined`. Pydantic v2 schemas in `api/app/schemas/`, routers (501 stubs) in `api/app/api/routers/`. **E2E fixture spine** ready — `docs/qa/fixture-plan.md` + `api/tests/fixtures/` (`seeded_db`/`handles`/`api_client`/`persona_client`); 17 J-* journeys mapped; 8 journey factories specified, awaiting the Week-2 cart/order/return handlers.
5. **Fixture test password is env-derived:** `TEST_PASSWORD = os.environ.get("HEARTH_TEST_PASSWORD", "CHANGE_ME_test_pw")` (handles.py); manifest mirrors the default. Week-2 auth-backed seeding writes exactly these per-persona credentials.
6. **Still Echo's (Week-2):** the **embedding provider seam now exists** (`app/services/embeddings.py`) — register a real provider in `_PROVIDERS` + set `EMBED_PROVIDER=<x>` (one line); confirm the model & whether `EMBED_DIM` changes from `768` (if so, re-run migration `0003`). **Semantic search seam is wired** (`app/services/search.py`: `SemanticRetriever`/`HybridRetriever` + `_ACTIVE_MODE`) — flipping it on + a real embedder makes `GET /search` hybrid with **no contract change**; the Day-10 retrieval smoke is the keyword baseline to beat (keyword recall on NL questions = 0 by construction). Also: RAGAS runner over `golden-v0.jsonl` (faithfulness ≥0.90); the SSE agent stream loop.
7. **DB ops:** `docker compose up -d db` = `pgvector/pgvector:pg16`; **host 5432 is occupied by a stray `local-postgres`** — bring up on an alt host port (`DB_PORT=55444`), set `DATABASE_URL` to it. Tooling via venv (`api/.venv`, gitignored) — ruff/mypy/pytest not on base PATH. Don't commit creds (GitGuardian scans full history; use trust/`CHANGE_ME`/var-refs).
8. **Housekeeping:** local `backup/pre-scrub` ref + a throwaway compose `db` may still be around from Day-7 — safe to delete/`docker compose down`. The Day-7 history rewrite changed SHAs on the data/backend line; if you have other local checkouts of `integration/backend`, reset them to `origin`.

**Phase:** Week 3 · Day 20 (W3D6, 2026-06-28) — ✅ **SHIPPED + integrated to `integration/frontend` @ `903379e`** (pushed; `dev` PR held for the Day-21 W3 gate). Cart + **agentic-checkout approval gate** (US-E6-06 `[REVIEW ✓ human-locked]`, ADR-0038), order status/returns + seller merch dashboard (US-E6-07), full shopping-journey E2E + agent transcript (US-QA-D20). Gate green by code: web lint ✓ · type-check ✓ · build ✓ (27 routes) · **e2e @web 80 passed (40/project: smoke-web + ui-flows + shopping-journey, desktop+mobile)** · @smoke 4/4 · axe 0 serious/critical. NB the agent line (Days 15–17) stays parked on `integration/agents` @ `ec741db` — both merge at the Day-21 W3 gate.

## ✅ Done — Day 20 (W3D6 — cart/checkout UI & seller dashboard, integrated to `integration/frontend` @ `903379e`)
- **[REVIEW ✓ human-locked, ADR-0038] — agentic-checkout approval gate:** Iris built the cart + agentic checkout; Atlas **rendered the approval gate in real Chromium** for the human (Day-19 lesson — render before locking), presenting inline-gate vs modal vs chat-inline; the human chose **"lock it as-is."** The gate is an **inline "Approval needed" card in the order-summary rail** (not modal, not chat-inline): Ember restates items/ship-to/payment/total with explicit **Approve / Cancel** and *"nothing charged until you approve"* — no money side-effect fires before Approve. Maps to the agent runtime's LangGraph `interrupt()`.
- **US-E6-06** cart + agentic-checkout UI `[REVIEW ✓ ADR-0038]` — Iris (`80fa108`). `/cart`: real lines, optimistic qty steppers (PATCH) + remove (DELETE) with rollback, subtotal/currency, empty state. `/checkout`: Ember-voiced flow → approval gate → contract **two-step payment** `POST /orders` (idempotent, `Idempotency-Key`) → `payment-intent` → `payment-confirm`; deterministic decline = the contract switch surfaced as a payment-method choice (`outcome="failed"`→`402 payment_declined`, order stays placed/unpaid) with graceful retry; success → confirmation deep-linking to order status.
- **US-E6-07** order status + returns + seller dashboard `[AFK]` — Iris (`2a11b3c`). `/orders`: list + detail with status **timeline** + payment badge + per-item snapshot. Returns from a delivered order; out-of-window → `hitl_pending` (`order-status-hitl-pending`). `/seller`: inventory/listings snapshot, orders-to-fulfil (optimistic), **Ember's merchandising nudges** (grounded reason + audited reversible accept). Mock handlers under `web/src/app/api/{cart,orders,seller}/**` + stores `web/src/lib/mock/{cart,orders,returns,seller}.ts` (ADR-0037; env bases flip live at the gate).
- **Cleanup** (Iris) — removed the orphaned `GET /api/search` route + helpers; the retired grid testids (`search-results`/`-result-card`/`-empty-state`) + the `J-BUY-01` fixme skeleton removed by Juno in the E2E rewrite.
- **US-QA-D20** full shopping E2E `[AFK]` — Juno (`903379e`). `e2e/tests/shopping-journey.spec.ts` (12 ×2 viewports): happy path (cart→checkout→**gate restates totals BEFORE charge**→Approve→confirmation→timeline+Paid) · **gate-is-a-real-gate** (no `POST /orders` until Approve; Cancel = no side-effect) · declined-card held-order+retry · cart mutations (qty/remove/empty) · returns in-window + **out-of-window HITL** · seller nudges accept + fulfil · **axe 0 serious/critical** on /cart, /checkout (review+gate), /orders, /seller. Emits the agent-transcript artifact (`docs/qa/agent/`, gitignored run output + committed README). Pinned `@web` to `workers:1` (mock stores are process-global). **No new a11y/contract failures.**
- **Atlas integration check:** US-E6-06 `[REVIEW]` resolved via the Chromium render→human-lock loop (ADR-0038). Single feature branch `feature/fe-checkout-seller` accumulated Iris's 2 stories + ADR-0038 + Juno's E2E ⇒ clean FF (`a4e0676..903379e`). Frontend-only day ⇒ gate = web lint/type-check/build + e2e (no pytest). Final green by code: web lint ✓ · type-check ✓ · **build ✓ (27 routes)** · **e2e @web 80 / @smoke 4** · axe clean. Pushed `integration/frontend` + `feature/fe-checkout-seller`. `dev` PR **held for the Day-21 W3 gate (US-QA-D21)**.
- **For the human (non-blocking, carried to the gate):** (1) a few UI-only fields (`OrderItemOut.slug`, cart-line `option_value`/`slug`, `OrderDetail.return_eligible`) are additive over the schemas — confirm vs live backend or resolve client-side. (2) **No seed order is delivered+out-of-window** → the returns→`hitl_pending` UI path has no data spine; Juno drives it test-side; fix-forward = Iris adds a delivered+ineligible seed order to `web/src/lib/mock/orders.ts`. (3) Direct (non-agent) out-of-window return defers to `hitl_pending` (chosen for UX) vs `409 return_window_closed` — confirm. (4) Sandbox blocks build-time Google-Fonts fetch (env-only; CI has network).

## ✅ Done — Day 19 (W3D5 — frontend core flows, integrated to `integration/frontend` @ `c848c45`)
- **[REVIEW ✓ human-ratified, ADR-0036 revised] — chat assistant layout:** the fork was first ratified as a **persistent docked panel**; Iris built it (`1d55b46`/`809fe87`), Atlas **rendered it in real Chromium** for the human's UX sign-off (design-review-taste), and the human **reworked the direction → conversation-first** (`/search` IS the conversation; rec cards inline in Ember's replies). ADR-0036 was revised to the final direction (honest decision-history kept); Iris rebuilt; Atlas re-rendered → **approved**. Lesson: render `[REVIEW]` UX in Chromium BEFORE locking.
- **US-E6-04** catalogue search + PDP `[AFK]` — Iris (`1d55b46`). `/search` results + product detail (`/product/[idOrSlug]`: gallery/variants/qty/add-to-cart/OOS/added-confirmation), grounded rec cards deep-link to the PDP. Money modeled `price_cents`+`currency` (flag: confirm vs live backend).
- **US-E6-05** conversation-first chat assistant `[REVIEW ✓ ADR-0036]` — Iris (`809fe87` docked → `d6d23e3` reworked). `/search` IS the conversation: one `agent-chat-panel`/`agent-chat-input` in the DOM; user turn → thinking (aria-live, static under reduced-motion) → token stream → citations → inline `agent-recommendation-card`+reason; clarify/refusal/error paths. `AgentProvider` route-scoped (dock removed from `(shop)` layout).
- **ADR-0037** (Iris) — frontend **mock SSE + shop clients**: Next route handlers (`/api/agent/**`, `/api/search`, `/api/cart/items`) + `lib/mock/*` emit real `text/event-stream` with deterministic keys (`ignore your…`→refusal, `which`/`?`→clarify, `boom`→error, else→recommend; `?instant=1` no-delay; `zzzznoresults`→empty; OOS variant→409). **Backend not live this session** — env-driven base URLs flip to the real API at the W3 gate (config only).
- **US-QA-D19** UI E2E `[AFK → Atlas recovery]` — **Juno's subagent hit the monthly spend cap before writing anything; Atlas wrote the spec directly.** `e2e/tests/ui-flows.spec.ts` (13 tests ×2 viewports): streaming happy path · citations · inline rec cards · card→PDP deep-link · loading (busy-disabled composer) · thinking indicator (route-delayed to assert deterministically) · clarify · refusal · mid-stream error (graceful alert, composer recovers) · empty-conversation state · PDP add-confirm · sold-out→OOS notice · unknown-slug 404 · **axe a11y (zero serious/critical) on /search + PDP**. **Found + fixed a real PDP a11y bug** (`347be92`): sold-out variant `opacity-60` → `#9c948d` on `#fdfcfb` = 2.91:1; dropped `opacity-60` (strikethrough + "· sold out" already signal it) → 7.39:1.
- **Atlas integration check:** US-E6-05 `[REVIEW]` resolved via the Chromium render→rework→re-render→approve loop (ADR-0036 revised). Single feature branch `feature/fe-core-flows` accumulated ADR + Iris's two rounds + Atlas's QA/a11y commits ⇒ clean FF (`8d7bbf2..c848c45`). Frontend-only day ⇒ gate = web lint/type-check/build + e2e (no pytest). Final green by code: ruff/mypy N/A · web lint ✓ · type-check ✓ · build ✓ (17 routes) · **e2e @web 56 passed** (30 smoke + 26 ui-flows, both viewports) · @smoke 4/4. Pushed `integration/frontend` + `feature/fe-core-flows`. `dev` PR **held for the Day-21 W3 gate (US-QA-D21)**.
- **For the human (non-blocking):** (1) money shape `price_cents`+`currency` assumed — confirm vs the live backend at the gate. (2) Conversation-first **orphaned the grid testids** (`search-results`/`-result-card`/`-empty-state`) + `GET /api/search` — cleanup follow-up. (3) Real SSE round-trips against the live agent runtime are proven when both lines merge at Day 21.

## ✅ Done — Day 18 (W3D4 — frontend foundation, integrated to `integration/frontend` @ `9f86966`)
- **[REVIEW ✓ human-ratified up front] — ADR-0035 component foundation:** human chose **shadcn/ui restyled to Hearth tokens** (Radix a11y primitives, source owned in-repo, 21st.dev/Magic composes bespoke pieces on top, Impeccable polish). Tokens are unchanged — primitives consume the existing CSS vars. Over "Magic-primary" and "hand-roll-everything".
- **US-E6-02** shadcn/ui on tokens `[REVIEW ✓ ADR-0035]` — Iris (`a8b5a7d`). Manual shadcn under `web/src/components/ui/` (`button` reconciled to Radix `Slot`+`cva` keeping ember/clay `primary` + clay/white `brand` variants; `input`/`label`/`card`/`dialog`/`dropdown-menu` + `cn()`); deps `class-variance-authority`/`clsx`/`tailwind-merge`/`@radix-ui/*`. Tokens untouched.
- **US-E6-01** scaffold reconcile `[AFK]` — Iris (folded into `a8b5a7d`/`51244f0`). App Router + TS strict + Tailwind-v4-from-tokens verified (light/dark vars resolve); home moved into a `(shop)` shell. Build green (15 routes).
- **US-E6-03** auth UI, layout, nav `[AFK]` — Iris (`51244f0`). `(shop)` app shell (sticky header: HearthMark, primary nav, search entry, cart link, ThemeToggle, Radix account menu + responsive mobile nav; footer; skip link) + `(auth)` `/login`+`/register` (client validation, accessible errors, buyer/seller role). Typed API client + **httpOnly-cookie session via route handlers** (`/api/auth/{login,register,logout}`); browser never reads the token (XSS-safe). Registry `data-testid`s wired. **Backend not live this session — wired to the contract shape, not a faked backend.**
- **US-QA-D18** browser smoke E2E `[AFK]` — Juno (`9da5511`). `e2e/tests/smoke-web.spec.ts` (route health · auth screens · app shell · **axe a11y baseline**) across **two real viewport projects** (desktop-chromium + mobile-chromium/Pixel 5); self-booting `webServer` (`next build && start :3100`), `E2E_NO_WEBSERVER` keeps the Day-1 `@smoke` lane server-free. Added `@axe-core/playwright`. J-* journey specs stay `test.fixme`. **Juno's axe gate caught a real WCAG AA finding** (24/30) and routed it to Atlas without patching app code.
- **Atlas a11y remediation + integration** (`9f86966`). Root-caused the axe `color-contrast` failures: **`cn()` tailwind-merge didn't recognize Hearth's NAMED font-size tokens** (`text-small`/`body`/`caption`/…), bucketed them with `text-{color}`, and **dropped the foreground class off every *sized* Button** → the Clay `brand` CTA inherited ink (3.57:1). Fix = `extendTailwindMerge` font-size classGroup (`web/src/lib/cn.ts`) — also un-breaks primary buttons that only *looked* right via inherited `--text`==`n-900`. Plus new role tokens `--on-brand` (white both themes, 5.0:1) + `--accent-text` (5.4:1 light / 8.7:1 dark) for small accent text, and `role=log`+`aria-label`+`tabIndex=0` on the scrollable agent message list (mobile `scrollable-region-focusable`). **axe now 30/30 both viewports.** FF-merged `feature/fe-foundation` → `integration/frontend` @ `9f86966`; pushed both. Frontend-only day ⇒ gate = web lint/type-check/build + e2e lanes (no pytest). `dev` PR **held for the Day-21 W3 gate**.
- **For the human (non-blocking):** (1) Iris assumed the auth response `data` shape `{token, expires_at, user}` and `UserOut` fields from ADR-0023/ORM — **Orion should confirm field names** at the W3-gate wiring. (2) `RegisterRequest.role` restricted to buyer|seller in the UI — confirm the backend rejects other roles on self-register. (3) Real auth round-trips await the live backend (the SSE agent runtime is on `integration/agents`); the W3-gate merge brings both lines together.

## ✅ Done — Day 14 (W2D7 — W2 review & merge to `dev` @ `cc23501`)
- **US-QA-D14** W2 integration gate + merge `[REVIEW ✓ human-ratified]` — Atlas/Juno. The single Week-2 PR (Days 8–13), mirroring W1's Day-7 pattern. **PR #5 `integration/backend` → `dev`**, merged as **`cc23501`** (merge commit, per-day history preserved). **All CI green:** ci · web+api lint · web+api type-check · **pgvector `test-api`** · e2e-smoke · docker-build (no-op) · **GitGuardian "No secrets detected ✅"**. By design CI re-runs on PRs to `dev`, not on the merge push — PR #5 validated the exact merged tree, so no separate post-merge gate.
- **CI remediation that got the gate green (3 passes, all by code):** (1) added missing `argon2-cffi` to `requirements.txt` (CI installs from it, not `pyproject.toml`) → fixed `type-check (api)` + `test (api)`. (2) **Actual GitGuardian block = a "Generic Password" false-positive** (`"definitely-not-it"` + sibling test passwords in the Week-2 auth tests); scrubbed all four to `CHANGE_ME_*` placeholders via `filter-repo` scoped to `integration/backend` (tests stay consistent, 27 pass). (3) Pre-emptive cred-doc hygiene — scrubbed literal test-DB creds (`postgres:postgres`/`qa_local_pw`/`ci_pw`) from the QA docs, range-scoped `filter-repo` across `dev`+`integration` together (inherited from a Day-6 commit on `dev`; human-authorized clean-history rewrite). **No app code/behavior changed by any rewrite** — only dependency/doc/test-password placeholders; the 228-test gate held throughout. **Lesson recorded:** read GitGuardian's *PR comment* for the real finding (the GitHub check output is generic) before remediating. Backups: `backup/pre-scrub*` tags.

## ✅ Done — Day 12 (W2D5 — agent tools, integrated to `integration/backend` @ `a25c13d`)
**Echo, agent tool layer (US-E5-01/02) — integrated @ `7fcabb1`:**
- **US-E5-01** typed tool defs + provider-agnostic registry: new `api/app/agent/` package, 8 tools (`search`/`productDetails`/`inventory`/`orderStatus` reads; `addToCart`/`draftOrder` mutating; `refund` sensitive; `applyCoupon` pure), each Pydantic v2 input (`extra=forbid`) + typed output; `ToolSpec.provider_schema()` → serializable `{name, description, parameters}` (no Groq/Gemini). New `app/services/coupons.py` (v0 deterministic stub, no coupons table). Commit `05cb623`.
- **US-E5-02** executor `app/agent/executor.py`: validate (422 + per-field) → scope (reads any-authed; cart/order owner via services 404-no-leak; refund = owner OR support/admin) → execute real services → idempotency (mutating, `tool:<name>`) → bounded retry+timeout (`TransientToolError` only, never domain `APIError`; exhaustion/timeout → `rate_limited` 429) → audit `AgentAction` (applied|refused). Commit `b82d92f`.
- **[REVIEW] guardrail fork for the human (ADR-0029):** refund allowed for owner OR support/admin with **no spend cap and no HITL threshold** (v0 default). Decide the cap + HITL threshold (above which → `hitl_deferred`) and whether owner-refund should require support/admin — seams (`_check_scope`, `AgentOutcome.hitl_deferred`) are in place for the future guardrail epic.
- **Gaps (ADR-0029):** coupons = static map, no DB table / redemption / expiry; refund = thin (mark captured Payment→refunded), full returns/refund epic deferred (returns router still 501). New ErrorCodes: **none**. Gate: ruff✓ mypy --strict✓ (app+tests) · **pytest 176 passed** (137 baseline + 39 agent). Juno (US-QA-D12) owns the per-tool E2E matrix.

**Juno, tool-call E2E (US-QA-D12) — integrated @ `a25c13d`:**
- New `api/tests/agent/test_tool_e2e.py` (36 tests): exhaustive **success x validation-failure x unauthorized** matrix for all 8 tools + journey-level idempotency / retry-timeout / audit-outcome. DB-backed on `seeded_db`/`handles`, calls `execute_tool` directly (one identity path). Pins to closed `ErrorCode`+status, asserts real effects (cart line / placed order+intent+**reserved stock** / refunded payment). Does NOT re-assert Echo's schema/executor atoms (layer split in module docstring).
- **Contract notes surfaced (asserted AS BUILT, not failures):** (1) `execute_tool` takes a non-optional `User` — the "no identity" unauth case is enforced upstream at `require_user`, not in the executor; the executor-level unauth surface is cross-user resource access (foreign order/cart → `not_found` no-leak) + the refund ownership gate. (2) `_check_scope` is a no-op (ADR-0029 [REVIEW] fork): a non-owner non-privileged `refund` (incl. a **seller**) surfaces `not_found` no-leak, **NOT `403 forbidden`** — no caller can currently produce a `forbidden` from this layer; the 403 seam is dormant pending the guardrail decision. (3) **addToCart reserves no stock** (reservation is taken at checkout/draftOrder) — verified.
- Gate: ruff✓ mypy --strict✓ · **pytest 212 passed, 0 failed** (176 baseline + 36 new); no regressions. Commit `4273454`.
- **Atlas integration check:** all 3 `[AFK]`, no `[REVIEW]` to gate up-front. Sequential dependency-order FF on DB host `55444` (shared working tree): Echo's `feature/tools-layer` (`7fcabb1`, both stories) → Juno's `feature/qa-tools-e2e` (`a25c13d`). Final independent gate green **by code**: ruff✓ · mypy --strict✓ (98 files) · **pytest 212 passed, 0 failed**; no migration. Pushed `integration/backend` (`25bc447`→`a25c13d`) + both `feature/*`. `dev` PR **held for Day-14 W2 gate**. **Carried to human:** Echo's refund-guardrail `[REVIEW]` (no spend cap / no HITL threshold; `_check_scope` no-op so refund mis-scope surfaces `not_found` not `403`) — ADR-0029, non-blocking, decide alongside the Day-13/14 guardrail work.

**Current branch:** `integration/frontend` @ `903379e` (Day 20 cart/checkout/seller, off `dev` @ `40127b4`). Parallel line: `integration/agents` @ `ec741db` (Days 15–17 agent layer). Both merge to `dev` at the Day-21 W3 gate.
**Last updated:** 2026-06-28 (end of Day 20 — cart/checkout + agentic approval gate + seller dashboard shipped)

## ✅ Done — Day 13 (W2D6 — backend integration & RAGAS harness, integrated to `integration/backend` @ `256eb2f`)
**Echo, RAGAS harness (US-E7-00) — branch `feature/evals-ragas-harness` off `integration/backend`:**
- New offline lib `api/app/eval/` scores all 4 RAGAS metrics over `golden-v0.jsonl`: **context precision/recall** (deterministic set-overlap, the SAME formula as the Day-10 retrieval smoke, factored to one shared helper — no judge) + **response relevancy / faithfulness** (behind a `Judge` seam; default `DeterministicJudge` = a *harness-exercising lexical stub, NOT a semantic judge*). Per-question `answer` comes from a default `StubAnswerer` (= `expected_answer` stand-in; no live agent runtime yet). Both seams mirror `get_embedder`/`get_retriever`: registry + `EVAL_JUDGE`/`EVAL_ANSWERER` settings (added to `app/core/config.py`), fail loud on unknown. Real **Claude eval judge** registers under `"claude"` (default model `claude-opus-4-8`, referenced-not-called) — a one-line swap when keys land.
- **Default path runs with NO LLM keys + NO network** (CI-safe); **no `ragas`/LLM hard-dep** (pure stdlib; real ragas slots behind the same `Judge` Protocol). Artifact `docs/qa/eval/results/ragas-<date>.json` + `ragas-latest.json` (gitignored, matches smoke) carries per-item 4 scores + means + judge/answerer identity + scope counts + **refusal log** (refusal awareness = signal, not a v0 gate).
- **CI-safe sample run** `api/tests/qa/test_ragas_harness.py` (1 DB-backed run-over-golden + 12 pure unit pins): asserts the harness RAN + 4 means well-formed `[0,1]` + artifact written + refusals logged — NOT quality thresholds on stub scores (smoke-style honesty). **Sample means (deterministic judge, 25 records):** precision **0.000** / recall **0.000** (expected raw-question keyword gap, closed by semantic retrieval Week-4) · relevancy **0.613** · faithfulness **1.000**; 17 product-grounded / 8 policy-only; all 4 `expects_refusal` items detected refusing.
- Gate: ruff✓ mypy --strict✓ (106 files) · **pytest 225 passed, 0 failed** (212 baseline + 13 new); no migration. **ADR-0030.** **`[REVIEW]` carried (non-blocking):** confirm Claude as the eval JUDGE + exact model (`claude-opus-4-8`) before wiring; product runtime LLM stays Groq/Gemini. NOT merged — Atlas integrates.

**Juno, backend regression pack (US-QA-D13) — branch `feature/evals-regression-pack` off `integration/backend`:**
- New `api/tests/regression/test_backend_regression.py` adds the **integration-pass** layer no per-feature suite provided: **one ordered traversal of the whole API surface against a single freshly-seeded DB** — auth (login→`/auth/me`) → catalog (list+detail) → search (keyword hit→PDP) → cart (add→`GET /cart`) → order (checkout→reserve→intent→capture→status) → agent tools via `execute_tool` (search+productDetails read, orderStatus, addToCart+draftOrder mutating) → error envelope (garbage-token 401 + cross-user 404). Pins the **cross-feature invariants** asserted nowhere else: REST + agent resolve to the SAME seeded product; order `total_minor` == cart subtotal; reserve+capture move inventory by exactly the ordered qty; agent `orderStatus` mirrors REST `GET /orders/{id}`; failure carries the canonical `{"error":{code,message,details}}` shape with a closed `ErrorCode`. Reuses `seeded_db`/`persona_client`/`handles` (no hand-seed); error asserts pin closed `ErrorCode`+status; inventory deltas read live (anti-drift). Layer split vs the per-feature atoms is documented in the module docstring + README.
- **Readable report:** a `report` fixture emits `docs/qa/regression/regression-<date>.md` + stable `regression-latest.md` — header = seed identity (live product count) + timestamp + surfaces-green/total, body = per-surface table (exercised / key invariant / pass-fail). Latest run = **8/8 surfaces green**. Run artifacts gitignored (`docs/qa/regression/regression-*.md`, matching the Day-10 smoke convention); committed `README.md` documents the pack + one-command runner `pytest tests/regression` (or `-k regression`).
- Gate: ruff✓ mypy --strict✓ (app+tests) · **pytest 228 passed, 0 failed** (225 baseline + 3 new, no regressions); no migration. **NO contract/impl discrepancy found** — the whole surface is green as built. No new `[REVIEW]`/`[DECISION]`. NOT merged — Atlas integrates.

**Atlas integration check:** both `[AFK]`, no up-front `[REVIEW]` to gate. Sequential dependency-order FF on DB host `55444` (shared working tree ⇒ sequential): Echo's `feature/evals-ragas-harness` (`530c829`, US-E7-00) → independent gate green (ruff✓ mypy --strict✓ 106 files · pytest 225) → Juno's `feature/evals-regression-pack` (US-QA-D13). Final independent gate on `integration/backend` green **by code**: ruff✓ · mypy --strict✓ (**108 files**) · **pytest 228 passed, 0 failed** (~8 min, per-test DB create/drop); no migration. **`dev` PR opened for the Day-14 W2 gate** (US-QA-D14) — human ratifies the merge.

**CI remediation (Day-14 gate, by code — PR #5, all checks green incl. GitGuardian ✅).** The first real GitHub CI run of the Week-2 backend (CI only fires on PRs to `dev`, so Days 8–13 had never been CI-scanned) surfaced issues fixed in three passes:
- **(1) Missing dep** — `argon2-cffi` (Day-9 auth hash) was in `pyproject.toml` but absent from `requirements.txt`, which is what CI `pip install`s → `type-check (api)` 4× import-not-found + `test (api)` conftest `ModuleNotFoundError`. Added to `requirements.txt`.
- **(2) The actual GitGuardian block = a "Generic Password" false-positive** in `api/tests/api/test_auth_e2e.py:179` (`"password": "definitely-not-it"`, a deliberately-wrong login in a negative-path test) + siblings `GOOD_PASSWORD`/`NEW_PASSWORD`/`"wrong-password"` in the Week-2 auth tests. Per the Day-7 precedent (env-derive/scrub test passwords) + the human's clean-history choice, all four literals were scrubbed to `CHANGE_ME_*` placeholders via `git filter-repo --replace-text` scoped to `integration/backend` only (these files don't exist on `dev`); tests stay internally consistent (27 auth+regression tests pass). Tip `256eb2f`.
- **(3) Pre-emptive cred-doc hygiene** — while hunting the finding, also scrubbed literal test-DB creds (`postgres:postgres@…:55444`, `qa_local_pw`, `ci_pw`) from the QA docs (`fixture-plan.md`, `db-migration-test-plan.md`, ADR-0019); these were inherited from a **Day-6 commit on `dev`**, so the human authorized a range-scoped `filter-repo` across **`dev` + `integration/backend` together** (shared base preserved), force-pushed both. NB: this cred was NOT the GitGuardian blocker (it had passed at the Day-7 scan) — it was real hygiene done alongside. Backups: tags `backup/pre-scrub-*` + origin reflog.
- **Net:** trees differ from pre-rewrite only in placeholder lines — **no app code or behavior changed**; local gate unchanged (228 passed). Each rewrite force-push auto-closed/replaced the PR (#4→#5). 

**Carried to human (non-blocking, all surfaced in the PR):** (1) Echo's refund guardrail (ADR-0029); (2) **Claude as the eval JUDGE + exact model** (ADR-0030, `claude-opus-4-8` registered-not-called; product runtime LLM stays Groq/Gemini); (3) Echo's real embedding model/`EMBED_DIM`; (4) Week-4 deploy keys.

## ✅ Done — Day 10 (W2D3 — catalog & hybrid search, integrated to `integration/backend` @ `eecc97a`)
- **US-E4-06** catalog endpoints + inventory `[AFK]` — Orion. Real `GET /products` (cursor list, `category`/`store_id` filters, active+not-deleted), `GET /products/{id_or_slug}` (detail w/ variants/images/inventory/rating; missing→`404`), `GET /stores/{id_or_slug}`. New `app/api/pagination.py` (reusable opaque base64url **keyset** cursor `(created_at,id)`, garbage→`422`) + `app/services/catalog.py` (ORM→DTO, `selectinload` no-N+1). **Inventory signal:** `VariantOut.in_stock=(qty_on_hand-qty_reserved)>0` (no row→OOS) + `restock_eta_days` — seeded low-stock/OOS edges surface right. id-or-slug guards the UUID cast (no PG `DataError`). **ADR-0025.** FF `fda3d9e` (gate: ruff✓ mypy(strict,54)✓ pytest 84✓).
- **US-E4-08** embedding provider seam + refresh-on-change `[AFK]` — Sable/Echo. Formalized the seed `embed_text` stub into `app/services/embeddings.py`: `Embedder` Protocol + deterministic `StubEmbedder` default + `get_embedder()` resolving `_PROVIDERS[settings.embed_provider]` (new `EMBED_PROVIDER`, default `"stub"`). **Echo's real-model swap = one line** (register provider + set env; unknown→fails loud). `refresh_product_embedding(session, product_id)` rebuilds the doc + UPSERTs the embedding — used by the seed now, by future seller writes later. `EMBED_DIM`=768 + migration 0003 untouched. **ADR-0026.** FF `0202e4f` (gate: ruff✓ mypy(strict,55)✓ pytest 86✓).
- **US-E4-07** hybrid search — keyword LIVE + semantic/rerank seam `[AFK]` — Orion/Sable. `GET /search` real, built to Decision-4. **Keyword = Postgres FTS:** migration **0005** adds STORED generated `products.search_tsv` (weights title A>category B>description C) + **GIN** index; `websearch_to_tsquery` + `ts_rank` (per-result `score`). `app/services/search.py`: `Retriever` Protocol w/ live `KeywordRetriever` + inert `SemanticRetriever`/`HybridRetriever` seams (reference `get_embedder()`), `get_retriever()`/`_ACTIVE_MODE` switch, identity `rerank()`. `mode` surfaces in `meta` (`SearchMeta`/`SearchEnvelope`, keeps `{data, meta}`). **Echo flips `EMBED_PROVIDER`+`_ACTIVE_MODE` → hybrid, no contract change.** v0: single ranked page (`next_cursor=null`; relevance has no keyset); no-match→empty 200; bad `q`/limit→422. **ADR-0027.** FF `aeac4eb` (gate: ruff✓ mypy(strict,56)✓ pytest 95✓; 0005 up→down→up clean).
- **US-QA-D10** search E2E + retrieval smoke `[AFK]` — Juno/Echo. `tests/api/test_search_e2e.py` (J-BUY-01 discovery journey: hit→fetchable PDP, refinement narrows, category facet, `meta.mode` stable). `tests/qa/test_retrieval_smoke.py` — **deterministic no-LLM** context **precision@5/recall@5** over the **17/25 product-grounded** golden items (8 policy-only excluded = Echo's agent-RAG surface); captures `docs/qa/eval/results/retrieval-smoke-*.json` (+ `-latest`) w/ per-item + failing-sample log; **SMOKE not a gate** (no recall floor). **Honest finding:** precision/recall@5 = **0.000** — `websearch_to_tsquery` ANDs every token so raw NL *questions* match nothing; a liveness anchor proves distinctive *terms* (tide/wallet/candle) DO recall the right product → the zero is the lexical gap **semantic retrieval closes**, not a bug. FF (gate: ruff✓ mypy(strict)✓ pytest 103✓).
- **Atlas integration check:** sequential gate-then-FF on DB host `55444` (dependency order 06→08→07→QA-D10; shared working tree ⇒ sequential). Final `integration/backend` green by code: ruff✓ mypy(strict)✓ **pytest 103 passed, 0 failed**. **Hygiene fix `eecc97a`:** smoke run-artifacts (`results/*.json`, wall-clock `generated_at`) untracked + gitignored to stop tree/CI churn (scores byte-identical bar timestamp); test still writes them on run. Pushed `integration/backend` + 4 `feature/*`. `dev` PR **held for Day-14 W2 gate**. Standing `[DECISION]` (non-blocking): Echo's real embedding model/`EMBED_DIM` (tied to Week-4 keys) — seam is ready.

## ✅ Done — Day 8 (W2D1 — backend skeleton gap-close, on `integration/backend`)
- **Reconciliation:** the planned Day-8 stories — **US-E4-01** (FastAPI skeleton/health/error-envelope), **US-E4-02** (Scalar `/docs`), **US-E4-03** (Pydantic v2 base schemas + envelope) — were **front-loaded into Day 6** (contract-v0 build) and already on `dev`. Verified by code. Scoped Day-8 to the two genuine gaps, both `[AFK]`.
- **US-E4-01 (gap)** runtime error envelope + CORS `[AFK]` — Orion. The canonical error body was *documented* in OpenAPI but never *emitted* — `stub()` raised a bare `HTTPException`→`{"detail":...}`. New `api/app/core/errors.py`: `APIError(status,code,message,details)` + `install_error_handlers(app)` mapping `APIError` · Starlette `HTTPException` (status→closed-`ErrorCode` + status-class fallback) · `RequestValidationError` (422 `validation_error`, per-field errors in `details`) · catch-all `Exception`→500 `internal_error` (no internal leak) → all to `{"error":{code,message,details}}`. `stub()` now raises `APIError(501, not_implemented)`. **CORS:** env-driven `cors_origins` (`HEARTH_CORS_ORIGINS`, comma-split; no global env_prefix so bare `DATABASE_URL`/`EMBED_DIM` still resolve) + `CORSMiddleware` (allow_credentials; headers `Authorization`/`Content-Type`/`Idempotency-Key`). `.env.example` updated. **ADR-0022.** `5e7ed35`.
- **US-QA-D08** API E2E smoke suite `[AFK]` — Juno. Named DB-free `api/tests/test_api_smoke.py` (15 tests, `TestClient` only): app boot + full router inventory mounted (1 path/group; catalog mounts at `/products`+`/stores`, no `/catalog` prefix), `/docs`→200 HTML w/ Scalar marker, `/openapi.json`→200 valid (3.x + non-empty `paths` + every group), CORS positive (ACAO echoed) **and negative** (`evil.example` not echoed), cross-group envelope spot-check (`/products`→501). Orion's `test_error_envelope.py` kept as the per-code detail layer (non-overlapping). `6727bb5`.
- **Atlas integration check:** independent full gate green **by code** — `ruff` clean · `mypy --strict` clean (48 files) · **pytest 34 passed + 15 skipped** (skips = DB tests, no Postgres in this run). Live boot smoke (no DB): 501/404/422 canonical envelopes + CORS preflight ACAO all verified. FF-merged `feature/api-cors-envelope` → `integration/backend` (`6727bb5`); pushed. `dev` PR **held for Day-14**.

## ✅ Done — Day 9 (W2D2 — auth, integrated to `integration/backend` @ `b51f746`)
- **US-E4-04** auth handlers `[REVIEW ✓ ADR-0023]` — Orion. Real `/auth/{register,login,logout,me,verify}` replacing the 501 stubs. **argon2id** password hashing + **SHA-256 opaque session tokens** (`app/core/security.py`); **sliding 7-day** sessions. register → unverified account + session (409 `email_taken` on live dup); login → 401 on bad-creds/unknown-email, **403 while `email_verified=false`**; logout deletes the row; verify flips the flag. Migration **0004** adds `users.email_verified` (+ ORM col); **auth-backed seeding** writes per-persona argon2 hash of `HEARTH_TEST_PASSWORD` + `email_verified=true` (idempotent). **`.test`-TLD allowed through `EmailStr`** so seeded personas validate (**ADR-0024**). Commits `b382a0f`, `61e75e5`.
- **US-E4-05** authZ scope dependency `[AFK]` — Orion. Shared `app/api/deps.py`: Bearer → SHA-256 → live-session lookup with sliding bump → current user; `require_user` + `require_role(*roles)` (403 on mismatch); `get_session` resolves the engine from current settings (lets the ASGI client hit the throwaway test DB). Agent layer (Echo) resolves the acting user via the same dep. All errors via `APIError` → canonical envelope.
- **Tests:** new `api/tests/api/test_auth_handlers.py` (10 handler tests: register happy/email_taken, login bad-creds 401 / unknown 401 / unverified 403 / verified 200, me, me-401, logout-revokes, verify-flips). **The 6 persona-login fixture xfails now PASS.** Local gate: `ruff`✓ · `mypy --strict`✓ (71 files) · **pytest 57 passed + 2 failed** — both failures are **stale Juno-owned assertions** my story invalidates (see below). Migration up→down(0004→0003)→up proven clean.
- **✓ Resolved by Juno (the 2 stale assertions Orion's handlers invalidated):** (1) `tests/test_error_envelope.py` 501-stub assertion repointed `/auth/login` → `GET /products` (still stubbed). (2) `tests/db/test_migrations.py` head `0003_embeddings` → `0004_email_verified`. (3) no change needed — `test_fixture_spine.py` uses *imperative* `pytest.xfail()` in an `except` that no longer fires (no decorator, no `xfail_strict`), so the 6 logins pass cleanly.
- **US-QA-D09** auth E2E `[AFK]` — Juno. New `api/tests/api/test_auth_e2e.py` (14 tests, DB-backed via `seeded_db`/`api_client`): full **register→verify→login→/auth/me→logout→denied** lifecycle + negatives (bad-pw 401, unknown-email 401, unverified 403, missing/garbage/wrong-scheme token 401, expired token 401, dup email 409) + sliding-window expiry bump. **Scope (US-E4-05):** `require_role("admin")` asserted on a test-only admin-guarded route reusing the production auth dep + error handlers + throwaway DB (wrong role→403, right role→200, no token→401) — no real scoped endpoint exists yet (routers still stubs). Assertions pin closed `ErrorCode`s, not prose; layer split (vs Orion's unit `test_auth_handlers.py`) documented in the module docstring. **Fixed the 2 stale assertions** (#1 repointed to `GET /products`; #2 head→`0004_email_verified`); **#3 needs no change** — the persona-login test uses imperative `pytest.xfail()` in an `except` that no longer fires (no decorator xfail, no `xfail_strict`), so the 6 logins pass cleanly. **Full gate green: `ruff`✓ · `mypy --strict`✓ (72 files) · pytest 73 passed, 0 failed.** Commits `5400f92`, `b51f746`.
- **Atlas integration check:** independent full gate green **by code** on the DB (host `55444`) — `ruff` clean · `mypy --strict` clean (72 files) · **pytest 73 passed, 0 failed** (lone warning = benign upstream `python-multipart` deprecation). E2E layer exercises the real ASGI app against a real seeded DB, so the runtime path is covered. **The 6 persona-login fixture xfails are flipped green.** Decision-of-record: **ADR-0023** (auth impl calls) + **ADR-0024** (`.test`-TLD `EmailStr` shim); contract `[REVIEW] §5`→RATIFIED. FF-merged `feature/auth-handlers` → `integration/backend` (`b51f746`); pushed. `dev` PR **held for Day-14 W2 gate**.

## ✅ Done — Day 10 (W2D3 — catalog + search, on `integration/backend`)
- **US-QA-D10** Search E2E (J-BUY-01) + RAGAS retrieval smoke `[AFK]` — Juno (Echo's eval concerns baked in). Two non-overlapping layers on the `seeded_db`/`api_client` spine (layer split vs Orion's `test_search.py` documented in each docstring). **(1)** `api/tests/api/test_search_e2e.py` (4): the discovery journey — search hit → fetchable PDP (id+slug→same detail, variants present), query refinement narrows to a subset, category-faceted hits fetchable + in-category, `meta.mode` stable across the journey. **(2)** `api/tests/qa/test_retrieval_smoke.py` (4): **no-LLM** retrieval-half — context precision@5/recall@5 over **17/25 product-grounded** golden items (variant sku→product slug resolved live = anti-drift; **8 policy-only excluded** as out-of-catalog/Echo's RAG surface). Captures `docs/qa/eval/results/retrieval-smoke-<date>.json` (+`-latest.json`) + prints summary + logs every failing sample. **Headline finding:** mean precision@5/recall@5 = **0.000** — keyword `websearch_to_tsquery` ANDs every token, so raw natural-language *questions* recall nothing; a liveness anchor proves single distinctive *terms* (tide/wallet/candle) DO recall the right product, so the zero is the lexical gap **semantic retrieval closes (Decision-4)**, not a broken endpoint. **SMOKE, NOT a gate** — asserts only that it ran + scores well-formed + failures logged + artifact written (no recall floor; that would hard-fail on the expected gap). README updated: this is the retrieval-half baseline Echo's Week-4 semantic+judge-LLM RAGAS extends. **Gate green: `ruff`✓ · `mypy --strict`✓ (82 files) · pytest 103 passed, 0 failed** (95 prior + 8 new); no heavy `ragas`/LLM dep added. On `feature/qa-search-ragas` — Atlas integrates.

## ✅ Done — Day 7 (W1 integration review — capstone, merged to `dev`)
- **US-QA-D07** W1 full integration dry run + merge to `dev` `[REVIEW ✓]` — Atlas/Juno/Rhea. Human ratified **1 consolidated PR**.
- **Topology:** the four W1 lines are a **strict linear chain** (`foundations ⊂ design ⊂ data ⊂ backend`) — `integration/backend` already contained all of Days 1–6, so **zero cross-branch conflicts**. One PR landed everything.
- **Real CI gate wired** (Rhea, the deferred Day-4 job): `ci.yml` placeholders → real commands — web lint (`next lint`) + type-check (`tsc --noEmit`), api lint (`ruff`) + type-check (`mypy --strict`), **`test-api` = pytest against a `pgvector/pgvector:pg16` service** (trust auth, no credential literal; suite CREATE/DROPs throwaway DBs per test). e2e-smoke real; docker-build a documented no-op.
- **Local dry run green by code:** ruff✓ · mypy --strict (47 files)✓ · **pytest 24 passed + 6 xfailed** (xfail = Week-2 `/auth/login`) · app boots, `/health` + Scalar `/docs` 200, `POST /agent/conversations` = `text/event-stream` · placeholder E2E smoke 2 passed.
- **GitGuardian remediation:** first scan flagged **2 test/ephemeral creds** (no real secrets, nothing rotated) — the CI service `postgres:postgres` (→ `POSTGRES_HOST_AUTH_METHOD=trust`) and the pinned fixture test password (→ **env-derived** `HEARTH_TEST_PASSWORD`, default a `CHANGE_ME` placeholder; manifest regenerated). Both literals scrubbed from history with **range-scoped `git filter-repo`** (`7745495..backend`) so the scaffold base shared with `dev` stayed byte-identical; force-pushed (authorized). A `backup/pre-scrub` safety ref caught a first whole-branch rewrite that severed the common base.
- **Merged:** **PR #3** `integration/backend → dev` — all checks green incl. **GitGuardian *No secrets detected ✅*** — merged as `385e541` (merge commit preserving per-day history). **PR #1** (foundations) auto-marked **merged** (commits on `dev`); the design line was included in the same merge (never needed its own PR). No open PRs remain.
- **Week 1 is complete.** `dev` now holds the full W1 foundation: monorepo + compose, brand/design system + 7 hi-fi screens, Postgres schema/migrations/ORM/seed, golden eval set, locked API contract v0 + Scalar docs, E2E fixture spine, and a real CI gate.

## ✅ Done — Day 6 (API contracts & E2E fixtures — both stories integrated)
- **US-E4-00** API contract inventory `[REVIEW ✓]` — Orion. Full contract for **36 operations** (auth 4 · products/catalog 5 · search 1 · cart 4 · orders 6 · returns 3 · agent 3 · seller 6 · admin 3 + health). Human **ratified all 6 decisions** (one changed the draft): opaque **session-token** auth · `{data, meta}` envelope · **cursor** pagination · **keyword search** semantic-ready (pgvector deferred, stays embedding-agnostic) · **agent chat = SSE `text/event-stream`** token→citations→done (the streaming generative-UI surface) · **two-step payment** intent→confirm w/ deterministic decline (`outcome="failed"` → `402 payment_declined`). Pydantic v2 schemas (`api/app/schemas/`), router stubs (501, `api/app/api/routers/`), **Scalar** docs at `/docs` (Swagger disabled). Handlers are stubs — Week-2 implements. `docs/api/contract-v0.md` (decisions → RATIFIED) + **ADR-0021**. FF-merged `feature/api-contracts-inventory` (`5438f73` draft → `7745495` locked) → `integration/backend`.
- **US-QA-D06** E2E fixture plan `[AFK]` — Juno (Sable concerns baked in). **One canonical source → 3 layers** (API httpx + session-token / Playwright testids / agent golden `source_docs`). `docs/qa/fixture-plan.md` (catalog + per-journey matrix, all **17 J-*** mapped + factory plan), `api/tests/fixtures/` + `fixture-manifest.json` (shared handles, anti-drift spine test), `api/tests/conftest.py` fixtures (`seeded_db`, `handles`, `api_client`, `persona_client`), `e2e/tests/_fixtures.ts` for the browser layer. Personas (buyer×2/seller×2/support/admin) + stock-edge products (in-stock/low/oos) + policy keys all resolve to real seed rows. **J-BUY-04** wired to the deterministic payment decline. FF-merged `feature/api-contracts-fixtures` → `1139be1`.
- **Atlas integration check:** combined `integration/backend` gate green **by code** on `pgvector/pgvector:pg16` — `ruff` clean · `mypy --strict` clean (47 files) · **pytest 24 passed + 6 xfailed** (xfails = login pending Week-2 auth). App boots, `/docs` 200, agent routes `text/event-stream`.
- **Known Week-2 follow-ups (non-blocking):** seed has no passwords (fixture pins test passwords → auth-backed seeding flips 6 xfails); 8 journey factories specified but await Week-2 cart/order/return handlers; `order_items` has no `store_id` (contract §4.3 open item, fine for v0 no-delete seed).

## ✅ Done — Day 5 (Seed, data-access & RAGAS data baseline — all 3 stories integrated)
- **US-E3-04** SQLAlchemy models / DA layer `[AFK]` — Sable. SQLAlchemy 2.0 typed ORM (`Mapped`/`mapped_column`) for all **21 tables** on the shared `Base`, split by domain under `api/app/db/models/`. Native PG enums referenced by name (`create_type=False`, labels mirrored from 0001); server defaults mirrored (uuidv7/now/jsonb/bools); partial-unique cart + email-live indexes; `embeddings.embedding = Vector(settings.embed_dim)` (never hardcoded) + HNSW. Thin DA layer `app/db/session.py` (engine + `sessionmaker` + `session_scope`). **Parity-proven vs a real migrated DB**: `test_no_metadata_diff` (Alembic `compare_metadata` → no drift; inline-UNIQUE vs `uq_*` name-only renames filtered), per-table column parity, core round-trip (user→store→product→variant→inventory + embedding at embed_dim). FF-merged `feature/data-access-models` → `9337dcc`.
- **US-E3-03** Seed: catalog/users/policies `[AFK]` — Sable. `python -m app.db.seed` (Hearth maker marketplace) via the ORM: 12 users (all 4 roles), 5 stores, 10 products, 15 variants (real SKUs/price_minor/options), 15 inventory (low-stock + restock_eta spread), images, 12 reviews (1–5 CHECK; **rating rollups recomputed**), **7 policies** (platform returns/shipping/payments/platform + per-store care/returns — genuine quotable RAG prose), 17 embeddings. **Idempotent** (upserts on natural keys); wired into `db_reset.sh` SEED hook (`SEED=0` opt-out). Embeddings via a **single Echo-replaceable `embed_text` stub** (`model='seed-stub'`, length=`settings.embed_dim`). ADR-0020. FF-merged `feature/data-access-seed` → `e4314e4`.
- **US-QA-D05** Golden eval dataset v0 `[AFK]` — Juno (Echo/Sable concerns baked in). **25 buyer/support Q&A** in `docs/qa/eval/golden-v0.jsonl` (JSONL, RAGAS/HF-native): each has `question`, grounded `expected_answer`, machine-checkable `source_docs` (product slug / variant sku / policy `kind@store|platform`), and `tags` (persona/category/difficulty/`expects_refusal`). Spread: buyer 18 / support 7; **4 refusal items** (faithfulness = don't fabricate); embedding-agnostic. Validator `api/tests/qa/test_golden_eval.py`: static well-formedness + **DB-backed seed-resolve** (reuses Sable's `migration_db` fixture → every source_doc resolves to a real row = anti-drift). README maps records → RAGAS metrics + Echo's Week-2 runner work. FF-merged `feature/qa-golden-eval` → `b456a16`.
- **Atlas integration check:** combined `integration/data` gate green **by code** on `pgvector/pgvector:pg16` — `ruff` clean, `mypy --strict` clean (22 files), **pytest 16 passed** (incl. DB-backed seed-resolve + migration contract, no skips).

## ✅ Done — Day 4 (Data model — ERD approved + schema/migrations)
- **US-E3-01** ERD `[REVIEW ✓]` — human-approved with rulings (added `reviews` + minimal `payments`; kept sessions/conversations/messages/agent_actions; single multi-store order with per-item snapshot; `EMBED_DIM`-driven vector dim). `docs/data/erd.md` flipped to APPROVED; §6/§7 resolved inline.
- **US-E3-02** Postgres schema + Alembic + pgvector `[AFK]` — Alembic initialized in `api/` (env-driven `DATABASE_URL`, no creds committed). 3 migrations: 0001 pgvector ext + 14 native enums; 0002 core schema (20 tables, FKs, CHECKs, partial-unique one-open-cart, UNIQUEs on email-live/slug/sku/order_number/inventory.variant_id, UUIDv7 fn+defaults); 0003 embeddings `vector(EMBED_DIM)` + HNSW cosine. **Proven on a fresh DB: `upgrade head` → `downgrade base` → `upgrade head` all clean** (21 tables/14 enums create+drop; uuidv7 nibble=7; dup-open-cart + review-rating CHECK enforced). ruff + mypy(strict) green. ADR-0018. Committed `feature/db-schema-migrations` → FF `integration/data`.
- **US-QA-D04** Migration contract `[AFK]` — turned Sable's one-off proof into a repeatable, CI-runnable gate. pytest `api/tests/db/` (per-test throwaway CREATE/DROP DATABASE, asserts **by name**): clean-create → 21 app tables + 14 enums + pgvector + `uuid_generate_v7()` + HNSW index (access method verified) + one-open-cart partial-unique + reviews CHECK + inventory UNIQUE; rollback → 0 tables/0 enums/fn gone/empty `alembic_version`, then idempotent `upgrade head` back to `0003_embeddings`. Guarded `api/scripts/db_reset.sh` (refuses non-dev DB names, idempotent, seed-hook for US-E3-03). **Proven on `pgvector/pgvector:pg16`: pytest 3 passed; reset guard+reset+idempotent re-run all ✓; ruff + mypy(strict) green.** Documented exception: `0001` downgrade intentionally leaves the `vector` extension (asserted, not a bug). CI = Postgres-service `test-api` job **documented in the plan, wiring deferred to Day-7** so the shared gate stays green. ADR-0019. Committed to `integration/data`.
- **Echo coordination:** `EMBED_DIM` defaults to **768** (Gemini text-embedding-004) so migrations apply today; Echo confirms the final embedding model → set `EMBED_DIM` + re-run the embeddings migration (one step). Not a blocker.

## ✅ Done — Day 3 (Design system & key screens — all 3 stories integrated)
- **US-E2-04** Design tokens → code `[REVIEW ✓]` — **Tailwind v4** (CSS-first, no config), every `palette-and-type.md` token as CSS vars on `:root`+`[data-theme=dark]`, `@theme inline` mapping; Fraunces/Outfit via `next/font`; `Button` (ember+ink primary / clay+white brand), `HearthMark` duotone, `/tokens` specimen + theme toggle. Human approved after a dark-mode tint-badge contrast fix (`text-n-900`). ADR-0015. Merged `915ac62`.
- **US-E2-05** Hi-fi screens `[REVIEW ✓]` — 7 token-truthful static HTML screens (home, search, product, cart, checkout, order-status, seller-dashboard) + index board, in `docs/brand/preview/screens/`. Carry the `data-testid` registry → double as Iris's build contract. Agent voice (Ember) is the signature surface; grounded recs + honest refusals + seller merch nudge. Human approved w/ tweaks: **qty steppers** (not dropdowns) + richer photo placeholders. ADR-0017. Merged.
- **US-QA-D03** UI E2E skeleton `[AFK]` — 17 `J-*` `test.fixme` specs (1:1 w/ QA matrix), `data-testid` registry (`docs/qa/test-ids.md` + typed `_selectors.ts`); agent voice = shared `agent-*` namespace. Smoke stays green (2 passed / 36 skipped, no server). ADR-0016. Merged.
- ADR collision fixed (both agents grabbed 0015 → QA renumbered to 0016); `.claude/skills/`+`worktrees/` gitignored.

## ✅ Done — Day 2 (shipped, all 4 [REVIEW] approved by human)
- **US-E2-01** Brand brief — name **Hearth**, positioning, tone, personas (buyer/seller/support/admin), design hand-off. `docs/brand/brand-brief.md`.
- **US-E2-02** Logo — human locked **"Keystone"** (round 1 of 3 rejected → round 2 of 6). Code-native SVG, not Stitch (ADR-0014). Canonical: `docs/brand/assets/hearth-logo-{mark,lockup}.svg`.
- **US-E2-03** Palette + type — **Clay `#B5512F`** primary / **Ember `#E08A3C`** agent-voice accent; **Fraunces** (serif display/wordmark) + **Outfit** (sans UI); warm-neutral ramp, semantic, light+dark, WCAG AA verified. Spec `docs/brand/palette-and-type.md`, specimen `docs/brand/preview/palette-and-type.html`. Lockup wordmark recut to Fraunces (TODO Day-3: outline to paths once font bundled).
- **US-QA-D02** QA matrix v0 — 4 personas, 17 journeys (`J-*` IDs, 1:1 to D03 E2E), 9 WCAG AA checks, 5 RAGAS gates (**faithfulness ≥0.90** per sign-off). `docs/qa/qa-matrix.md`.
- 4 feature branches FF-merged to `integration/design` (commits `d00c447`→`397f6ec`). ADR-0014.

## ✅ Done — Day 1
- Branch tree `main`→`dev`→`integration/foundations`; remote policy = push + real PRs.
- US-E1-01/02/03/04 + US-QA-D01: monorepo, docker-compose (pgvector, live), CI skeleton, Playwright e2e smoke, CONTRIBUTING. ADRs 0009–0013.
- **PR #1** (`integration/foundations` → `dev`): 9/9 green incl. GitGuardian. Held for the Day-7 gate.

## ✅ Done — Day 11 (W2D4 — cart & orders, integrated to `integration/backend` @ `deb6b05`)
- **US-E4-09** cart endpoints `[AFK]` (Orion) — integrated @ `1fa81ee`. All 4 `/cart` handlers live over new `app/services/cart.py` (auth via `require_user`): GET lazy-creates the single open cart; POST upserts/increments (201, `out_of_stock` 409 on cumulative over-request); PATCH sets absolute qty (qty=0→422); DELETE removes; cross-user line access→404; no inventory mutation (reservation = US-E4-10). `CartItem` gained a read-only `viewonly` `variant` nav (ORM metadata only, **no migration**). No new ErrorCodes. ruff✓ mypy(strict,57)✓ **pytest 118 passed** (103 prior + 15 new).
- **US-E4-10** order create + test-mode payment + lifecycle `[REVIEW ✓ human-approved]` (Orion) — integrated @ `5c7edf2`. 5 live handlers over new `app/services/orders.py` (+ `app/services/idempotency.py`, `app/services/errors.py`), all `require_user`: `POST /orders` checkout (open cart→`OrderDetail` 201, per-line snapshots frozen, cart→`converted`, reserve `qty_reserved += qty`; empty→`409 empty_cart`; shortfall→`409 out_of_stock` ATOMIC), `GET /orders` (newest-first), `GET /orders/{id}` (detail+payments; foreign→404), `POST /orders/{id}/payment-intent` (mock `Payment` pending, amount=total, 201), `POST /orders/{id}/payment-confirm` (`captured`→200 + capture `on_hand-=qty`,`reserved-=qty`; `failed`→**402 `payment_declined`** + release reservation + order stays `placed`). `/orders/{id}/returns` left a 501 stub (later epic). Reserve-at-checkout/capture-on-pay w/ `SELECT…FOR UPDATE` row locks, counters floored at 0; retry-after-decline re-reserves on a fresh intent. Idempotency via new **`idempotency_keys` table (migration 0006**, up→down→up clean; ORM `IdempotencyKey`; parity/migration counts 21→22) keyed `(user_id,key,endpoint)`. Decline branch `commit()`s-then-raises so release/`failed`/idempotency persist (session rolls back on raise). **Shipping/tax v0 = flat $0** (total==subtotal). No NEW ErrorCode members (`empty_cart`/`payment_declined` already in enum). **ADR-0028.** ruff✓ mypy(strict, app 61 + tests 28)✓ **pytest 131 passed** (118 prior + 13 new).
- **US-QA-D11** Checkout API E2E (J-BUY-04) `[AFK]` (Juno) — integrated @ `deb6b05`. New `api/tests/api/test_checkout_e2e.py` (6 DB-backed journey tests on `seeded_db`/`persona_client`/`handles`; inventory read live variant→`inventory` row, variants resolved from seed = anti-drift). Layer split documented in the docstring (handler-boundary atoms in Orion's `test_orders_handlers.py`/`test_cart_handlers.py` not re-asserted; these assert the *cumulative* ADR-0028 invariants end-to-end). **Happy path:** add→`GET /cart` reflects line→checkout 201 `OrderDetail` (snapshots frozen, total==subtotal) + **reserve-at-checkout (`qty_reserved`+=qty, `qty_on_hand` unchanged)** + cart→converted (fresh `GET /cart` = NEW empty cart)→intent pending (amount==total)→confirm captured 200 + **capture (`qty_on_hand`-=qty AND `qty_reserved` restored)**→`GET /orders`+`/orders/{id}` show captured payment. **Decline:** `failed`→402 `payment_declined` + PERSISTED release + payment `failed` + order still `placed`. **Idempotency:** same `Idempotency-Key`→same order, count +1 only, reserved once. **Out-of-stock:** 409 `out_of_stock`, atomic (no order row, no reservation). Cross-user→404; unauth→401. All pinned to closed `ErrorCode`+status. **No contract/impl discrepancy found.** ruff✓ mypy(strict, 1 file)✓ **pytest 137 passed** (131 prior + 6 new); no regressions.
- **Atlas integration check:** `[REVIEW]` on US-E4-10 resolved up-front — human approved (1) **reserve-at-checkout / capture-on-pay** inventory and (2) building the **two-step payment exactly as ratified** (contract-v0 / ADR-0021). Sequential dependency-order FF on DB host `55444` (cart → orders → QA; shared working tree ⇒ sequential, and orders reuses `app/services/cart.py`). Final `integration/backend` green **by code**: ruff✓ · mypy --strict✓ (90 files) · **pytest 137 passed, 0 failed**; migration 0006 up→down→up clean. FF-merged `feature/cart-endpoints` (`1fa81ee`) → `feature/orders-checkout` (`5c7edf2`) → `feature/qa-checkout-e2e` (`deb6b05`); pushed `integration/backend` + all 3 `feature/*`. `dev` PR **held for Day-14 W2 gate**. ADR-0028 records the order/payment/reservation state machine + the deliberate commit-then-raise on decline.

## 🔧 In progress
- _nothing_

## ⛔ Blocked
- _nothing_

## 🙋 Needs your decision
- **Day-14 W2 gate (`[REVIEW]`, ACTIVE):** ratify the merge of the held **`dev` PR** (`integration/backend` → `dev`) once CI is green — the single Week-2 PR (Days 8–13). See the PR description for the consolidated decision list below.
- **NEW — Claude as the eval JUDGE (ADR-0030, non-blocking):** the RAGAS harness scores faithfulness/relevancy behind a `Judge` seam; default is the CI-safe `DeterministicJudge`. Confirm **Claude `claude-opus-4-8`** as the real eval judge (registered-not-called) + that this is the model you want before wiring keys. The product RUNTIME LLM stays Groq/Gemini free-tier — the eval judge is a separate concern.
- **Refund guardrail (ADR-0029, non-blocking):** spend cap + HITL threshold; whether owner-refund needs support/admin. Seams (`_check_scope`, `hitl_deferred`) ready.
- **Echo embedding model (non-blocking):** confirm the final model → register the provider + set `EMBED_PROVIDER`; if `EMBED_DIM` changes from 768 re-run migration `0003`. Flipping `_ACTIVE_MODE`→hybrid is then one line. Day-10 keyword retrieval is live meanwhile.
- **Week 4 (deploy):** free-tier keys — Vercel, Render/Fly, Supabase/Neon, Groq/Gemini.
- _(ERD [REVIEW] resolved Day 4; merge strategy + GitGuardian remediation resolved Day 7.)_

## ⏭️ Next up
- **Week 3 (starts 2026-06-23, W3D1):** open `docs/plan/week-3.md`, assign stories, branch a fresh `integration/<module>` off `dev` @ `cc23501`. Likely the Next.js frontend build (Iris) + the product's agent runtime / SSE chat loop (Echo) on top of the now-live backend — confirm against the plan. Same cadence: accumulate on the integration line, one reviewed PR into `dev` at the week gate.
- **Carried `[REVIEW]`/`[DECISION]` (non-blocking, now on `dev`):** refund guardrail (ADR-0029), Claude eval judge (ADR-0030), real embedding model/`EMBED_DIM`, Week-4 deploy keys.
