# STATUS — single source of truth

> Atlas updates this at the start and end of every session. Keep it short.

## ▶ Next session — START HERE
You are **Atlas**, the orchestrator (full brief: `.claude/agents/atlas.md`). **Week 1 COMPLETE** (`dev` @ `385e541`). **Day 9 (W2D2 — Authentication) DONE** — real `/auth/*` handlers (argon2id hashing · **sliding 7-day** opaque session tokens · `email_verified` login gate), authZ scope dependency (`require_user`/`require_role`), migration `0004`, auth-backed seeding, and the auth E2E suite. **The 6 fixture login xfails are flipped green.** US-E4-04 `[REVIEW]` was ratified by the human (ADR-0023; `.test`-TLD `EmailStr` shim = ADR-0024). All on `integration/backend` (@ `b51f746`), pushed. Next is **Day 10 (2026-06-18) — Catalog & hybrid search**.
1. Read **only** this file + the **2026-06-18** block of `docs/plan/week-2.md`. Don't re-read earlier days or the whole repo.
2. **Day 10 = catalog + search, all `[AFK]`** (no `[REVIEW]` today). Implement: **US-E4-06** catalog list/detail + inventory endpoints (Orion); **US-E4-07** hybrid search endpoint (pgvector + keyword) + rerank stub (Orion/Sable) — build to contract Decision-4 (keyword-only **semantic-ready**: per-result `score` + top-level `SearchMode` discriminator now, **pgvector retrieval drops in behind the same shape, no contract change**); **US-E4-08** embedding generation on product change (Sable/Echo) — replace the `embed_text` `model='seed-stub'` hook. **Auth is now real** — protect/owner-scope write routes with `require_user`/`require_role` from `app/api/deps.py` (agents act under user scope). Then later-week: cart/orders (Day 11, US-E4-10 `[REVIEW]` payment), agent tools + SSE loop (Day 12, Echo), RAGAS harness (Day 13), **W2 gate + `dev` PR (Day 14, US-QA-D14 `[REVIEW]`)**. Branch per feature → `integration/backend` → PR → `dev`.
   - **Cadence note:** the `dev` PR for all of Week-2 is **held for the Day-14 W2 gate** (US-QA-D14), mirroring W1's Day-7 single-PR pattern. Accumulate on `integration/backend`; keep it green by code each day.
   - **Error-envelope + CORS are now live** (not just documented): every non-2xx returns `{"error":{code,message,details}}` via `app/core/errors.py` (`APIError` + `install_error_handlers`); raise `APIError(status, ErrorCode.x, msg)` in new handlers, **don't** use bare `HTTPException`. CORS origins are env-driven (`HEARTH_CORS_ORIGINS`).
3. **CI is real now** — every PR to `dev` runs web+api lint/type-check + the **pgvector `test-api`** job + GitGuardian. Keep it green by code. The `test-api` service uses **trust auth** (no credential literal). docker-build is still a documented no-op (wire `docker compose build` when CI minutes are tuned).
4. **Locked contract-v0 (the Week-2 build target):** `docs/api/contract-v0.md` + ADR-0021. 36 ops. Ratified: opaque **session-token** auth · `{data, meta}` envelope · **cursor** pagination · **keyword search** (semantic-ready, pgvector deferred to Echo) · **agent chat = SSE** `text/event-stream` (token→citations→done) · **two-step payment** intent→confirm, deterministic decline via `PaymentConfirmRequest.outcome="failed"` → `402 payment_declined`. Pydantic v2 schemas in `api/app/schemas/`, routers (501 stubs) in `api/app/api/routers/`. **E2E fixture spine** ready — `docs/qa/fixture-plan.md` + `api/tests/fixtures/` (`seeded_db`/`handles`/`api_client`/`persona_client`); 17 J-* journeys mapped; 8 journey factories specified, awaiting the Week-2 cart/order/return handlers.
5. **Fixture test password is env-derived:** `TEST_PASSWORD = os.environ.get("HEARTH_TEST_PASSWORD", "CHANGE_ME_test_pw")` (handles.py); manifest mirrors the default. Week-2 auth-backed seeding writes exactly these per-persona credentials.
6. **Still Echo's (Week-2):** `EMBED_DIM=768` + `embed_text` `model='seed-stub'` → real model/dim + re-run migration `0003`; RAGAS runner over `golden-v0.jsonl` (faithfulness ≥0.90); implement the SSE agent stream loop.
7. **DB ops:** `docker compose up -d db` = `pgvector/pgvector:pg16`; **host 5432 is occupied by a stray `local-postgres`** — bring up on an alt host port (`DB_PORT=55444`), set `DATABASE_URL` to it. Tooling via venv (`api/.venv`, gitignored) — ruff/mypy/pytest not on base PATH. Don't commit creds (GitGuardian scans full history; use trust/`CHANGE_ME`/var-refs).
8. **Housekeeping:** local `backup/pre-scrub` ref + a throwaway compose `db` may still be around from Day-7 — safe to delete/`docker compose down`. The Day-7 history rewrite changed SHAs on the data/backend line; if you have other local checkouts of `integration/backend`, reset them to `origin`.

**Phase:** Week 2 · Day 9 (2026-06-17) — ✅ COMPLETE (auth: handlers + scope dep + E2E; 6 login xfails flipped; US-E4-04 `[REVIEW]` ratified). Day 10 (catalog & search, all `[AFK]`) not started.
**Current branch:** `integration/backend` @ `b51f746` (W2 integration line). `dev` PR held for Day-14 W2 gate.
**Last updated:** 2026-06-17 (end of Day 9)

## ✅ Done — Day 8 (W2D1 — backend skeleton gap-close, on `integration/backend`)
- **Reconciliation:** the planned Day-8 stories — **US-E4-01** (FastAPI skeleton/health/error-envelope), **US-E4-02** (Scalar `/docs`), **US-E4-03** (Pydantic v2 base schemas + envelope) — were **front-loaded into Day 6** (contract-v0 build) and already on `dev`. Verified by code. Scoped Day-8 to the two genuine gaps, both `[AFK]`.
- **US-E4-01 (gap)** runtime error envelope + CORS `[AFK]` — Orion. The canonical error body was *documented* in OpenAPI but never *emitted* — `stub()` raised a bare `HTTPException`→`{"detail":...}`. New `api/app/core/errors.py`: `APIError(status,code,message,details)` + `install_error_handlers(app)` mapping `APIError` · Starlette `HTTPException` (status→closed-`ErrorCode` + status-class fallback) · `RequestValidationError` (422 `validation_error`, per-field errors in `details`) · catch-all `Exception`→500 `internal_error` (no internal leak) → all to `{"error":{code,message,details}}`. `stub()` now raises `APIError(501, not_implemented)`. **CORS:** env-driven `cors_origins` (`HEARTH_CORS_ORIGINS`, comma-split; no global env_prefix so bare `DATABASE_URL`/`EMBED_DIM` still resolve) + `CORSMiddleware` (allow_credentials; headers `Authorization`/`Content-Type`/`Idempotency-Key`). `.env.example` updated. **ADR-0022.** `5e7ed35`.
- **US-QA-D08** API E2E smoke suite `[AFK]` — Juno. Named DB-free `api/tests/test_api_smoke.py` (15 tests, `TestClient` only): app boot + full router inventory mounted (1 path/group; catalog mounts at `/products`+`/stores`, no `/catalog` prefix), `/docs`→200 HTML w/ Scalar marker, `/openapi.json`→200 valid (3.x + non-empty `paths` + every group), CORS positive (ACAO echoed) **and negative** (`evil.example` not echoed), cross-group envelope spot-check (`/products`→501). Orion's `test_error_envelope.py` kept as the per-code detail layer (non-overlapping). `6727bb5`.
- **Atlas integration check:** independent full gate green **by code** — `ruff` clean · `mypy --strict` clean (48 files) · **pytest 34 passed + 15 skipped** (skips = DB tests, no Postgres in this run). Live boot smoke (no DB): 501/404/422 canonical envelopes + CORS preflight ACAO all verified. FF-merged `feature/api-cors-envelope` → `integration/backend` (`6727bb5`); pushed. `dev` PR **held for Day-14**.

## ✅ Done — Day 9 (W2D2 — auth, integrated to `integration/backend` @ `b51f746`)
- **US-E4-04** auth handlers `[REVIEW ✓ ADR-0023]` — Orion. Real `/auth/{register,login,logout,me,verify}` replacing the 501 stubs. **argon2id** password hashing + **SHA-256 opaque session tokens** (`app/core/security.py`); **sliding 7-day** sessions. register → unverified account + session (409 `email_taken` on live dup); login → 401 on bad-creds/unknown-email, **403 while `email_verified=false`**; logout deletes the row; verify flips the flag. Migration **0004** adds `users.email_verified` (+ ORM col); **auth-backed seeding** writes per-persona argon2 hash of `HEARTH_TEST_PASSWORD` + `email_verified=true` (idempotent). **`.test`-TLD allowed through `EmailStr`** so seeded personas validate (**ADR-0024**). Commits `b382a0f`, `61e75e5`.
- **US-E4-05** authZ scope dependency `[AFK]` — Orion. Shared `app/api/deps.py`: Bearer → SHA-256 → live-session lookup with sliding bump → current user; `require_user` + `require_role(*roles)` (403 on mismatch); `get_session` resolves the engine from current settings (lets the ASGI client hit the throwaway test DB). Agent layer (Echo) resolves the acting user via the same dep. All errors via `APIError` → canonical envelope.
- **Tests:** new `api/tests/api/test_auth_handlers.py` (10 handler tests: register happy/email_taken, login bad-creds 401 / unknown 401 / unverified 403 / verified 200, me, me-401, logout-revokes, verify-flips). **The 6 persona-login fixture xfails now PASS.** Local gate: `ruff`✓ · `mypy --strict`✓ (71 files) · **pytest 57 passed + 2 failed** — both failures are **stale Juno-owned assertions** my story invalidates (see below). Migration up→down(0004→0003)→up proven clean.
- **✓ Resolved by Juno (the 2 stale assertions Orion's handlers invalidated):** (1) `tests/test_error_envelope.py` 501-stub assertion repointed `/auth/login` → `GET /products` (still stubbed). (2) `tests/db/test_migrations.py` head `0003_embeddings` → `0004_email_verified`. (3) no change needed — `test_fixture_spine.py` uses *imperative* `pytest.xfail()` in an `except` that no longer fires (no decorator, no `xfail_strict`), so the 6 logins pass cleanly.
- **US-QA-D09** auth E2E `[AFK]` — Juno. New `api/tests/api/test_auth_e2e.py` (14 tests, DB-backed via `seeded_db`/`api_client`): full **register→verify→login→/auth/me→logout→denied** lifecycle + negatives (bad-pw 401, unknown-email 401, unverified 403, missing/garbage/wrong-scheme token 401, expired token 401, dup email 409) + sliding-window expiry bump. **Scope (US-E4-05):** `require_role("admin")` asserted on a test-only admin-guarded route reusing the production auth dep + error handlers + throwaway DB (wrong role→403, right role→200, no token→401) — no real scoped endpoint exists yet (routers still stubs). Assertions pin closed `ErrorCode`s, not prose; layer split (vs Orion's unit `test_auth_handlers.py`) documented in the module docstring. **Fixed the 2 stale assertions** (#1 repointed to `GET /products`; #2 head→`0004_email_verified`); **#3 needs no change** — the persona-login test uses imperative `pytest.xfail()` in an `except` that no longer fires (no decorator xfail, no `xfail_strict`), so the 6 logins pass cleanly. **Full gate green: `ruff`✓ · `mypy --strict`✓ (72 files) · pytest 73 passed, 0 failed.** Commits `5400f92`, `b51f746`.
- **Atlas integration check:** independent full gate green **by code** on the DB (host `55444`) — `ruff` clean · `mypy --strict` clean (72 files) · **pytest 73 passed, 0 failed** (lone warning = benign upstream `python-multipart` deprecation). E2E layer exercises the real ASGI app against a real seeded DB, so the runtime path is covered. **The 6 persona-login fixture xfails are flipped green.** Decision-of-record: **ADR-0023** (auth impl calls) + **ADR-0024** (`.test`-TLD `EmailStr` shim); contract `[REVIEW] §5`→RATIFIED. FF-merged `feature/auth-handlers` → `integration/backend` (`b51f746`); pushed. `dev` PR **held for Day-14 W2 gate**.

## ✅ Done — Day 7 (W1 integration review — capstone, merged to `dev`)
- **US-QA-D07** W1 full integration dry run + merge to `dev` `[REVIEW ✓]` — Atlas/Juno/Rhea. Human ratified **1 consolidated PR**.
- **Topology:** the four W1 lines are a **strict linear chain** (`foundations ⊂ design ⊂ data ⊂ backend`) — `integration/backend` already contained all of Days 1–6, so **zero cross-branch conflicts**. One PR landed everything.
- **Real CI gate wired** (Rhea, the deferred Day-4 job): `ci.yml` placeholders → real commands — web lint (`next lint`) + type-check (`tsc --noEmit`), api lint (`ruff`) + type-check (`mypy --strict`), **`test-api` = pytest against a `pgvector/pgvector:pg16` service** (trust auth, no credential literal; suite CREATE/DROPs throwaway DBs per test). e2e-smoke real; docker-build a documented no-op.
- **Local dry run green by code:** ruff✓ · mypy --strict (47 files)✓ · **pytest 24 passed + 6 xfailed** (xfail = Week-2 `/auth/login`) · app boots, `/health` + Scalar `/docs` 200, `POST /agent/conversations` = `text/event-stream` · placeholder E2E smoke 2 passed.
- **GitGuardian remediation:** first scan flagged **2 test/ephemeral creds** (no real secrets, nothing rotated) — the CI service `postgres:postgres` (→ `POSTGRES_HOST_AUTH_METHOD=trust`) and the pinned fixture test password (→ **env-derived** `HEARTH_TEST_PASSWORD`, default a `CHANGE_ME` placeholder; manifest regenerated). Both literals scrubbed from history with **range-scoped `git filter-repo`** (`7745495..backend`) so the scaffold base shared with `dev` stayed byte-identical; force-pushed (authorized). A `backup/pre-scrub` safety ref caught a first whole-branch rewrite that severed the common base.
- **Merged:** **PR #3** `integration/backend → dev` — all checks green incl. **GitGuardian *No secrets detected ✅*** — merged as `385e541` (merge commit preserving per-day history). **PR #1** (foundations) auto-marked **merged** (commits on `dev`); the design line was included in the same merge (never needed its own PR). No open PRs remain.
- **Week 1 is complete.** `dev` now holds the full W1 foundation: monorepo + compose, brand/design system + 7 hi-fi screens, Postgres schema/migrations/ORM/seed, golden eval set, locked API contract v0 + Scalar docs, E2E fixture spine, and a real CI gate.

## ✅ Done — Day 6 (API contracts & E2E fixtures — both stories integrated)
- **US-E4-00** API contract inventory `[REVIEW ✓]` — Orion. Full contract for **36 operations** (auth 4 · products/catalog 5 · search 1 · cart 4 · orders 6 · returns 3 · agent 3 · seller 6 · admin 3 + health). Human **ratified all 6 decisions** (one changed the draft): opaque **session-token** auth · `{data, meta}` envelope · **cursor** pagination · **keyword search** semantic-ready (pgvector deferred, stays embedding-agnostic) · **agent chat = SSE `text/event-stream`** token→citations→done (the streaming generative-UI surface) · **two-step payment** intent→confirm w/ deterministic decline (`outcome="failed"` → `402 payment_declined`). Pydantic v2 schemas (`api/app/schemas/`), router stubs (501, `api/app/api/routers/`), **Scalar** docs at `/docs` (Swagger disabled). Handlers are stubs — Week-2 implements. `docs/api/contract-v0.md` (decisions → RATIFIED) + **ADR-0021**. FF-merged `feature/api-contracts-inventory` (`5438f73` draft → `7745495` locked) → `integration/backend`.
- **US-QA-D06** E2E fixture plan `[AFK]` — Juno (Sable concerns baked in). **One canonical source → 3 layers** (API httpx + session-token / Playwright testids / agent golden `source_docs`). `docs/qa/fixture-plan.md` (catalog + per-journey matrix, all **17 J-*** mapped + factory plan), `api/tests/fixtures/` + `fixture-manifest.json` (shared handles, anti-drift spine test), `api/tests/conftest.py` fixtures (`seeded_db`, `handles`, `api_client`, `persona_client`), `e2e/tests/_fixtures.ts` for the browser layer. Personas (buyer×2/seller×2/support/admin) + stock-edge products (in-stock/low/oos) + policy keys all resolve to real seed rows. **J-BUY-04** wired to the deterministic payment decline. FF-merged `feature/api-contracts-fixtures` → `1139be1`.
- **Atlas integration check:** combined `integration/backend` gate green **by code** on `pgvector/pgvector:pg16` — `ruff` clean · `mypy --strict` clean (47 files) · **pytest 24 passed + 6 xfailed** (xfails = login pending Week-2 auth). App boots, `/docs` 200, agent routes `text/event-stream`.
- **Known Week-2 follow-ups (non-blocking):** seed has no passwords (fixture pins test passwords → auth-backed seeding flips 6 xfails); 8 journey factories specified but await Week-2 cart/order/return handlers; `order_items` has no `store_id` (contract §4.3 open item, fine for v0 no-delete seed).

## ✅ Done — Day 5 (Seed, data-access & RAGAS data baseline — all 3 stories integrated)
- **US-E3-04** SQLAlchemy models / DA layer `[AFK]` — Sable. SQLAlchemy 2.0 typed ORM (`Mapped`/`mapped_column`) for all **21 tables** on the shared `Base`, split by domain under `api/app/db/models/`. Native PG enums referenced by name (`create_type=False`, labels mirrored from 0001); server defaults mirrored (uuidv7/now/jsonb/bools); partial-unique cart + email-live indexes; `embeddings.embedding = Vector(settings.embed_dim)` (never hardcoded) + HNSW. Thin DA layer `app/db/session.py` (engine + `sessionmaker` + `session_scope`). **Parity-proven vs a real migrated DB**: `test_no_metadata_diff` (Alembic `compare_metadata` → no drift; inline-UNIQUE vs `uq_*` name-only renames filtered), per-table column parity, core round-trip (user→store→product→variant→inventory + embedding at embed_dim). FF-merged `feature/data-access-models` → `9337dcc`.
- **US-E3-03** Seed: catalog/users/policies `[AFK]` — Sable. `python -m app.db.seed` (Hearth maker marketplace) via the ORM: 12 users (all 4 roles), 5 stores, 10 products, 15 variants (real SKUs/price_minor/options), 15 inventory (low-stock + restock_eta spread), images, 12 reviews (1–5 CHECK; **rating rollups recomputed**), **7 policies** (platform returns/shipping/payments/platform + per-store care/returns — genuine quotable RAG prose), 17 embeddings. **Idempotent** (upserts on natural keys); wired into `db_reset.sh` SEED hook (`SEED=0` opt-out). Embeddings via a **single Echo-replaceable `embed_text` stub** (`model='seed-stub'`, length=`settings.embed_dim`). ADR-0020. FF-merged `feature/data-access-seed` → `e4314e4`.
- **US-QA-D05** Golden eval dataset v0 `[AFK]` — Juno (Echo/Sable concerns baked in). **25 buyer/support Q&A** in `docs/qa/eval/golden-v0.jsonl` (JSONL, RAGAS/HF-native): each has `question`, grounded `expected_answer`, machine-checkable `source_docs` (product slug / variant sku / policy `kind@store|platform`), and `tags` (persona/category/difficulty/`expects_refusal`). Spread: buyer 18 / support 7; **4 refusal items** (faithfulness = don't fabricate); embedding-agnostic. Validator `api/tests/qa/test_golden_eval.py`: static well-formedness + **DB-backed seed-resolve** (reuses Sable's `migration_db` fixture → every source_doc resolves to a real row = anti-drift). README maps records → RAGAS metrics + Echo's Week-2 runner work. FF-merged `feature/qa-golden-eval` → `b456a16`.
- **Atlas integration check:** combined `integration/data` gate green **by code** on `pgvector/pgvector:pg16` — `ruff` clean, `mypy --strict` clean (22 files), **pytest 16 passed** (incl. DB-backed seed-resolve + migration contract, no skips).

## ✅ Done — Day 4 (Data model — ERD approved + schema/migrations)
- **US-E3-01** ERD `[REVIEW ✓]` — human-approved with rulings (added `reviews` + minimal `payments`; kept sessions/conversations/messages/agent_actions; single multi-store order with per-item snapshot; `EMBED_DIM`-driven vector dim). `docs/data/erd.md` flipped to APPROVED; §6/§7 resolved inline.
- **US-E3-02** Postgres schema + Alembic + pgvector `[AFK]` — Alembic initialized in `api/` (env-driven `DATABASE_URL`, no creds committed). 3 migrations: 0001 pgvector ext + 14 native enums; 0002 core schema (20 tables, FKs, CHECKs, partial-unique one-open-cart, UNIQUEs on email-live/slug/sku/order_number/inventory.variant_id, UUIDv7 fn+defaults); 0003 embeddings `vector(EMBED_DIM)` + HNSW cosine. **Proven on a fresh DB: `upgrade head` → `downgrade base` → `upgrade head` all clean** (21 tables/14 enums create+drop; uuidv7 nibble=7; dup-open-cart + review-rating CHECK enforced). ruff + mypy(strict) green. ADR-0018. Committed `feature/db-schema-migrations` → FF `integration/data`.
- **US-QA-D04** Migration contract `[AFK]` — turned Sable's one-off proof into a repeatable, CI-runnable gate. pytest `api/tests/db/` (per-test throwaway CREATE/DROP DATABASE, asserts **by name**): clean-create → 21 app tables + 14 enums + pgvector + `uuid_generate_v7()` + HNSW index (access method verified) + one-open-cart partial-unique + reviews CHECK + inventory UNIQUE; rollback → 0 tables/0 enums/fn gone/empty `alembic_version`, then idempotent `upgrade head` back to `0003_embeddings`. Guarded `api/scripts/db_reset.sh` (refuses non-dev DB names, idempotent, seed-hook for US-E3-03). **Proven on `pgvector/pgvector:pg16`: pytest 3 passed; reset guard+reset+idempotent re-run all ✓; ruff + mypy(strict) green.** Documented exception: `0001` downgrade intentionally leaves the `vector` extension (asserted, not a bug). CI = Postgres-service `test-api` job **documented in the plan, wiring deferred to Day-7** so the shared gate stays green. ADR-0019. Committed to `integration/data`.
- **Echo coordination:** `EMBED_DIM` defaults to **768** (Gemini text-embedding-004) so migrations apply today; Echo confirms the final embedding model → set `EMBED_DIM` + re-run the embeddings migration (one step). Not a blocker.

## ✅ Done — Day 3 (Design system & key screens — all 3 stories integrated)
- **US-E2-04** Design tokens → code `[REVIEW ✓]` — **Tailwind v4** (CSS-first, no config), every `palette-and-type.md` token as CSS vars on `:root`+`[data-theme=dark]`, `@theme inline` mapping; Fraunces/Outfit via `next/font`; `Button` (ember+ink primary / clay+white brand), `HearthMark` duotone, `/tokens` specimen + theme toggle. Human approved after a dark-mode tint-badge contrast fix (`text-n-900`). ADR-0015. Merged `915ac62`.
- **US-E2-05** Hi-fi screens `[REVIEW ✓]` — 7 token-truthful static HTML screens (home, search, product, cart, checkout, order-status, seller-dashboard) + index board, in `docs/brand/preview/screens/`. Carry the `data-testid` registry → double as Iris's build contract. Agent voice (Ember) is the signature surface; grounded recs + honest refusals + seller merch nudge. Human approved w/ tweaks: **qty steppers** (not dropdowns) + richer photo placeholders. ADR-0017. Merged.
- **US-QA-D03** UI E2E skeleton `[AFK]` — 17 `J-*` `test.fixme` specs (1:1 w/ QA matrix), `data-testid` registry (`docs/qa/test-ids.md` + typed `_selectors.ts`); agent voice = shared `agent-*` namespace. Smoke stays green (2 passed / 36 skipped, no server). ADR-0016. Merged.
- ADR collision fixed (both agents grabbed 0015 → QA renumbered to 0016); `.claude/skills/`+`worktrees/` gitignored.

## ✅ Done — Day 2 (shipped, all 4 [REVIEW] approved by human)
- **US-E2-01** Brand brief — name **Hearth**, positioning, tone, personas (buyer/seller/support/admin), design hand-off. `docs/brand/brand-brief.md`.
- **US-E2-02** Logo — human locked **"Keystone"** (round 1 of 3 rejected → round 2 of 6). Code-native SVG, not Stitch (ADR-0014). Canonical: `docs/brand/assets/hearth-logo-{mark,lockup}.svg`.
- **US-E2-03** Palette + type — **Clay `#B5512F`** primary / **Ember `#E08A3C`** agent-voice accent; **Fraunces** (serif display/wordmark) + **Outfit** (sans UI); warm-neutral ramp, semantic, light+dark, WCAG AA verified. Spec `docs/brand/palette-and-type.md`, specimen `docs/brand/preview/palette-and-type.html`. Lockup wordmark recut to Fraunces (TODO Day-3: outline to paths once font bundled).
- **US-QA-D02** QA matrix v0 — 4 personas, 17 journeys (`J-*` IDs, 1:1 to D03 E2E), 9 WCAG AA checks, 5 RAGAS gates (**faithfulness ≥0.90** per sign-off). `docs/qa/qa-matrix.md`.
- 4 feature branches FF-merged to `integration/design` (commits `d00c447`→`397f6ec`). ADR-0014.

## ✅ Done — Day 1
- Branch tree `main`→`dev`→`integration/foundations`; remote policy = push + real PRs.
- US-E1-01/02/03/04 + US-QA-D01: monorepo, docker-compose (pgvector, live), CI skeleton, Playwright e2e smoke, CONTRIBUTING. ADRs 0009–0013.
- **PR #1** (`integration/foundations` → `dev`): 9/9 green incl. GitGuardian. Held for the Day-7 gate.

## 🔧 In progress
- _nothing mid-flight_ — Day 9 closed on `integration/backend` (`b51f746`), pushed. Day 10 (catalog & search) not started.

## ⛔ Blocked
- _nothing_

## 🙋 Needs your decision
- **Week 4 (deploy):** free-tier keys — Vercel, Render/Fly, Supabase/Neon, Groq/Gemini.
- **Echo embedding model (Week-2):** confirm the final model → set `EMBED_DIM` + re-run migration `0003` (currently 768 = Gemini text-embedding-004).
- _(ERD [REVIEW] resolved Day 4; merge strategy + GitGuardian remediation resolved Day 7.)_

## ⏭️ Next up
- **Day 10 (2026-06-18) — Catalog & hybrid search (all `[AFK]`):** **US-E4-06** catalog list/detail + inventory (Orion); **US-E4-07** hybrid search (pgvector + keyword) + rerank stub, built behind contract Decision-4's `SearchMode`/`score` shape (Orion/Sable); **US-E4-08** embedding generation on product change — replaces the `embed_text` `seed-stub` hook (Sable/Echo). Protect write routes with the now-real `require_user`/`require_role`. **Echo coordination lands here:** US-E4-08 is the first real-embedding touchpoint → confirm the model/`EMBED_DIM` (currently 768) and re-run migration `0003` if it changes.
- **Rest of Week 2:** cart/orders (Day 11, US-E4-10 `[REVIEW]` payment) · agent tools + SSE loop (Day 12, Echo) · RAGAS harness + regression pack (Day 13) · **W2 gate + `dev` PR (Day 14, US-QA-D14 `[REVIEW]`)**. Echo still owns the SSE agent loop + RAGAS runner. Accumulate on `integration/backend`, green by code.
