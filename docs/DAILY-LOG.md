# DAILY LOG

> One line per day. Append at end of day. Format: `YYYY-MM-DD — <shipped> | <blocked/notes>`

2026-06-09 — Project scaffold generated (CLAUDE.md, 8 agents, docs, 4-week plan, ADRs) | awaiting repo init + Week 1 kickoff
2026-06-09 — W1D1 shipped: baseline→main, branch tree (dev/integration/foundations), CONTRIBUTING (US-E1-02), monorepo (US-E1-01), docker-compose+pgvector live-verified (US-E1-03), CI skeleton (US-E1-04), Playwright e2e smoke (US-QA-D01). PR integration/foundations→dev open | none blocked
2026-06-10 — W1D2 shipped (all 4 [REVIEW] approved): brand brief→name **Hearth** (US-E2-01), logo **Keystone** picked after 2 rounds, code-native SVG not Stitch/ADR-0014 (US-E2-02), palette **Clay/Ember** + **Fraunces/Outfit** type WCAG-AA (US-E2-03), QA matrix v0 — 4 personas/17 journeys/9 a11y/5 RAGAS gates (US-QA-D02). Rendered brand artifacts via Playwright/Chromium. integration/design @ 397f6ec | none blocked
2026-06-11 — W1D3 shipped (all 3 integrated): design tokens→code **Tailwind v4 + CSS vars** (US-E2-04, [REVIEW]✓ after dark-mode contrast fix, ADR-0015); **7 hi-fi screens** code-native + token-truthful + carry testid registry (US-E2-05, [REVIEW]✓ w/ qty-stepper + photo tweaks, ADR-0017); **UI E2E skeleton** 17 J-* fixme specs + testid registry (US-QA-D03, ADR-0016). ADR collision 0015→0016 fixed; skills/worktrees gitignored. integration/design pushed | none blocked
2026-06-12 — W1D4 shipped (all 3): ERD **[REVIEW]✓** w/ rulings — +reviews +payments, kept agent/session tables (US-E3-01); **schema → Alembic + pgvector** (US-E3-02) — 3 migrations (ext+14 enums / 20-table core / embeddings vector(EMBED_DIM)+HNSW), UUIDv7 in-DB fn, proven on fresh DB upgrade↔downgrade↔upgrade clean, ADR-0018; **migration contract** (US-QA-D04) — pytest throwaway-DB asserts-by-name for clean-create/rollback/round-trip + guarded db_reset.sh w/ seed-hook, 3 passed, ADR-0019. ruff+mypy(strict) green throughout. integration/data @ e7642b5 (not pushed, held for Day-7) | EMBED_DIM=768 default pending Echo's model pick (not blocking)
2026-06-13 — W1D5 shipped (all 3): **SQLAlchemy 2.0 ORM** for all 21 tables on shared Base + thin DA layer, parity-proven vs migrated DB (no metadata drift), Vector(EMBED_DIM) never hardcoded (US-E3-04, `9337dcc`); **idempotent Hearth seed** `python -m app.db.seed` wired into db_reset.sh hook — 12 users/5 stores/10 products/15 variants/15 inventory/12 reviews/7 policies/17 embeddings, Echo-replaceable embed stub (US-E3-03, ADR-0020, `e4314e4`); **golden eval dataset v0** — 25 buyer/support Q&A w/ expected answer + machine-checkable seed source_docs + tags, 4 refusal items, DB-backed seed-resolve validator (US-QA-D05, `b456a16`). Full combined gate green by code: ruff✓ mypy(strict, 22 files)✓ pytest **16 passed**. integration/data @ b456a16 (not pushed, held for Day-7) | Echo to swap real embeddings + RAGAS runner Week-2 (not blocking)
2026-06-14 — W1D6 shipped (both, on new `integration/backend` cut from `integration/data`): **API contract v0 [REVIEW]✓** — 36 ops (auth/catalog/search/cart/orders/returns/agent/seller/admin), human ratified 6 decisions (session-token auth, `{data,meta}`+cursor envelope, keyword search semantic-ready, **agent chat = SSE**, two-step payment w/ deterministic decline), Pydantic v2 schemas + 501 router stubs + **Scalar** `/docs`, ADR-0021 (US-E4-00, `7745495`); **E2E fixture spine [AFK]** — one canonical source → API/browser/agent, 17 J-* journeys mapped, personas/products/policies resolve to real seed rows, anti-drift spine test (US-QA-D06, `1139be1`). Full gate green by code: ruff✓ mypy(strict, 47 files)✓ pytest **24 passed + 6 xfailed** (login xfails pending Week-2 auth). integration/backend @ 1139be1 (not pushed — Day-7 pushes) | seed has no passwords yet → 6 fixture xfails flagged for Week-2 auth-backed seeding (not blocking)
2026-06-15 — W1D7 **integration capstone shipped** (US-QA-D07, [REVIEW]✓): all four W1 lines merged → **`dev`** (`385e541`). Lines were a strict linear chain (`foundations ⊂ design ⊂ data ⊂ backend`) → **1 consolidated PR #3**, zero cross-branch conflicts. **Real CI gate wired** (Rhea) — web+api lint/type-check + **pgvector `test-api`** job (the deferred Day-4 Postgres-service job), docker-build documented no-op. Local dry run green by code: ruff✓ mypy(strict,47)✓ **pytest 24 passed + 6 xfailed**, app boots `/health`+Scalar `/docs` 200, agent SSE `text/event-stream`, e2e smoke 2 passed. **GitGuardian** flagged 2 test/ephemeral creds (CI `postgres:postgres`, fixture test password) → CI switched to trust auth, fixture password **env-derived** (`HEARTH_TEST_PASSWORD`); both literals scrubbed from history via range-scoped `git filter-repo` (base shared with `dev` preserved), force-pushed (authorized). PR #3 all checks green incl. **GitGuardian clean**; merged. PR #1 auto-merged. | none blocked — Week 1 complete
2026-06-16 — W2D1 (Day 8) shipped (both `[AFK]`, on `integration/backend`): **reconciliation** — planned Day-8 skeleton/docs/schemas (US-E4-01/02/03) were front-loaded into Day 6 & already on `dev`; scoped Day-8 to the two real gaps. **US-E4-01 gap** — runtime **canonical error envelope** (`app/core/errors.py`: `APIError` + handlers for `APIError`/`HTTPException`/`RequestValidationError`/catch-all → `{"error":{code,message,details}}` closed `ErrorCode`; `stub()`→501 `not_implemented`) + **CORS** (env-driven `cors_origins` + `CORSMiddleware`), ADR-0022, `5e7ed35`. **US-QA-D08** — named DB-free API smoke suite (15 tests: boot/router-inventory/`/docs`/`/openapi.json`/CORS pos+neg/cross-group envelope), `6727bb5`. Gate green by code: ruff✓ mypy(strict,48)✓ **pytest 34 passed/15 skipped**; live 501/404/422 envelopes + CORS preflight verified. FF→`integration/backend` (`6727bb5`), pushed; `dev` PR held for Day-14 W2 gate | none blocked — Day-9 auth (US-E4-04 `[REVIEW]`) next

- **2026-06-17 (Day 9 — Authentication):** shipped real `/auth/*` (argon2id · sliding-7d opaque session tokens · `email_verified` login gate · migration 0004 · auth-backed seeding) + authZ scope dep (`require_user`/`require_role`) + auth E2E. **6 fixture login xfails flipped green.** US-E4-04 `[REVIEW]` ratified (ADR-0023; `.test`-TLD shim ADR-0024). Gate green by code (ruff✓ mypy --strict 72✓ pytest 73 passed). On `integration/backend` @ `b51f746`, pushed; `dev` PR held for Day-14. Blocked: none.
- **2026-06-18 (Day 10 — Catalog & hybrid search):** shipped all 4 `[AFK]` — **catalog** list/detail + `/stores` + inventory signal (`app/services/catalog.py`, keyset cursor `app/api/pagination.py`, US-E4-06, `fda3d9e`); **embedding provider seam** + `refresh_product_embedding` on change (`EMBED_PROVIDER`, default `StubEmbedder`; Echo's real-model swap = one line; US-E4-08, ADR-0026, `0202e4f`); **search** `GET /search` keyword LIVE via Postgres FTS (migration **0005** GIN `tsvector`/`ts_rank`, `meta.mode`) + inert semantic/rerank seam = hybrid later with no contract change (US-E4-07, ADR-0027, `aeac4eb`); **search E2E + no-LLM retrieval smoke** (precision/recall@5 captured; US-QA-D10). **Honest finding:** keyword recall on NL questions = 0 (FTS ANDs tokens) → quantifies the gap semantic retrieval closes; endpoint proven live on distinctive terms. Hygiene: untracked/gitignored smoke run-artifacts (`eecc97a`). Gate green by code: ruff✓ mypy --strict✓ **pytest 103 passed, 0 failed**. `integration/backend` @ `eecc97a`, pushed; `dev` PR held for Day-14. Blocked: none. | Echo's embedding-model `[DECISION]` non-blocking (seam ready); Day-11 payment is a `[REVIEW]`.
- **2026-06-19 (Day 11 — Cart & orders):** shipped 2 `[AFK]` + 1 `[REVIEW ✓ human-approved]`. **US-E4-09** cart — all 4 `/cart` handlers + idempotent upsert over `app/services/cart.py`, no inventory mutation (`1fa81ee`). **US-E4-10** orders/checkout + **two-step test-mode payment** over `app/services/orders.py`: reserve-at-checkout (`qty_reserved+=qty`, atomic, `409 out_of_stock`/`empty_cart`), payment-intent→confirm (`captured`→capture+restore; `failed`→**402 `payment_declined`**+release, order stays `placed`), idempotency via `idempotency_keys` table (migration **0006**), flat $0 ship/tax v0, ADR-0028 (`5c7edf2`). **US-QA-D11** checkout E2E (J-BUY-04) asserts the full reserve→capture/release lifecycle + idempotency + atomic OOS (`deb6b05`). Human `[REVIEW]` approved reserve-at-checkout + ratified payment flow. Gate green by code: ruff✓ mypy --strict (90)✓ **pytest 137 passed, 0 failed**; migration 0006 up→down→up clean. `integration/backend` @ `deb6b05`, pushed; `dev` PR held for Day-14. Blocked: none. | Day-12 agent tools (Echo) next; Echo's embedding-model/`_ACTIVE_MODE` `[DECISION]` still non-blocking.
- **2026-06-20 (Day 12 — Agent tools):** shipped all 3 `[AFK]`. **US-E5-01/02** new `api/app/agent/` package — 8 typed tools (search/productDetails/inventory/orderStatus reads · addToCart/draftOrder mutating · refund sensitive · applyCoupon pure) + provider-agnostic registry (`ToolSpec.provider_schema()`, no Groq/Gemini) + `execute_tool` executor (validate→scope→real-service→idempotency `tool:<name>`→bounded retry/timeout→`AgentAction` audit; exhaustion→`rate_limited` 429); v0 shims: coupons=static map, refund=mark captured Payment→refunded (returns epic deferred). ADR-0029 (`05cb623`,`b82d92f`). **US-QA-D12** exhaustive per-tool E2E matrix — success×validation×unauthorized for all 8 + idempotency/retry/audit (`4273454`). Gate green by code: ruff✓ mypy --strict (98)✓ **pytest 212 passed, 0 failed** (137→212); no migration. `integration/backend` @ `a25c13d`, pushed; `dev` PR held for Day-14. Blocked: none. | Carried `[REVIEW]`: refund guardrail (spend cap + HITL threshold) — ADR-0029, non-blocking. Day-13 RAGAS harness + regression pack next.
2026-06-22 — W2D7 **Week 2 complete** (US-QA-D14, [REVIEW]✓): the full Week-2 backend (Days 8–13) merged → **`dev`** (`cc23501`) via **PR #5**, all CI green incl. **GitGuardian ✅**. Single consolidated PR (W1 Day-7 pattern). Ships on `dev`: error-envelope+CORS, argon2 auth+sessions, catalog+keyword-search (semantic seams), cart+orders+two-step payment, the 8-tool agent layer+executor, the RAGAS harness (ADR-0030), and a clean-seed regression pack. CI remediation by code: added `argon2-cffi` to `requirements.txt`; scrubbed a GitGuardian "Generic Password" false-positive (test passwords) + literal test-DB creds from docs via scoped `filter-repo` (human-authorized clean history). | none blocked — Week 2 complete; Week 3 next
- **2026-06-24 (Day 16 — Support agent · RAG · guardrails):** shipped 3 `[REVIEW]` (ratified up front, **ADR-0033**) + 1 `[AFK]`, integrated to `integration/agents` @ `dd22f15`. **US-E5-05** hybrid RAG over catalog + policies — product `SemanticRetriever`+`HybridRetriever` (RRF) behind the seam (key-free default), new `app/services/rag.py` policy retriever (on-the-fly FTS, no migration), citations carry golden `source_docs` keys (`b1a8d21`). **US-E5-07** guardrails — `app/agent/guardrails.py`: tiered refund caps (AUTO ≤$50 / HITL $50–$200→`hitl_deferred` / REFUSE >$200, config minor-unit caps) in the executor + strict-boundary injection defense (detect-log-refuse over user turns AND retrieved passages; identity/scope/tier never settable by content); adversarial injection tests pass (`456e813`). **US-E5-06** support agent — filled the `support` node: grounded policy Q&A + order status + refunds under the tiers; injectable `SupportBrain` ⇒ key-free CI; SSE citations now real (`5869601`). **US-QA-D16** RAGAS v1 gate (`app/eval/gate.py`) — floors (faithfulness ≥0.90 · relevancy ≥0.80 · recall ≥0.70) on the real support-RAG path, **armed only under the live Claude judge** (`claude-opus-4-8`, lazy import), CI = deterministic smoke; refusal + injection gate deterministically; one tracked defect (EVAL-025 keyword-retriever mis-rank, closes under armed/semantic path) (`dd22f15`). Gate green by code: ruff✓ mypy --strict (127)✓ **pytest 300 passed, 0 failed**; no migration. Pushed; `dev` PR held for Day-21 W3 gate. Blocked: none. | Closes ADR-0029 refund fork + confirms ADR-0030 Claude judge. Open follow-up (non-blocking): one LOCAL armed RAGAS run (`ANTHROPIC_API_KEY` + `pip install anthropic` + `EMBED_PROVIDER=gemini`). Day-17 merchandising agent + LLM routing next.
- **2026-06-16 (Day 17 — Merchandising agent · LLM routing/fallback · semantic cache):** shipped 2 `[AFK]` + 1 `[REVIEW ✓ ADR-0034]`, integrated to `integration/agents` @ `ec741db`. **US-E5-09** Groq→Gemini **failover** (central `build_failover`; auto-cascade on transient 429/timeout/5xx after bounded retry; domain `APIError` never falls back; both exhausted→`rate_limited` 429) + **non-personalized semantic cache** (Option A — caches intent-classification + grounded policy answers only; ordering scan→lookup→LLM→store; NEVER-cache shopping/tool-result/PII/refusal/injection-fired; pgvector store migration **0007**, cosine≥0.95, stub→exact-match; content-version + TTL invalidation) (`2e9649a`). **US-E5-08** merchandising agent — DRAFT listing + comparables-grounded price suggestion (basis surfaced), **never auto-publishes**, guardrail-first, injectable `MerchBrain` (`4822f34`). **US-QA-D17** agent eval report (`app/eval/agent_report.py`) over the 4 ADR-0034 areas carrying pass/fail·token cost·latency·failing traces — fallback cascade, cache cold-vs-warm delta, merch draft quality (smoke armed under live judge), **tool-call F1=1.000** from audit rows; deterministic areas gate in CI key-free (`ec741db`). **`[REVIEW]` resolved up front via ADR-0034** (Echo+Orion both independently recommended Option A; human ratified after a cross-user-sharing clarification). Juno's subagent hit a spend cap mid-run → Atlas verified its files, ran the gate, committed US-QA-D17. Gate green by code: ruff✓ mypy --strict (136)✓ **pytest 333 passed, 0 failed**; migration 0007 (EMBED_DIM stays 768). Pushed; `dev` PR held for Day-21 W3 gate. Blocked: none. | Day-18 frontend foundation (Iris) next.
- **2026-06-26 (Day 18 — Frontend foundation):** **Frontend line begins** — fresh `integration/frontend` off `dev` @ `40127b4`. Shipped 4 stories. **US-E6-02 `[REVIEW ✓ ADR-0035]`:** component foundation = **shadcn/ui on Hearth tokens** (human pick; Radix a11y, we own source, Magic composes on top). **US-E6-01** scaffold reconcile (App Router + TS strict + Tailwind-v4-from-tokens; `(shop)` shell). **US-E6-03** app shell (header/nav/account-menu/mobile-nav/footer/skip-link) + `/login`+`/register` wired to the contract via httpOnly-cookie route handlers; registry test-ids. **US-QA-D18** browser smoke (route health · auth screens · app shell · **axe a11y**) on **desktop + mobile** viewports, self-booting webServer; @smoke stays server-free. **Juno's axe baseline caught a real WCAG AA gap; Atlas root-caused it to `cn()` tailwind-merge dropping the foreground class off sized Buttons** (named font-size tokens collided with `text-{color}`), fixed via `extendTailwindMerge` + `--on-brand`/`--accent-text` roles + a focusable scroll region. Gate green by code: web lint✓ type-check✓ build✓ (15 routes) · **e2e @web 30/30 (both viewports)** · @smoke 4/4. `integration/frontend` @ `9f86966`, pushed; `dev` PR held for the Day-21 W3 gate. Blocked: none. | Day 19 (W3D5) frontend core flows next (catalog/search UI + streaming chat).
- **2026-06-27 (Day 19 — Frontend core flows):** shipped US-E6-04 (catalogue search + PDP), US-E6-05 `[REVIEW ✓]` chat assistant, US-QA-D19 UI E2E — on `feature/fe-core-flows` → `integration/frontend` @ `c848c45` (pushed). **US-E6-05 went two rounds:** Iris first built a **persistent docked panel** (ADR-0036); Atlas rendered it in real Chromium for the human, who **reworked the layout → conversation-first** (`/search` IS the conversation, rec cards inline in Ember's replies). ADR-0036 revised; Iris rebuilt (removed the dock, one `agent-chat-panel` in DOM); re-rendered → **human approved.** All over deterministic mock SSE/cart endpoints (ADR-0037; backend not live — flip-to-live is config at the W3 gate). **US-QA-D19 recovery:** Juno's subagent hit the monthly spend cap before writing anything, so **Atlas wrote the UI E2E directly** — `e2e/tests/ui-flows.spec.ts` (13 ×2 viewports): streaming happy path/citations/inline cards/card→PDP, loading+thinking, clarify/refusal/mid-stream-error, empty-conversation, PDP add-confirm/sold-out-OOS/404, axe a11y. **Found+fixed a real PDP a11y bug** (sold-out variant `opacity-60` → 2.91:1 contrast). Gate green by code: web lint/type-check/build✓ (17 routes) · **e2e @web 56 passed** (30 smoke + 26 ui-flows, both viewports) · @smoke 4/4. `dev` PR held for the Day-21 W3 gate. Blocked: none. | Flagged: conversation-first rework orphaned the grid testids (search-results/-result-card/-empty-state) + GET /api/search — cleanup follow-up. Day 20 (W3D6) cart/checkout UI + seller dashboard next.
- **2026-06-28 (Day 20 — Cart/checkout UI & seller dashboard):** shipped US-E6-06 `[REVIEW ✓ human-locked]` cart + **agentic-checkout approval gate**, US-E6-07 `[AFK]` order status/returns + seller merch dashboard, US-QA-D20 `[AFK]` full shopping-journey E2E — on `feature/fe-checkout-seller` → `integration/frontend` @ `903379e` (pushed). **US-E6-06 approval gate:** Iris built it; Atlas rendered it in real Chromium (Day-19 lesson) presenting inline-gate vs modal vs chat-inline → **human chose "lock it as-is"** (ADR-0038): an inline "Approval needed" card in the order-summary rail, Ember-voiced, Approve/Cancel, no money side-effect before Approve; maps to the agent runtime's LangGraph `interrupt()`. Two-step payment (`POST /orders`→intent→confirm, `outcome=failed`→402 decline + graceful retry). All over deterministic mocks (ADR-0037; flip-to-live = config at the W3 gate). **US-QA-D20:** `shopping-journey.spec.ts` (12 ×2 viewports) — happy path, gate-is-a-real-gate, declined-card, cart mutations, returns + out-of-window HITL, seller nudges/fulfil, axe clean; emits an agent-transcript artifact. Gate green by code: web lint/type-check/build✓ (27 routes) · **e2e @web 80 / @smoke 4** · axe 0 serious/critical. `dev` PR held for the Day-21 W3 gate. Blocked: none. | Carried (non-blocking, fix at the gate): additive UI-only fields confirm; no delivered+out-of-window seed order (HITL data-spine gap, Iris fix-forward); sandbox blocks build-time Google-Fonts fetch (CI has network). Day 21 (W3D7) = the W3 integration gate, merge BOTH lines to `dev`.
- **2026-06-29 (Day 21 — W3 integration gate):** **🎉 Week 3 COMPLETE.** US-QA-D21 `[REVIEW ✓ human-ratified]` — merged BOTH parallel Week-3 lines to `dev` via a single consolidated **PR #6** (W1 Day-7 / W2 Day-14 pattern): the agent layer (Days 15–17, `a11bd08`) + the frontend (Days 18–20, `2fcac6f`), both off `dev` @ `40127b4`, reconciled on `integration/w3-gate` and merged as **`4b3fc5b`** (merge commit, per-day history preserved). **Scope call (ADR-0039, human-ratified):** merge **on mocks**, defer flip-to-live to Week 4 — reading both trees showed it's real adapter work (`price_cents`→`price_minor`, three client-derived UI fields, auth wrapping; `CamelModel` keeps snake_case so no camelCase mismatch), not a config flip. **Pre-merge regression (Juno, by code):** agents line **pytest 333/0** + **RAGAS gate v1 PASS** (keyless deterministic judge = SMOKE by design) + ruff/mypy clean; frontend lint/type-check ✓ + `@smoke 4/4`. **Merge:** disjoint code subtrees ⇒ zero code conflicts; 5 doc/config conflicts resolved chronologically; **proved zero code drift** (api/ ≡ agents line, web/+e2e/ ≡ frontend line). **CI all green on PR #6** incl. pgvector `test (api)` + GitGuardian. | Carried to Week 4 (non-blocking): full `@web 80` browser lane is not in CI (only server-free `@smoke 4`) + was font-blocked locally → re-prove in Week 4; ADR-0039 flip-to-live items = Day-22 work. Day 22 (W4D1) = full-stack E2E integration + flip-to-live.
